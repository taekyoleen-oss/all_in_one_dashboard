# PLAN — 길찾기(도보) 위젯

작성일 2026-09-06 · 갱신 2026-09-07 · 상태: **P0·P1·P3·P4 완료 — 요구 1·2·3 전부 동작. 남은 것: P5(문서·이력 정리)**

**확정된 결정**
- 지도 = **A안(TMAP StaticMap 이미지 + 자체 SVG 오버레이)** — 앱키 서버 전용, 새 의존성 0
- TMAP 앱키 = `.env.local`의 `TMAP_APP_KEY`

### P4 ✅ **완료** (2026-09-07) — 현재 위치 추적 + 다음 안내 (요구 2) · **요구 1·2·3 전부 동작**

**실브라우저 E2E**(Playwright로 GPS 좌표를 실제 이동시키며 검증, 임시 승인 계정은 삭제 — 잔여 0행):

| 상황 | 배너 | 경로 요약 |
|------|------|-----------|
| 출발 직후 | "40m 앞 **우회전** / 이태원역 3번출구에서 우회전 후 27m 이동" | 3.0km · 40분 |
| 경로 따라 30% 이동 | "170m 앞 **횡단보도**" | 3.0km · 40분 ← **변화 없음** |
| 70% 이동 | "60m 앞 **좌회전**" | 3.0km · 40분 ← **변화 없음** |
| 경로 350m 이탈 | "**경로에서 벗어났습니다** / 경로까지 약 270m" + [다시 계산] | 3.0km · 40분 |
| [다시 계산] 클릭 | "150m 앞 좌회전" | **2.1km · 29분** ← 현재 위치 기준 새 경로 |
| 도착지 도달 | "**도착했습니다**" | — |
| GPS 정확도 120m로 53m 벗어남 | 정상 안내 유지(오판 없음) | — |

걸어가는 동안 **요약이 그대로**인 것이 "경로는 한 번만 부른다"(계획서 §3)의 증거다 — 안내만 다시 계산되고 TMAP 호출은 늘지 않는다. 배너·지도의 파란 점·고도 그래프의 세로선이 항상 같은 위치를 가리킨다.

tsc 0 · 신규 lint 0 · **test 109/109**(신규 11) · build 그린.

**만든 것**: `lib/widgets/route/guidance.ts`(+테스트 11 — turnType 라벨표, 다음 안내 선택) · `components/widgets/route/NextGuidance.tsx` + RouteBody 연결.

**이번에 잡아 고친 진짜 결함: 좌표 스냅이 목적지를 옮기고 있었다**

GPS 흔들림 때문에 경로가 매번 다시 요청되는 걸 막으려고 좌표를 0.001° 격자로 반올림했는데, 그 격자가 **약 111m**다. 즉 사용자가 고른 **도착지가 최대 110m 옮겨졌다.** 경로 끝에 서 있는데 "80m 앞 도착"이 뜨는 것으로 발각됐다.

→ 반올림을 폐기하고 **출발지 잠금(latch)** 으로 바꿨다: 첫 좌표로 경로를 한 번 만들고, 목적지·옵션이 바뀌거나 사용자가 '다시 계산'을 누를 때만 다시 잡는다. 좌표는 이제 그대로 전달되므로 경로 끝 = 설정한 목적지다. 자동 재계산을 없앤 대신 **'다시 계산' 버튼**을 이탈 배너에 넣었고, 출발지를 직접 지정한 위젯에서는 눌러도 결과가 같으므로 **버튼을 숨긴다**(동작하지 않는 컨트롤을 두지 않음).

*(검증 중 "40m 전인데 도착이라고 뜬다"고 의심한 건 오진이었다 — 실측해 보니 그 좌표는 경로상 40m 전이 아니라 **도착점 옆으로 40m 벗어난 지점**이었고, 허용 오차 50m 안이라 도착이 맞다.)*

---

### P3 ✅ **완료** (2026-09-07) — 위젯 UI (요구 1·3)

**실브라우저 E2E 통과**(임시 승인 계정, 검증 후 계정·pb_members 행 삭제 확인 — 잔여 0행): 팔레트에 **길찾기** 노출 → 더블클릭 추가 → 미설정 타일이 "도착지를 설정하세요" 안내(조용한 빈 화면 아님) → ⋮ 편집에서 출발 **이태원역 6호선**·도착 **N서울타워** 검색·선택 → 저장 → 타일에 **지도+경로선(156점)+고도 그래프** 렌더, 요약 "2.9km · 38분" → 새로고침 후에도 config 유지 → '전체'에서 큰 지도(1024×485) + 안내 **23단계**("이마트24 R이태원피닉스점에서 8시 방향 좌회전 후 152m 이동") + 고도 그래프.

tsc 0 · 신규 lint 0 · test 98/98 · build 그린.

**만든 것**: `components/widgets/route/{types,useCurrentPosition,useWalkRoute,RouteMap,ElevationChart,RouteBody,CompactView,ExpandedView,ConfigEditor,index}` + registry 등록. 위치 UI는 공용 `LocationPicker` 재사용, 경로 조회는 공용 `usePoll` 재사용(주기 24시간 = 사실상 1회). **npm 의존성 0.**

**실브라우저에서 잡아 고친 결함 3건**:

1. **고도 그래프 축 라벨이 찌그러졌다** — 그래프는 타일 크기에 맞춰 가로세로가 따로 늘어나야 해서 `preserveAspectRatio="none"`인데, 그러면 SVG 안의 `<text>`도 같이 늘어난다. 라벨을 **SVG 밖 HTML(flex)로** 빼서 해결. 덤으로 좁은 타일에서 잘리던 출처 문구도 `truncate`+title로 정리.
2. **전체보기에서 지도가 83px로 찌그러졌다** — 안내 목록·그래프가 높이를 다 먹고 지도가 남는 공간만 받았다. 전체보기 지도에 `min-h-[260px]` 보장 + 넘치면 세로 스크롤, 목록은 `max-h-40`.
3. **lint 2건**(둘 다 실제 위반): ElevationChart의 early return 뒤 `useMemo`(rules-of-hooks) → 100개 덧셈이라 메모 제거. useCurrentPosition의 effect 내 동기 `setState`(set-state-in-effect) → geolocation 지원 여부를 `useSyncExternalStore`로 읽어 파생값으로 변경.

**부수**: 공용 `usePoll`에 `message` 필드 추가(에러 봉투의 사용자 문구 보존) — 기존 위젯은 읽지 않으므로 동작 변화 없음. 이게 있어야 "이 지역은 도보 경로가 제공되지 않습니다"가 위젯에 뜬다.

**남은 것**: **P4 — 전체보기 현재 위치 추적 + 다음 안내(방향·거리)** = 요구 2. 기반은 이미 있다(`useCurrentPosition({watch})`, `projectOntoPath`, `steps[].distanceFromStart`, 지도·그래프의 현재위치 마커).

---

### P1 ✅ **완료** (2026-09-07) — 서버 표면

`node _workspace/verify-walk-api.mjs` **28/28 PASS**(실서버 + 임시 승인 계정, 검증 후 계정·pb_members 행 삭제 확인). tsc 0 · 신규 lint 0 · **test 98/98**(신규 22) · build 그린(`/api/route/walk`·`/api/map/static` 등록).

**만든 것**: `lib/widgets/route/geo.ts`(+테스트 22) · `lib/api/{tmapClient,elevationClient}.ts` · `app/api/route/walk/route.ts` · `app/api/map/static/route.ts` · `output/api-shapes.ts`(WalkRoute) · `.env.example`(TMAP_APP_KEY). **npm 의존성 0 · DB 마이그레이션 0.**

검증된 것: 익명 401(no-store) · 좌표 누락/범위이탈 400 · 실좌표 200(1.4초) · 캐시 헤더(경로 s-maxage=86400 / 지도 immutable) · **응답에 앱 키 미노출** · zoom·크기 초과값 클램프 · 안내지점 누적거리 단조증가 + 도착지=총거리(2967=2967) · 고도 100점이 0~총거리를 덮음.

**첫 실행에서 잡힌 결함 3건**(전부 수정 + 테스트로 고정):

1. **좌표 누락이 400이 아니라 502였다** — `Number(null)`은 NaN이 아니라 **0**이라, 파라미터 없는 요청이 (0,0) 좌표로 업스트림까지 갔다. 누락을 먼저 거르도록 수정.
2. **고도 그래프가 도착점에 못 닿았다**(2,863m에서 끝남, 총 2,967m) — 리샘플한 100점을 직선으로 이어 다시 재니 원본의 굽이를 가로질러 **3.5% 짧아졌다.** 거리축을 원본 경로 기준(`resampleDistances`)으로 교체.
3. **서비스 지역 밖이 "경로를 가져오지 못했습니다 (400)"으로 뭉개졌다** — TMAP은 빈 결과가 아니라 **400 + 전용 코드**를 준다(실측: 울릉도 → `code 3102` "해당 서비스가 지원되지 않는 구간입니다.([NoServiceArea]…)"). 공식 코드표엔 3002로 적혀 있어 **둘 다 + 메시지 문구로** 판정하도록 수정 → 이제 **422 unsupported_area**로 정확한 안내가 나간다. (대조: 대구는 200 정상)

**다음(P2는 P1에 흡수됨)**: `geo.ts`+테스트를 P1에서 함께 만들었다(서버가 리샘플을 필요로 해서 분리가 불가능). → **다음은 P3(위젯 UI)**.

---

### P0 ✅ **전체 통과** (2026-09-07)

키 문제는 **상품 사용 신청 누락**이 맞았다(사용자가 TMAP·TMAP 대중교통 신청 후 즉시 해소). 전 상품·전 전달방식 200.

| 항목 | 결과 |
|------|------|
| ① 보행자 경로안내 | ✅ **12/12 PASS** — `verify-tmap.mjs`. 이태원역→N서울타워 **2,967m·40분**, feature 50개(Point 24 + LineString 26). **우리가 조립한 폴리라인 길이 = API totalDistance 정확히 일치(2967m)** |
| ② StaticMap 좌표계 | ✅ **확정** — 아래 §2.1 |
| ③ 고도 파이프라인 | ✅ **10/10 PASS** — `verify-elevation.mjs`. 실경로 기준 59m→270m·고저차 211m·누적상승 381m, 100점 1회 요청, 101점은 400 거부 |

**실측으로 확정된 turnType**(문서 코드표와 일치): 11 직진 · 12 좌회전 · 13 우회전 · 17 10시방향 · 18 2시방향 · 200 출발 · 201 도착 · 211 횡단보도 · 213 우측 횡단보도. `description`에 한국어 문구가 그대로 실려 온다(예: "이태원역 3번출구에서 우회전 후 27m 이동") → **요구 2의 안내 문구를 우리가 만들 필요가 없다.**

---

### ⚠ P0의 최대 수확: **티맵 StaticMap은 512px 타일 = 표준 Web Mercator zoom+1**

계획 §2가 세운 "표준 Web Mercator(256·2^z)" 가정은 **틀렸다.** 정밀 실측 결과 **정확히 2.00000배**다.

측정 방법(`verify-tmap-scale.mjs`): 중심을 Δlon만큼 옮긴 지도를 받아 원본과 픽셀 상관(SAD)으로 실제 이동량을 재고, 여러 Δ를 선형회귀해 양자화 오차를 제거.

| Δlon | 표준 예측 | 실측 | 배율 |
|---|---|---|---|
| 1.03e-3° | 24px | **48px** | 2.0000 |
| 2.06e-3° | 48px | **96px** | 2.0000 |
| 3.09e-3° | 72px | **144px** | 2.0000 |
| 4.12e-3° | 96px | **192px** | 2.0000 |
| 5.15e-3° | 120px | **240px** | 2.0000 |

회귀 기울기 46,603 px/도 vs 표준 23,301.7 px/도 → **배율 2.00000, 오차 0.00%**. SAD 0.22~0.35(거의 완전 정렬). 축 직교(경도 이동 시 Δy=0), 위도/경도 비율은 정확히 메르카토르 → **투영은 메르카토르가 맞고 축척만 2배**.

**구현 규칙**: `geo.ts`의 투영에서 `TILE = 512`를 쓴다(또는 zoom+1). 이 한 줄이 틀리면 경로선이 지도와 2배 어긋난다.

*(중간에 1.92배라는 값이 나왔었는데, 이는 탐색 범위 ±120px가 실제 시프트 128px를 담지 못해 생긴 경계 아티팩트였다. 범위를 넓히자 정확히 2.0으로 수렴.)*

**부수 확정**: StaticMap의 `markers` 파라미터는 **실제로 핀을 그리지 않는다**(픽셀 diff 0). `|`·`_` 구분자는 500 또는 무시. → 출발·도착·현재위치 마커는 **전부 우리 SVG로 그린다**(원래 설계 그대로라 영향 없음).

---

### 좌표계 육안 최종 확인 (`verify-closeup.mjs`)

TILE=512로 고친 뒤 경로를 지도에 겹쳐 확대:

- **출발부**: 경로가 이태원역 출구에서 시작해 **이태원로 위를 정확히 따라가다** 이마트24 앞에서 북쪽 골목으로 꺾인다. 안내지점이 교차로에 정확히 떨어짐
- **도착부**: **YTN서울타워(=N서울타워) 건물에 정확히 도달**
- 전 구간에서 폴리라인이 도로망을 벗어나지 않음

→ **A안(이미지 + SVG 오버레이) 확정. C안(Leaflet) 폐기.**

**남은 소소한 이슈**: 정수 zoom 단계라 bbox 맞춤 시 최대 2배까지 여백이 생긴다(현재 fit은 PAD=24). 실제로 거슬리면 이미지를 크게 받아 크롭하는 방식으로 개선 — **지금은 하지 않는다.**

---

## 0. 요구사항 (사용자 원문)

1. 지도에 **도보 이동경로** 표시
2. **현재 위치에서 가야 하는 방향 + 그 지점까지의 거리** 표시 (전체보기에서만)
3. 출발↔도착 경로의 **고도를 그래프 형태로만** 표시 + 그래프 위에 **현재 위치** 표시
4. 출발 기본값 = 현재 위치, 지도는 "네이버나 티맵"

---

## 1. 사실 확인 결과 (조사 완료)

| 항목 | 결론 | 근거 |
|------|------|------|
| **네이버 Directions 5** | **도보 경로 미지원 — 자동차 전용.** 후보에서 탈락 | ncloud 공식 문서 + 개발자 포럼 답변 |
| **TMAP 보행자 경로안내** | `POST https://apis.openapi.sk.com/tmap/routes/pedestrian?version=1`, 헤더 `appKey`. body = `startX/startY/endX/endY/startName/endName`, `searchOption`(0 추천 / 10 최단 / **30 최단+계단없음**), 경유지 5개까지 | skopenapi 공식 레퍼런스 |
| **TMAP StaticMap** | `GET .../tmap/staticMap` — center(lat/lon)+zoom(6~19)+markers+PNG/JPG. **최대 512×512, 경로선(polyline) 파라미터 없음** | tmap-skopenapi 레퍼런스 |
| **TMAP 무료 한도** | **경로안내 그룹 1,000건/일**(자동차·보행자·타임머신·이미지·화물차 **합산**) · **StaticMap 1,000건/일** · POI 20,000건/일. **초과 시 과금이 아니라 자동 차단** | openapi.sk.com 요금표(Free) |
| **보행자 경로 제공 지역** | **전국이 아님** — 서울·수도권 시지역·6대광역시·제주도 + 강원/경남/경북/전남/전북/충남/충북 주요 시 | 보행자 경로안내 공식 문서 |
| **고도 데이터** | **Open-Meteo Elevation API** `GET /v1/elevation?latitude=..&longitude=..` — **키 불필요**, **1요청당 좌표 100개**, Copernicus DEM GLO-90(**90m 해상도**) | open-meteo 공식 문서 |

**핵심 함의 3가지**

- 도보 경로는 **TMAP 외 실질적 대안이 없다**(국내 기준). → `TMAP_APP_KEY` 발급이 이 기능의 전제.
- StaticMap이 경로선을 못 그린다. → 지도는 **배경 이미지 + 자체 SVG 오버레이** 구조가 된다(아래 §2).
- 고도는 **이미 이 저장소가 쓰는 Open-Meteo**로 해결된다. 새 키·새 계정 0개. 요구 3번이 가장 싸다.

---

## 2. 지도 렌더링 — **A안 확정** (B·C는 폐기하되 근거로 남김)

| | **A. TMAP StaticMap + 자체 SVG 오버레이 ✅ 채택** | B. TMAP Web SDK(Tmapv3) | C. Leaflet + OSM 타일 |
|---|---|---|---|
| 앱키 노출 | **없음**(서버 프록시 `/api/map/static`) | **브라우저에 노출**(도메인 제한 키) | 키 자체가 없음 |
| 가드레일 | **준수** | **예외 승인 필요** | 준수 |
| 새 의존성 | 0 | 외부 스크립트 1 | npm `leaflet` 1 |
| 팬/줌 | 없음(±줌 버튼만 — 재요청) | 완전 지원 | 완전 지원 |
| 해상도 | **512×512 상한**(전체보기 확대 시 흐림) | 무제한 | 무제한 |
| 한국 지도 품질 | 티맵 그대로 | 티맵 그대로 | 라벨·건물 정보 열세 |
| 구현량 | 지도 컴포넌트 ≈120줄 | SDK 로더+리사이즈 처리 ≈180줄 | ≈100줄 + 의존성 |

**A를 추천하는 이유**: 경로 API 때문에 어차피 TMAP 키를 발급받는데, 같은 키 하나로 지도까지 해결되고 그 키가 서버 밖으로 나가지 않는다. 대시보드 타일 크기(≈300~500px)에서 512×512는 충분하고, 팬/줌은 이 위젯의 핵심 가치("경로 모양 + 지금 어디로 + 고도")가 아니다.

**A의 전제 — ✅ 검증 완료(수정 포함)**: 좌표계는 메르카토르가 맞으나 **타일이 512px**(표준의 2배)이었다. `TILE=512`로 고친 뒤 경로선이 도로에 정확히 정합함을 확대 확인. 상세는 상단 P0 절.

512 상한이 나중에 실제로 불편해지면: 전체보기에서만 2×2로 4장 이어 붙여 1024×1024 구성(같은 수학, `<img>` 4개). **지금은 만들지 않는다.**

---

## 3. 아키텍처

```
ConfigEditor ─ 출발(기본: 현재 위치) · 도착 ── LocationPicker 재사용(기존 컴포넌트)
      │ config(jsonb)에 저장 — 새 DB 테이블/마이그레이션 없음
      ▼
useWalkRoute ── GET /api/route/walk?sx&sy&ex&ey ──┬─ TMAP 보행자 경로 (서버 키)
      │                                            └─ Open-Meteo 고도 (키리스, 100점 리샘플)
      │            ← { path[], steps[], profile[], totalDistance, totalTime }
      ▼
RouteMap        ← <img src="/api/map/static?…"> (서버가 appKey 주입) + SVG 경로/마커 오버레이
ElevationChart  ← SVG 순수 렌더 (차트 라이브러리 없음)
useCurrentPosition (전체보기 전용) ── watchPosition → 폴리라인에 투영
      → 지도 현재위치 점 · 고도 그래프 세로선 · "230m 앞 좌회전"
```

### 비용을 좌우하는 설계 결정: **경로는 한 번만 부른다**

GPS가 갱신될 때마다 경로를 재계산하지 **않는다**. 이미 받아둔 폴리라인에 현재 좌표를 **투영**해서 마커만 움직인다. 재계산은 ① 경로에서 80m 이상 벗어난 상태가 10초 지속 ② 사용자가 '경로 다시 계산' 클릭 ③ 목적지 변경 — 이 세 경우뿐. 결과적으로 TMAP 호출은 **하루 수십 건 수준**.

응답 캐시: `/api/route/walk`는 `s-maxage=86400`(도보 경로는 하루 단위로 안 변함), `/api/map/static`은 `s-maxage=604800, immutable`.

---

## 4. 요구사항 → 구현 매핑

### 요구 1 — 지도에 도보 경로

TMAP 응답은 GeoJSON FeatureCollection이다. `LineString` feature들의 좌표를 순서대로 이어 하나의 폴리라인으로 만들고, bbox에 맞는 center/zoom을 계산해 StaticMap 배경을 요청한 뒤, 같은 투영으로 SVG `<polyline>`을 그 위에 겹친다. 출발/도착 핀도 SVG.

### 요구 2 — 현재 위치에서 방향 + 거리 (전체보기)

TMAP 응답의 `Point` feature들이 안내 지점이고, **`description`에 이미 한국어 안내 문구가 들어 있다**(예: "보행자도로를 따라 100m 이동 후 좌회전"). `turnType` 코드는 **화살표 아이콘 선택에만** 쓰고, 모르는 코드는 일반 화살표로 폴백한다 — 코드표가 틀려도 기능이 죽지 않도록.

표시 로직: 현재 위치를 폴리라인에 투영 → 누적거리 산출 → 그보다 앞에 있는 첫 안내 지점까지의 **경로상 거리**를 계산 → `"{거리}m 앞 {안내}"`.

**정직성 가드**: 투영 거리가 50m를 넘으면 안내 대신 **"경로에서 벗어났습니다"**를 띄운다. 틀린 방향을 자신 있게 말하는 것이 최악이다.

### 요구 3 — 고도 그래프 + 현재 위치

폴리라인을 **거리 기준 등간격 100점 이하로 리샘플** → Open-Meteo 고도 1회 요청 → SVG area+line 차트(X=누적거리, Y=고도). 현재 위치는 세로선 + 점.

**정직성 가드**: 90m 해상도 DEM이라 도심 짧은 경로는 거의 평평하게 나온다(계단·육교·지하보도는 안 잡힌다). 차트 하단에 **"90m 해상도 지구고도 근사치"** 표기 — 주식 위젯의 '근사치' 배지와 같은 원칙.

---

## 5. 파일 목록 (신규 12 · 수정 3, **npm 의존성 0 · DB 마이그레이션 0**)

**서버**
- `lib/api/tmapClient.ts` — 보행자 경로 호출 + 정규화 (SERVER-ONLY, geocodeClient 패턴)
- `lib/api/elevationClient.ts` — Open-Meteo 고도 (키리스)
- `app/api/route/walk/route.ts` — `requireUser()` 게이트 + 두 upstream 병합 + zod 검증
- `app/api/map/static/route.ts` — StaticMap 이미지 프록시(appKey 서버 주입)
- `output/api-shapes.ts` — `WalkRouteSchema` 추가 *(수정)*

**순수 로직 + 테스트**
- `lib/widgets/route/geo.ts` — haversine · 누적거리 · 폴리라인 리샘플 · 점→폴리라인 투영 · Web Mercator 좌표변환
- `lib/widgets/route/geo.test.ts` — **필수**. 위 5개는 전부 조용히 틀릴 수 있는 수학이고, 틀리면 "엉뚱한 방향 안내"로 나타난다. `node --test`가 이미 `lib/**/*.test.ts`를 돈다.

**위젯**
- `components/widgets/route/{index.ts, CompactView.tsx, ExpandedView.tsx, ConfigEditor.tsx, RouteMap.tsx, ElevationChart.tsx, useWalkRoute.ts, useCurrentPosition.ts}`
- `components/widgets/registry.ts` *(수정)* · `.env.example` *(수정: `TMAP_APP_KEY`)*

**config 형태** (새 테이블 없음 — `pb_widgets.config` jsonb)
```ts
interface RouteConfig {
  start: LocationValue | null;  // null = 현재 위치 (기본값)
  end: LocationValue | null;
  avoidStairs: boolean;         // → searchOption 30
}
```

**위젯 정의**: `type:"walk-route"`, displayName `"길찾기"`, category `extended`, dataMode `static`, defaultSize `w8×h16`(outfit 위젯 선례), minSize `w4×h8`.

---

## 6. 단계 (각 단계 = 검증 게이트)

| P | 내용 | 통과 기준 |
|---|------|-----------|
| ~~P0~~ | ✅ **완료** — `verify-tmap.mjs` 12/12 · `verify-elevation.mjs` 10/10 · `verify-tmap-scale.mjs`(TILE=512 확정) · `verify-closeup.mjs`(도로 정합) | 통과 |
| P1 | 서버 표면 4파일 + api-shapes | 익명 호출 401 · 실좌표로 200 · 캐시 헤더 확인 |
| P2 | geo.ts + 테스트 | `node --test` 그린 (투영·리샘플 경계 케이스 포함) |
| P3 | 위젯 — 지도 + 고도 그래프 + ConfigEditor **(요구 1·3)** | 팔레트 추가 → 출발/도착 지정 → 경로선·고도 그래프 렌더 |
| P4 | 전체보기 현재위치 추적 + 다음 안내 **(요구 2)** | 실기기/실브라우저에서 위치 이동 시 마커·안내 갱신, 이탈 시 이탈 표시 |
| P5 | 실브라우저 E2E + CLAUDE.md 이력 갱신 | tsc 0 · lint 신규 0 · test 그린 · build 그린 · 콘솔에러 0 |

---

## 7. 리스크 / 미해결

| 리스크 | 대응 |
|--------|------|
| ~~TMAP 무료 제공량 미확인~~ → **해소**: 경로 1,000건/일 · StaticMap 1,000건/일, **초과 시 자동 차단(요금 폭탄 없음)** | §3의 "경로 1회만 호출" + StaticMap CDN 캐시(`s-maxage`)로 실사용은 한도의 극히 일부. 무료 플랜 유지 |
| **보행자 경로가 전국 지원이 아님** (§1) | 제공 지역 밖 목적지는 경로가 안 나온다 → 위젯이 "이 지역은 도보 경로가 제공되지 않습니다"로 명시 안내(조용한 빈 화면 금지) |
| **StaticMap 좌표계 가정** | P0 게이트에서 실측 검증. 실패 시 C안(Leaflet+OSM)으로 전환 — 경로·고도 로직은 그대로 재사용 |
| **90m DEM 한계** | UI에 근사치 표기(§4) |
| **TMAP 약관의 결과 저장·재배포 제한 가능성** | KIS 선례와 동일하게 **개인 본인 용도·재배포 금지**로 운용. 가입 시 약관 확인 |
| 승인 회원이 관리자 TMAP 한도를 공유 | 기존 공유 자원 가드레일과 동일(KIS·Kakao와 같은 취급) |
| iOS Safari geolocation | HTTPS 필수 — Vercel 배포라 충족. 권한 거부 시 출발지 수동 지정으로 폴백 |

---

## 8. 착수 전 사용자 액션 (**현재 유일한 블로커 — 키 거부**)

공식 [API 사용 안내](https://openapi.sk.com/products/detail?linkMenuSeq=122)의 5단계 중, **4단계(상품 사용 신청)를 건너뛰면 지금과 똑같은 403이 난다.**

| 단계 | 위치 | 확인할 것 |
|------|------|-----------|
| 1. 회원 가입 | — | **개인 회원**이면 충분(세금계산서 불필요). 사업자 전환은 되돌릴 수 없으니 개인으로 둘 것 |
| 2. 앱 만들기 | 대시보드 > **앱** > 앱 만들기 | 앱이 하나 있어야 함 |
| 3. 앱 키 확인 | 대시보드 > 앱 > *(앱 선택)* > **앱키(appKey) 탭** | 이 탭에 표시되는 값이 정답. 앱 키는 **앱 단위**로 발급된다 |
| **4. 상품 사용 신청** ← **여기가 유력** | 홈 > 상품 카테고리 > 상품 클릭 > **사용 신청** | **보행자 경로 안내**와 **StaticMap**을 각각 신청. 신청 시 **어느 앱에 포함할지 선택**해야 하며, 3번의 그 앱을 골라야 한다 |
| 5. 호출 | — | 헤더에 `appKey` + `Accept: application/json` |

요금제는 **무료체험(Free)** 을 선택하면 된다 — 경로안내 1,000건/일, StaticMap 1,000건/일이고 **한도를 넘으면 과금이 아니라 자동 차단**이라 요금 사고가 나지 않는다. 우리 설계(경로 1회 호출 + 지도 CDN 캐시)로는 한도의 극히 일부만 쓴다.

값을 고친 뒤 재확인은 명령 한 줄:

```bash
node _workspace/probe-tmap-key.mjs   # 통과하면 ↓
node _workspace/verify-tmap.mjs      # P0 ①② 본검증 + out/overlay.html 생성
```

`verify-tmap.mjs`가 만드는 `_workspace/out/overlay.html`이 **A안의 최종 관문**이다 — 우리 수학으로 그린 경로선이 지도의 도로를 따라가면 통과, 건물을 가로지르면 지도만 C안(Leaflet)으로 교체하고 나머지 계획은 그대로 간다.

---

## 9. 의도적으로 뺀 것 (요청 시 추가)

- 자동차·대중교통 경로 (요구는 도보만)
- 실시간 음성 안내 / 백그라운드 추적
- 경유지, 최근 목적지 목록, 즐겨찾기 경로
- 지도 팬/줌 (§2 A안 선택 시) — 실제로 불편해지면 그때 1024 확장 or C안
- 안드로이드 홈 위젯 연동 (웹 위젯 먼저)
