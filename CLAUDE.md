@AGENTS.md

# PaneBoard

개인 모듈형 캔버스 대시보드. **단일 진실 원천(설계서)**: `../PaneBoard_웹개발_설계서_v1.3.md`(파일명은 v1.3 유지, 내부 버전 v1.4). 구현 결정·범위·검증 기준은 설계서를 따른다.

- **스택**: Next.js **16** (App Router) · React 19 · TypeScript · Tailwind v4 · shadcn/TweakCN(미초기화 — 현재 토큰 기반 커스텀 컴포넌트) · Supabase(Postgres+RLS+Storage+Auth) · Vercel
- **테이블 프리픽스**: `pb_`
- **데이터 핸드오프**: `output/`(영속 산출물: schema.sql, types/database.ts, contract.ts, api-shapes.ts, widget-manifest.md), `_workspace/`(오케스트레이션 스크래치)
- **⚠ Next.js 버전 주의**: 위 `@AGENTS.md`대로, 이 Next.js는 학습 데이터와 다른 breaking change가 있다. **Next.js 코드(라우트 핸들러·middleware·레이아웃 등) 작성 전 `node_modules/next/dist/docs/`의 관련 가이드를 먼저 읽는다.**

## 하네스: PaneBoard 빌드

**목표:** 5인 에이전트 팀(db-architect·ui-builder·api-designer·widget-engineer·qa)으로 설계서를 구현한다.

**트리거:** PaneBoard 빌드/구현/이어서/phase 진행/위젯·캔버스·API·DB 작업 요청 시 `paneboard-orchestrator` 스킬을 사용한다. 단순 질문은 직접 응답 가능. (에이전트·스킬 목록은 `.claude/agents/`·`.claude/skills/`에서 관리 — 여기 중복 기재하지 않음)

## 불변 가드레일 (모든 세션 준수)
- **보안**: 모든 `pb_*` 테이블 RLS(`user_id = auth.uid()`). 외부 API 키·**KIS 자격증명은 서버 전용**(클라이언트 미노출). 인제스트는 per-user 토큰.
- **주식**: KIS는 **읽기 전용 시세만** — 주문/잔고 엔드포인트 **사용 금지**. 시세는 개인 본인 용도(재배포 금지).
- **민감정보**: 주민번호·카드번호 전체·비밀번호 **저장 금지**. 법인등록번호·주소 등 중간 수준만 평문(RLS). 민감값 서버 로그·에러 노출 금지.
- **인증**: **관리자 승인제(소수 공유)** — 관리자(`ALLOWED_EMAIL` 1개, 항상 통과하는 부트스트랩)가 앱의 `설정 > 멤버`에서 승인한 이메일(`pb_members.status='approved'`)만 로그인 가능. 승인 검사는 `lib/auth/access.ts` 한 곳의 규칙을 `app/page.tsx`·`/auth/callback`·`/share`·`login/actions`가 공유한다(proxy는 세션 유무만 — DB 금지·리다이렉트 루프 방지). 미승인자는 auth 계정 자체가 생기지 않는다.
- **공유 자원**: 승인 회원은 서버 키(KIS 토큰 공유캐시·Kakao·KMA·Naver·Anthropic)를 **공유**한다 = 관리자 명의의 한도·요금을 함께 쓴다. 회원 수를 적게 유지하고, 권한 회수는 `설정 > 멤버`의 차단·삭제로(계정까지 삭제돼 세션이 즉시 끊긴다).

## 변경 이력
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-09-26 | **숨기기/보이기 애니메이션(PC)** + 환율 **정보 페이지**(웹 더블클릭·폰 버튼) + 폰 접힌 환율 = **환전 계산기** (APK v35) | components/canvas/WidgetFrame, components/widgets/fx/{rows(fxInfoUrl),FxRateRow}, output/api-shapes(WidgetFxItem.infoUrl), app/api/widget/fx/route.ts, android/(widget/{FxCalcActivity(신규),FxWidget}, data/{WidgetStore,model/FxItem}, quotes/QuoteDeleteActivity, AndroidManifest, build.gradle, twa-manifest), lib/widgets/fx/rows.test.ts, _workspace/verify-quote-api.mjs | 요구 4건. ① **PC 접기 애니메이션**: 지금까지 접기는 타일 높이만 줄이고 본문을 즉시 언마운트해 툭 사라졌다 → WidgetFrame에 **세 단계 상태**(보임 → 걷히는 중 → 접힘)를 두고 본문 래퍼를 `overflow-hidden` + `max-height`/`opacity` 260ms 트랜지션으로 접는다. **접힌 뒤에는 그대로 언마운트**(v26 규약 유지 — 숨긴 위젯이 폴링·타이머를 계속 돌리면 공유 API 한도를 먹는다). 타일 높이도 같은 260ms(globals.css)라 본문·타일·아래 위젯이 한 리듬으로 움직인다. `collapsed` prop이 바뀐 **뒤** 클래스를 입혀야 트랜지션이 도는 시간 축 상태라 effect + 근거 명시 disable 1줄. ② **환율 정보 페이지**(요구): `fxInfoUrl(code)` 하나를 **웹 행 더블클릭과 폰이 함께 쓴다** — 원화 기준이라 네이버 환율 상세 `FX_{코드}KRW`(USD·JPY·EUR·CNY 실측 200), 3자리 코드가 아니면 시장지표 메인(없는 페이지로 보내지 않는다). 서버가 `WidgetFxItem.infoUrl`로 내려주고 폰은 그 값을 열 뿐이다(규칙 두 벌 금지). 웹 FxRateRow는 주식과 같은 규약: **한 번 클릭 무시 · 더블클릭/Enter로 새 탭**(끌어 옮기다 눌리는 자리다). 폰은 행 탭 화면의 버튼 라벨이 '환율 정보 보기'. ③ **접힌 환율 위젯 = 환전 계산기**(요구): 통화별로 `10,000원 = 7.32 USD`를 크게 그리고, 헤더에 **÷10 · ×10 · 방향(원→외화 / 외화→원)** 탭을 둔다 — 위젯엔 입력창이 없어(계획서 §0.3) 금액을 **자리수 이동**으로 정한다(1~10억). 조작은 투명 액티비티 `FxCalcActivity`(v14·v15 규약: 포그라운드 크레딧·터치 통과·동작마다 고유 data URI), 금액·방향은 이 폰에만(WidgetStore). 환산식은 **FxItem.wonToForeign/foreignToWon 한 곳** — 엔은 unit=100이라 위젯에서 직접 나누면 100배 틀린다(실값 검산: 10,000원=1,158엔 / 10,000엔=86,344원). ④ ⚠ **폰 위젯은 애니메이션이 불가**하다 — Glance/RemoteViews에는 트랜지션 API가 없고 런처가 프레임을 그려 준다. 중간 프레임을 2~3장 억지로 밀어 넣으면 v15에서 잡은 stale 렌더·입력 삼킴 위험만 커지고 미끄러지는 대신 **덜컥거린다** → 폰은 즉시 전환을 유지하고 그 공간에 요약(주식)·계산기(환율)를 채우는 쪽으로 갚았다. 검증: **실서버 54/54 PASS**(신규 2 — USD·JPY 환율 링크), **실브라우저**(임시 승인 계정, 검증 후 삭제 — 잔여 0행): 결정적 배치(주식 426px/환율 426/메모 318)에서 숨기기 → 본문 래퍼 **377→179→24→0px**·불투명도 1→0.17→0.003·타일 426→144→102·아래 위젯 522→240→198·954→672→630, **660ms 뒤 본문 언마운트**, 보이기 → 마운트 후 0→53→352→377px·타일 102→401→426·아래 위젯 원위치, 환율 위젯도 동일(426→207→102→426), USD 행 **더블클릭 → FX_USDKRW 열림 · 한 번 클릭은 0건**, 콘솔 에러 0. tsc 0·lint 0(기존 WidgetFrame 1건만)·test **183/183**(신규 1 — fxInfoUrl), APK **v35**(versionCode 35·지문 동일·병합 매니페스트에 FxCalcActivity·dex에 '환율 정보 보기'). adb 미연결로 기기 실측은 미실시. **잔여(사용자)**: v35 설치 → 환율 위젯 숨기기 → 계산기(÷10·×10·방향) 확인, 행 탭 → 환율 정보 보기. |
| 2026-09-26 | 접힘 요약 **긴 이름 +2sp(16sp)** + 폰에서 **종목 정보 웹페이지** 열기 (APK v34) | components/widgets/stock/format.ts, output/api-shapes(WidgetStockQuote.infoUrl), app/api/widget/stocks/route.ts, android/(data/model/QuoteItem, widget/StocksWidget, quotes/QuoteDeleteActivity, app/build.gradle, twa-manifest), _workspace/verify-quote-api.mjs | 요구 2건. ① **긴 이름 +2sp**: 하한을 매직넘버에서 상수 `MIN_NAME_SP`로 빼고 14→**16sp**(계수 7.6은 유지 — 올리면 중간 길이가 먼저 잘린다). 하한 구간은 글자폭 14 이상(한글 7자 이상) → 필라델피아 반도체 16sp. ② **종목 정보 페이지**(요구: "PC에서 더블클릭하면 나오는 그 웹페이지를 폰 편집 화면에 추가"): 행을 누르면 열리는 화면(QuoteDeleteActivity)에 **'종목 정보 보기'** 버튼을 삭제 버튼과 한 칸 떨어뜨려 배치했다 — 행 탭 자체를 링크로 만들면 삭제하러 들어온 사람이 브라우저로 튕겨 나간다. ⒜ **링크 규칙은 한 곳**: 폰이 규칙을 따로 갖지 않고 서버가 웹 행 클릭과 **같은 `quoteInfoUrl`**로 만들어 `WidgetStockQuote.infoUrl`에 담아 보낸다(두 벌이면 웹·폰 링크가 갈라진다 — v24 검색 경로와 같은 판단). 옛 캐시엔 없을 수 있어 그때만 네이버 검색으로 폴백. ⒝ **웹도 함께 고쳐졌다**: `quoteInfoUrl`이 `^`로 시작하는 지수 중 네이버 맵(코스피·코스닥·다우·S&P·나스닥)에 없는 것을 **네이버 통합검색**으로 보내고 있어 필라델피아 반도체(^SOX)를 누르면 `"^SOX"` 검색 결과가 떴다 → 야후 지수 페이지로. 즉 이번 요구를 제대로 하려면 **먼저 고쳐야 했던 링크**다. ⒞ QuoteItem이 infoUrl을 왕복·keepKnown에서 최신 링크 유지, deleteIntent에 extras로 전달(환율은 null → 버튼 없음). 검증: **실서버 52/52 PASS**(기존 48 + 신규 4 — 지수→네이버 지수, 국내 종목→네이버 종목, 미국→야후 종목, **^SOX→야후 지수**), 세 링크를 curl로 따라가 **최종 200** 확인(005930→stock.naver.com/domestic/stock/005930/price · ^SOX→finance.yahoo.com/quote/%5ESOX · KOSPI→…/index/KOSPI/price), tsc 0·lint 0(기존 WidgetFrame 1건만)·test 182/182, APK **v34**(versionCode 34·지문 동일·classes2.dex에 '종목 정보 보기' 문자열 확인). adb 미연결로 기기 실측은 미실시. **잔여(사용자)**: v34 설치 → 요약 글자 확인 + 행 탭 → '종목 정보 보기'. |
| 2026-09-26 | 접힘 요약 — **가장 긴 이름만 +1sp**(하한 14sp) (APK v33) | android/(widget/StocksWidget, app/build.gradle, twa-manifest) | 요구: "글자가 많은 경우의 폰트 크기를 1 정도 더 크게". **하한만** 0.46→0.50(12.9→**14sp**)로 올리고 계수(7.6)는 건드리지 않았다 — 요구가 "글자가 많은 경우"이고, 계수를 올리면 중간 길이(삼성전자 26.6·SK하이닉스 21.3sp)가 먼저 자리를 넘어 **그쪽이 잘린다**. 하한이 걸리는 구간은 글자폭 16 이상(= 한글 8자 이상, 필라델피아 반도체·긴 ETF 이름). 결과: 필라델피아 반도체 12.9→**14.0sp**, KODEX 200선물인버스2X도 14.0sp, 그 외(지수 28·삼성전자 26.6·SK하이닉스 21.3)는 v32 그대로. APK **v33**(versionCode 33·지문 동일). |
| 2026-09-26 | 접힘 요약 — **긴 이름 글자 한 단계 확대**(하한 13sp) (APK v32) | android/(widget/StocksWidget, app/build.gradle, twa-manifest) | 요구: "필라델피아 반도체 같은 이름을 13sp 정도로, 다른 긴 글자도 전체적으로 크게". v31의 연속식 `배율 = 6.8/글자폭`(하한 0.42 ≈ 11.8sp)을 **`7.6/글자폭`·하한 0.46(≈12.9sp)** 으로 올렸다 — 표를 고치는 게 아니라 **상수 두 개**라 긴 이름 전체가 같은 비율로 함께 커진다(요구의 "전체적으로"). 결과: 필라델피아 반도체 11.8→**12.9sp**, SK하이닉스 19→21.3, 삼성전자 23.8→26.6, S&P 500 27.2→28(지수 4종은 그대로 28). ⚠ 대가를 알고 올렸다 — 긴 이름들이 계산상 자리(105dp)를 5dp쯤 넘어서므로 **좁은 위젯에서는 끝이 말줄임**될 수 있다(작게 보이는 쪽보다 큰 쪽을 택한 결과, 주석에 기록). 값·등락률은 v31대로 전부 고정(16/28sp). APK **v32**(versionCode 32·지문 동일). |
| 2026-09-26 | 접힘 요약 — **자동 축소를 이름에만**(값·등락률은 전부 고정) (APK v31) | android/(widget/StocksWidget, app/build.gradle, twa-manifest) | 요구: "지수는 고정하고 이름이 긴 경우 이름만 축소, 자동축소는 이름에만". v30은 지수를 통째로 빼서 "필라델피아 반도체"가 말줄임됐는데, 줄여야 할 것은 **이름 하나뿐**이었다 → 값(16sp)·등락률(28sp)은 **지수든 종목이든 언제나 같은 크기**로 두고 이름만 `nameScale`로 줄인다(지수 예외 분기 삭제 — 이름 길이만 본다). 계단식 표 대신 **연속식 한 줄**: 한 칸이 대략 `0.52 × 글자크기` dp를 먹고 값·등락률이 쓰고 남는 자리가 ~105dp라 `배율 = 6.8 / 글자폭`(한글 2·로마자 1, 하한 0.42). 표를 손으로 맞추던 v29~v30보다 짧고, 이름이 바뀌어도 알아서 맞는다. 하한 0.42는 **기본 설정에서 ≈11.8sp** — 그보다 작으면 읽히지 않으니 차라리 말줄임이 낫다는 선. 실측 대신 같은 식을 python으로 돌려 확인: 코스피 28sp(84dp) · S&P 500 27.2sp(105dp) · 삼성전자 23.8sp · SK하이닉스 19sp · **필라델피아 반도체 11.8sp(101dp — 이제 안 잘린다)**, KODEX 200선물인버스2X만 하한에 걸려 말줄임. ⚠ 105dp는 **4칸 위젯(≈290dp) 가정 상수**다(`ponytail:` 주석) — 더 좁으면 말줄임, 더 넓으면 조금 작게 보인다. 실폭을 읽으려면 SizeMode.Exact가 필요한데 v26에서 걷어낸 방식이라 그 대가가 이 오차보다 크다. APK **v31**(versionCode 31·지문 동일). |
| 2026-09-26 | 접힘 요약 — **자동 축소는 개별 종목만, 지수는 전부 같은 크기** (APK v30) | android/(widget/StocksWidget, app/build.gradle, twa-manifest) | 요구: "자동 축소는 주식 종목에만 적용되고 지수는 다른 숫자와 동일하게". v29가 이름 폭만 보고 줄여 **필라델피아 반도체 행의 숫자까지 16sp**로 작아졌는데, 요약은 지수들이 한 덩어리로 읽혀야 한다 → `SummaryRow`가 `item.isIndex`면 배율을 **1.0으로 고정**(코스피·코스닥·다우·S&P 500·나스닥·필라델피아 반도체 전부 28sp 동일), `summaryScale`은 개별 종목·ETF에만 적용(삼성전자 0.88 · SK하이닉스 0.78 · KODEX 200선물인버스2X 0.5). isIndex는 서버가 카탈로그에서 정해 보내는 값이라 폰이 다시 판단하지 않는다. ⚠ 대가: 28sp에서 한 행에 들어가는 한글은 3~4자라 **"필라델피아 반도체"는 말줄임된다**(값·등락률은 온전). 숫자 크기 통일이 요구였으므로 이 쪽을 택했고, 이름까지 살리려면 ⒜ 그 지수만 짧은 표시 이름(예 "반도체")을 쓰거나 ⒝ 이름만 줄이는 예외를 두면 된다(한 줄). APK **v30**(versionCode 30·지문 동일). |
| 2026-09-25 | 접힘 요약 — **긴 이름은 글자를 자동 축소** (APK v29) | android/(widget/StocksWidget, app/build.gradle, twa-manifest) | 요구: "필라델피아 반도체 지수처럼 글자가 많은 이름은 폰트를 맞게 줄여서". Glance에는 **자동 축소(autosize)가 없다** → `summaryScale(name)`이 **글자 폭**으로 배율을 정한다 — 한글·한자·가나는 로마자의 두 배 폭이라 2로 센다(코스피 6 · S&P 500 7 · 필라델피아 반도체 17 · KODEX 200선물인버스2X 21). 배율은 6칸 이하 1.0 / 8 이하 0.88 / 12 이하 0.78 / 16 이하 0.68 / 20 이하 0.58 / 그 위 0.5 — 요구의 네 지수(코스피·코스닥·다우·나스닥)는 **28sp 그대로**, S&P 500은 24.6sp, 필라델피아 반도체는 16.2sp가 된다. **이름만 줄이지 않는다** — 옆 등락률이 그대로 크면 행이 어그러지므로 값·등락률까지 같은 배율로 줄인다. 행 간격(10dp)은 고정(행마다 달라지면 들쭉날쭉). 폭 계산은 python으로 같은 식을 돌려 여섯 이름 모두 한 행(≈290dp) 안에 들어오는지 확인했다(최장 이름도 251dp). ⚠ 첫 빌드는 `sumOf`가 Int/Long 오버로드 모호로 6건 컴파일 오류 → `fold(0)`으로 타입을 못 박아 해소. APK **v29**(versionCode 29·지문 동일). |
| 2026-09-25 | 접힘 요약 **글자 2단계 확대 + 행 간격 확대** (APK v28) | android/(widget/StocksWidget, app/build.gradle, twa-manifest) | 사용자 확인 후 요구: "숨기기 했을 때 보이는 주식 시세 폰트를 2개 정도 키우고 문단 간격도 넓게". 요약 글자 **22→28sp**(글자 크기 단계 2칸 비율 ×1.27), 곁들이는 값 13→16sp, 행 위아래 여백 **6→10dp**(행 사이 20dp). `scaled`를 그대로 쓰므로 ⚙의 글자 크기를 올리면 요약 글자·간격이 같은 비율로 함께 커진다(v22 규약 유지). 이름은 `defaultWeight`+`maxLines 1`이라 길면 말줄임된다 — 28sp에서 "필라델피아 반도체"처럼 긴 이름은 잘릴 수 있다(값·등락률은 그대로 보인다). APK **v28**(versionCode 28·지문 `5ecfc115…9d99` 동일). 웹은 손대지 않았다(변경 파일 전부 android/). |
| 2026-09-25 | 접힌 폰 위젯을 쓸모 있게 — 주식 **요약(크게·상승 빨강/하락 파랑)** 기본 + **투명하게 접기** 옵션, **필라델피아 반도체(^SOX)** 카탈로그 추가 (APK v27) | lib/api/stock/symbols.ts, android/(data/WidgetStore, widget/{StocksWidget,FxWidget,TasksWidget,NotesWidget,WidgetStyleActivity}, app/build.gradle, twa-manifest), _workspace/{verify-quote-api, test-account→account-tool}.mjs | 요구: 제안 **2(요약)를 기본 + 3(투명)을 옵션**으로. 주식은 코스닥·코스피·다우·S&P500·필라델피아 반도체를 "요약처럼 글자를 크게, 상승 빨강·하락 파랑, 배경 투명"으로, 환율은 "투명하게 하여 접어". ① **요약은 접힘 상태에만**: 펼치면 지금까지와 똑같다 — 접었을 때만 목록 대신 큰 글씨 요약이 뜬다(`SummaryRow`, 본문 15sp 기준 **22sp**로 `scaled` — 글자 크기 설정을 키우면 요약도 같은 비율로 커진다). 값(가격)은 작게 곁들여 **이름·등락률만 커 보이게** 했다(요구의 "원래 종목과 구분"). 색은 `AgendaTheme.up/down`(한국 관례) 그대로라 평소 행과 같은 규칙이다. ② **대상은 "지금 위젯에 있는 종목 중에서"**(요구): ⚙ 표시 설정에 현재 종목 체크 목록을 두고, **고른 적이 없으면 지수 전부**가 기본 — 요구의 다섯 지수가 정확히 그것이라 설정을 한 번도 열지 않아도 바로 원하는 화면이 나온다. 없는 종목은 후보에 없다(고를 수는 있는데 영영 안 나오는 일을 막는다). ③ **요약 행은 탭을 주지 않았다** — 평소 행은 탭=삭제인데, 접어 둔 요약을 눌렀다고 삭제 화면이 뜨면 놀란다. 펼치기는 헤더 '보이기'. ④ **투명은 네 위젯 공통 옵션**(`collapsedClear(kind)`) — 접혔을 때만 배경을 칠하지 않는다(`.let{}`으로 background만 건너뜀, `appWidgetBackground()`는 유지). 기본값은 **주식·환율 켜짐 / 작업·노트 꺼짐**(요구가 주식·환율만 지목했고, 글 목록은 배경이 없으면 읽기 힘들다). ⑤ **필라델피아 반도체가 카탈로그에 없었다** — 요약에 넣으려면 먼저 위젯에 추가할 수 있어야 한다 → `INDEX_CATALOG`에 `^SOX`(USD·지수) 추가. 웹 지수 토글·검색·폰 검색이 같은 카탈로그를 쓰므로 한 줄로 세 경로가 함께 열린다(신규 위젯 기본 종목도 6개가 된다). ⑥ 저장은 전부 **이 폰 로컬**(SharedPreferences) — 글자 크기·배경과 같은 자리라 서버·DB·웹 변경 0. 검증: **실서버(로컬 dev + 실 Supabase) 48/48 PASS**(기존 44 + 신규 4 — "반도체" 검색에 ^SOX 1순위, 추가, **시세 12,492.54 USD -0.33%·isIndex**, 삭제; 임시 계정 잔여 0행), tsc 0·lint 0(기존 WidgetFrame 1건만)·test **182/182**·APK **v27**(versionCode 27·지문 `5ecfc115…9d99` = assetlinks 동일). 부수: `_workspace/test-account.mjs`가 node --test의 기본 글롭(`test-*.mjs`)에 걸려 매번 1건 실패로 잡히던 것을 `account-tool.mjs`로 개명(스크립트 내용 동일). adb 미연결로 기기 설치·실측은 미실시 — APK 직접 전달. **잔여(사용자)**: v27 설치 → 주식 위젯 '숨기기' → 지수 요약이 크게 뜨는지, 환율 '숨기기' → 배경이 비치는지, ⚙에서 요약 종목·투명 옵션 확인. |
| 2026-09-25 | 숨기기/보이기 **밀어올리기 정밀 검증**(이미 동작 — 측정으로 확인) + 본문 없는 위젯 **크래시 수정** | components/widgets/shared/urls.ts, lib/widgets/shared/urls.test.ts | 요구: "숨기기 시 아래 위젯이 위로 올라오고 보이기 하면 다시 내려가게". **이미 그렇게 동작**하고 있어 코드 변경 없이 정해진 배치(A 위 / B 바로 아래 / C 옆 열 / D 더 아래)를 심고 실브라우저로 측정해 확인했다 — 데스크톱(1500px): A 426→**102px**, B 631→**307**, D 1063→**739**(둘 다 정확히 324px 상승), **C는 199 그대로**(옆 열이라 안 움직이는 것이 맞다 — 나란히 놓은 위젯이 따라 올라오면 배치가 망가진다), 보이기 → **네 개 전부 원위치**. 모바일 폭(700px·md)에서도 D 415→199→415로 동일. 즉 '안 올라온다'고 느끼신 경우는 **아래가 아니라 옆 열**에 있는 위젯일 가능성이 크다(원하시면 열과 무관하게 모두 올리도록 바꿀 수 있다). ⚠ 검증 중 **별개의 크래시 발견·수정**: 본문(text)이 없는 메모 config에서 `findUrls(undefined)`가 `matchAll`을 호출해 터지고 타일이 오류 화면('위젯을 표시할 수 없습니다')으로 떨어졌다 — 공용 파서에 빈 값 가드 1줄 + 테스트 1건(182/182). 앱으로 만든 메모는 항상 text가 있어 평소엔 안 보이지만, 가져오기·옛 데이터 경로에서 날 수 있는 실제 결함이다. tsc 0·test 182/182. **폰 위젯의 공간 활용**은 코드 변경 없이 제안만 드렸다(위젯은 스스로 런처 격자에서 이동·크기 변경이 불가 — 대화 참조). |
| 2026-09-25 | **'더보기' 제거 → 숨기기/보이기 토글**(PC·폰 동일) (APK v26) | components/canvas/{WidgetPopover 삭제,WidgetFrame,GridCanvas,CanvasShell}, android/(widget/{WidgetListActivity 삭제,ToggleHiddenActivity(신규),StocksWidget,FxWidget,NotesWidget,TasksWidget}, data/WidgetStore, AndroidManifest, res/values/widget_strings, build.gradle, twa-manifest) | 요구: 더보기는 없애고, **숨기기**로 내용을 접어 제목만 남기고 **보이기**로 원래대로 — 폰은 **공간은 그대로 차지한 채** 제목만. 직전 행에서 만든 팝업(웹 WidgetPopover·폰 WidgetListActivity)은 진입점이 사라지므로 **파일째 삭제**했다(죽은 코드로 남기지 않는다). ① **웹**: 헤더 버튼을 아이콘만 있던 ∧/∨에서 **글자 버튼 '숨기기'/'보이기'**로 바꿨다('전체'와 같은 꼴 — 아이콘만으로는 무슨 기능인지 안 보인다). ⚠ **실브라우저에서 잡은 빠진 부분**: 접기는 타일 높이만 2행(102px)으로 줄일 뿐이라 **제목 아래로 내용이 한두 줄 비어져 나왔다**(노트는 CompactView가 스스로 숨겨서 안 보였을 뿐, 메모 등 일반 위젯은 그대로 노출). WidgetFrame이 숨김 상태에서 **본문을 아예 렌더하지 않도록** 고쳐 진짜 '제목만'이 됐다. ② **폰**: 직전 행의 '위젯 높이가 108dp 미만이면 자동으로 제목만'(SizeMode.Exact+LocalSize) 방식을 걷어내고 **사용자가 누르는 토글**로 바꿨다 — 요구가 '누르면'이고, 자동 판정은 크기를 조금 줄였을 뿐인데 내용이 사라져 놀라게 한다. 접힘 여부는 `WidgetStore.hidden(kind)`(이 폰에만, 글자 크기·배경과 같은 자리)에 저장하고, 위젯은 홈 화면 칸을 **그대로 차지한 채** 헤더만 그린다. 즉시 반영은 v14·v15 실측대로 **투명 포그라운드 액티비티**(`ToggleHiddenActivity` — PbInvisible· 터치 통과)가 플래그를 바꾸고 그 종류의 위젯만 다시 그린다. 종류별 고유 data URI(`pbhide://stocks`)로 PendingIntent 병합 차단(v10). ③ **배치**: 토글은 **제목 바로 옆**에 둔다 — 헤더 오른쪽 끝은 폭이 모자라면 잘리는데, 되돌리는 버튼이 잘리면 사용자가 갇힌다. 작업 위젯만 예외로 **보이는 상태에선 글리프(⌃)**, 숨긴 상태에선 '보이기' 글자를 쓴다(필터 3버튼·목록·＋로 헤더가 이미 빽빽하다). 숨기면 작업의 필터도 함께 감춘다(고를 목록이 없다). 검증: **실브라우저**(더보기 버튼 페이지 전체 **0개** 확인 / 숨기기 → 210→**102px**·본문 노드 **0개**·헤더만·아래 위젯 108px 상승·버튼 '보이기'로 전환 → 보이기 → **210px·본문 복원·아래 위젯 원위치**, 콘솔 0), tsc 0·test 181/181·build 그린, APK **v26**(versionCode 26·지문 동일·병합 매니페스트에 ToggleHiddenActivity 있고 WidgetListActivity **0건**). 저장소 lint 1건은 여전히 `WidgetFrame.tsx`의 ⋮ 제목변경 신호(기존 항목·이번 변경과 무관). adb 미연결로 기기 실측은 미실시. **잔여(사용자)**: v26 설치 → 위젯 헤더의 숨기기/보이기 왕복 확인. |
| 2026-09-25 | **주식 위젯 깜빡임·지연 수정** + **모든 위젯 '제목만 접기'·'더보기 팝업'**(PC·폰) (APK v25) | app/api/widget/{stocks,fx}/route.ts, output/api-shapes(WidgetStockQuote.unavailable·WidgetFx.unavailable), components/canvas/{WidgetPopover(신규),WidgetFrame,GridCanvas,CanvasShell}, lib/persistence/usePersistence, android/(widget/{WidgetListActivity(신규),StocksWidget,FxWidget,NotesWidget,TasksWidget}, quotes/{QuoteSync(신규),QuoteAddActivity}, data/{WidgetApi,WidgetStore,model/QuoteItem}, sync/AgendaSyncWorker, AndroidManifest, res/values/widget_strings, build.gradle, twa-manifest), _workspace/verify-quote-api.mjs | 다른 세션을 통해 전달된 요구 3건. ① **일부 종목이 사라졌다 다시 나타나는 현상 = 내가 v23에 넣은 버그**였다 — GET이 `symbols.flatMap(...)`으로 **시세를 못 받은 종목의 행을 통째로 빼** 버려서, Yahoo/KIS가 한 번 흔들릴 때마다 목록에서 종목이 빠졌다가 다음 폴링에 돌아왔다. 이제 **행은 항상 유지**하고 `unavailable` 표식만 붙인다(resolveMeta로 이름·통화는 채운다). 폰은 `QuoteItem.keepKnown`으로 그 행에 **직전 값을 그대로** 보여 주고(가격이 '—'로 깜빡이지도 않는다), 직전 값이 없을 때만 '—'. 환율은 한 번에 다 받거나 못 받으므로 **목록 단위** `unavailable`을 두고, 실패 시 폰이 캐시를 덮어쓰지 않는다(빈 목록을 '통화 없음'으로 오해해 안내 문구가 뜨던 경로 차단). ② **추가 후 반영이 늦던 원인**: 추가 화면이 `AgendaSyncWorker.syncNow`(WorkManager)만 걸고 닫혀서 기기 사정에 따라 수 초~수십 초 뒤에야 목록이 갱신됐다 → `pullNow`(신규) — 추가 API를 부른 **그 백그라운드 스레드에서 곧바로 재조회**(ETag 미전송으로 반드시 200)한 뒤 위젯을 다시 그리고 창을 닫는다. 실패하면 그때만 WorkManager로 폴백. ③ **제목만 접기(PC)**: 노트 전용이던 접기 경로를 **모든 위젯**이 쓰도록 일반화했다 — `computeNoteCollapse`의 'title'·'normal'은 원래 위젯 종류와 무관했고, 하드코딩돼 있던 `registry["note"].minSize.h`만 **그 인스턴스 종류의 minSize**로 바꾸면 끝이었다(usePersistence·GridCanvas 두 곳). 타일 헤더에 접기(∧/∨) 버튼을 달아 누르면 제목 한 줄(2행)로 줄고 **아래 위젯이 그만큼 올라온다**. 상태는 위젯 config(`collapse`/`normalHeight`)에 저장돼 기기 간 공유된다. ④ **더보기 팝업(PC)**: 전체보기(FocusOverlay)는 화면을 다 덮으므로 요구('화면 전체를 가리지 말 것')에 맞지 않아 `WidgetPopover`를 새로 만들었다 — **스크림 없음**(뒤 캔버스가 보이고 읽힌다), 타일 자리에서 시작해 **화면 아래 12px까지** 늘어나고, 타일이 화면 하단이면 **위로 올려** 최소 높이(화면 60%·최대 520px)를 확보한다(처음엔 148px밖에 안 돼 실측 후 보정). 내용은 같은 ExpandedView를 재사용하고 헤더의 '전체'로 승격할 수 있다. 스크림이 없으니 바깥 클릭은 document capture로 듣는다. ⑤ **폰도 제목만 + 더보기**: 네 위젯 모두 `SizeMode.Exact`로 실제 높이를 읽어 **108dp보다 낮게 줄이면 헤더만** 그린다(작업은 그때 필터도 감춘다 — 목록이 없으니 고를 이유가 없다). 헤더의 **더보기**는 `WidgetListActivity`(신규, **대화상자 테마**라 화면을 다 가리지 않는다)를 열어 목록 전체를 스크롤로 보여 주고, 행을 누르면 기존 화면(작업 수정·소제목 내용·주식/환율 삭제)으로 간다. 팝업은 **캐시를 그리므로 네트워크 호출 0**이고 위젯과 값이 정확히 같다. 헤더 폭을 벌려고 주식·환율의 '＋ 종목/＋ 통화'는 '＋'로, 갱신 시각의 '갱신' 글자는 뺐다(↻는 유지). 검증: **실서버 44/44 PASS**(기존 41 + 신규 3 — config에 조회 불가 심볼을 직접 심어 **행이 남고 unavailable이 붙는지**, 정상 행엔 안 붙는지), **실브라우저**(메모 타일 접기 210→**102px**·아래 위젯 108px 상승·버튼 라벨 전환, DB에 `collapse:title`+`normalHeight:4`·layout h=2 저장 확인 — **노트가 아닌 위젯**에서, 더보기 팝업 420×432 / 뷰포트 1280×720에 `coversScreen:false`·하단 여백 12px·23행 스크롤, Esc·바깥 클릭 닫힘, 콘솔 0), tsc 0·test 181/181·build 그린, APK **v25**(versionCode 25·지문 동일·병합 매니페스트에 WidgetListActivity 확인). ⚠ 저장소 전체 lint는 **1건**이 남는데 `WidgetFrame.tsx:165`(⋮ '제목 변경' 신호 effect)로 **이번 변경과 무관한 기존 항목**이다(2026-08-18 행에도 기록). **잔여(사용자)**: v25 설치 → 위젯을 낮게 줄여 '제목만' 확인 + 더보기 팝업, PC에서 헤더 ∧로 접기·더보기. |
| 2026-09-24 | 폰 위젯 **행 간격 확대** + **주식·환율을 폰에서 추가·삭제**(검색 포함) (APK v24) | android/(widget/{WidgetStyle(rowPadDp),StocksWidget,FxWidget,TasksWidget,NotesWidget,AgendaWidget}, quotes/{QuoteAddActivity,QuoteDeleteActivity}(신규 2), data/{WidgetApi,WidgetStore,model/{QuoteItem,FxItem}}, AndroidManifest, res/values/widget_strings, build.gradle, twa-manifest), lib/api/stock/search.ts(신규), app/api/{stocks/search,widget/stocks,widget/fx}/route.ts + app/api/widget/stocks/search/route.ts(신규), output/api-shapes(WidgetSymbolHit·WidgetSymbolSearch), components/widgets/{shared/useConfigRemoteSync(신규),note/{CompactView,useNoteRemoteSync 삭제},stock/CompactView,fx/CompactView}, _workspace/verify-quote-api.mjs | 요구 2건. ① **문단 간격**: 목록 행 여백 3dp(행 사이 6dp)가 빽빽하다는 신고 → `WidgetStyle.rowPadDp` 신설로 **6dp(행 사이 12dp)**, 오늘 일정은 4→7dp. 글자 크기를 키우면 간격도 같은 비율로 커진다(`scaled`) — 큰 글씨에 좁은 간격이 더 답답하기 때문. 작업·노트·주식·환율·오늘 일정 다섯 위젯 모두 적용. ② **폰에서 추가·삭제**(직전 행의 '읽기 전용' 결정을 요구로 뒤집음): 헤더에 **＋ 종목/＋ 통화**, 행을 누르면 **2단계 확인 삭제**. ⒜ **검색이 관건**이었다 — 폰엔 KRX 카탈로그가 없다. 웹 검색 라우트에 인라인으로 있던 번역·Yahoo 호출을 `lib/api/stock/search.ts`로 빼 **웹과 폰이 같은 경로**를 쓰게 하고(두 벌이면 결과가 갈라진다), 폰용 `searchAllSymbols`는 지수→국내→미국 순으로 합쳐 준다(국내에서 충분히 찾으면 Yahoo를 부르지 않아 타이핑마다 외부 호출을 하지 않는다). ⒝ **브리지 확장**: `/api/widget/stocks` POST(심볼 추가)·DELETE(?symbol=), `/api/widget/fx` POST(통화)·DELETE(?code=), `/api/widget/stocks/search`. 심볼에 `^`가 들어가 경로 인코딩이 성가시므로 삭제는 **쿼리 파라미터**로 받는다. ⒞ **넣기 전에 값을 확인한다** — 주식은 provider로 시세를, 환율은 fetchRates로 환율을 한 번 조회해 나오지 않으면 400. 오타가 config에 남아 위젯에서 영영 '—'로 보이는 일을 막는다(폰에는 원인이 안 보인다). ⒟ **config 통째 쓰기 금지**: 서버가 현재 config에 `symbols`/`quotes`만 갈아끼운다 — 폰 조작으로 mobileSync·색·크기가 날아가지 않는다(검증에 불변식으로 고정). ⒠ 환율의 **기준 통화(base)** 삭제 요청은 409로 웹으로 보낸다 — 폰이 기준을 바꾸면 위젯 전체 의미가 달라진다. ⒡ **웹이 폰을 따라잡게**: 노트 전용이던 포커스 재조회 훅을 `shared/useConfigRemoteSync`로 일반화해 노트(sections)·주식(symbols)·환율(quotes)이 함께 쓴다(pb_widgets는 realtime 발행 대상이 아니라 열려 있는 웹이 폰의 변경을 모른 채 되덮을 수 있다). ⒢ 안드로이드는 Glance 규약 그대로 — 행마다 **고유 data URI**(`pbdel://{kind}/{심볼}`)로 PendingIntent 병합 차단(v10), clickable→padding(v5), 삭제 시 캐시에서 즉시 빼고 재렌더(다음 동기화를 기다리면 '삭제가 안 먹었다'로 보인다). 미연결이면 ＋를 감춘다(눌러도 409). 검증: **실서버 41/41 PASS**(기존 20 + 신규 21 — 한글·지수·영문 검색, 소문자 심볼 정규화 추가, 중복 409, 없는 종목 400, **mobileSync 보존**, 삭제 후 목록 반영, 재삭제 404, 통화 추가/중복/KRW 거부/형식 오류/없는 통화/삭제/재삭제), **실브라우저**(폰 역할로 '카카오' 검색→추가 → 웹 창 포커스 복귀에 타일에 즉시 등장 → 폰에서 삭제 → 포커스 복귀에 사라짐, 콘솔 0), tsc 0·lint 0·test 181/181·build 그린(라우트 3종), APK **v24**(versionCode 24·지문 동일·병합 매니페스트에 QuoteAddActivity·QuoteDeleteActivity 확인). ⚠ 검증 중 **429**를 만났다 — 디바이스 토큰당 **분당 20회**(requireDevice) 한도다. 실사용(15분 주기 + 가끔 탭)과는 무관하지만 검증 스크립트는 구간마다 기기를 새로 붙이도록 고쳤다. **잔여(사용자)**: v24 설치 → 간격 확인, ＋로 종목·통화 추가, 행 눌러 삭제. |
| 2026-09-24 | **폰 위젯 2종 추가 — 주식·환율**(웹 위젯과 연동·대상 선택) + 주식 **시간외 표식 PRE/시간외/AFTER** (APK v23) | output/api-shapes(StockQuote.session·WidgetStocks·WidgetFx 신규), lib/api/{stock/{fallbackClient,kisClient},widgetCore(+test),widgetMobileTarget(신규)}, app/api/widget/{stocks,fx}/route.ts(신규 2), components/widgets/{shared/MobileSyncToggle(신규),fx/{rows(신규),useFxRates,types,ConfigEditor},stock/{types,ConfigEditor,format,QuoteRow},tasks/ConfigEditor}, lib/widgets/fx/rows.test.ts(신규), android/(widget/{StocksWidget,StocksWidgetReceiver,FxWidget,FxWidgetReceiver}(신규 4)·{AgendaTheme,WidgetStyleActivity,ItemColorRow}, data/{WidgetApi,WidgetStore,model/{QuoteItem,FxItem}(신규 2)}, sync/AgendaSyncWorker, res/{xml,layout} 4 신규·widget_strings, AndroidManifest, build.gradle, twa-manifest), _workspace/verify-quote-api.mjs(신규) | 요구: 주식·환율도 폰 위젯으로, 웹과 연동하고 **사용자가 대상 위젯을 선택**하되 "현재는 기존 위젯에 연결", 주식은 **시간외 거래도 확인**되게 하고 그 경우 pre 등을 표시. ① **읽기 전용 설계**: 작업·노트와 달리 폰에서 고칠 것이 없다 — 종목·통화 추가/삭제는 웹에서만 하고 폰 위젯엔 행 탭 동작이 없다(같은 검색 UI를 폰에 두 번 만들지 않는다). ② **대상 선택 = 같은 규칙 + 폴백 하나**: 속성의 '모바일 홈 화면에 표시'(config.mobileSync) 중 마지막에 켠 것. 다만 **지정이 없고 그 종류 위젯이 하나뿐이면 그것**(`pickMobileInstanceOrOnly`, 테스트 4케이스) — 고를 여지가 없는데 "웹에서 켜세요"를 띄우면 일만 늘고, 읽기 전용이라 잘못 쓸 위험이 없다. 요구의 '현재는 기존 위젯에 연결'이 이것으로 **사용자 데이터를 건드리지 않고** 성립한다(실계정 확인: 주식·환율 각 1개 → 자동 연결). 작업·노트에는 이 폴백을 **쓰지 않는다** — 거기선 폰이 글을 쓰므로 대상이 틀리면 엉뚱한 노트가 고쳐진다(v19 결정 유지). ③ **시간외 표식**(요구): StockQuote에 `session?: "pre"|"post"`를 더하고, 값을 **실제로 갈아끼운 그 자리**에서만 채운다 — 미국은 프리/애프터 분봉을 채택할 때 `currentTradingPeriod.regular.start` 기준으로 pre/post를 가르고(fallbackClient), 국내는 KIS **시간외 단일가**를 덧입힐 때 post(kisClient). 등락은 그대로 **전일 종가 대비**라 표식이 붙어도 숫자의 의미가 바뀌지 않는다. 표시는 국내 '시간외' · 미국 PRE/AFTER(`sessionLabel` 한 곳, 웹 QuoteRow 배지 + 폰 위젯 공용 규칙). ④ **브리지 2라우트**: GET `/api/widget/{stocks,fx}`(Bearer 디바이스 토큰·ETag/304). 시세는 웹과 **같은 provider**(KIS 있으면 KIS, 없으면 키리스 폴백)를 타고, 종목 순서는 웹에 저장된 순서를 지킨다. 환율은 **폰이 계산하지 않도록** 원화 환산까지 서버가 끝내 보낸다. ⑤ **환산식 한 곳**: `/api/fx`는 base=KRW라 rates[C]=1원당 C — 뒤집기와 전일대비 **부호 반전**을 웹 훅과 브리지가 따로 하면 숫자가 갈라진다 → `components/widgets/fx/rows.ts` 하나를 둘이 함께 쓴다(테스트 4: 뒤집기·엔 100단위·부호 반전·0 나눗셈 차단). ⑥ **토글 UI도 공용화**: 작업 ConfigEditor에만 있던 토글을 `shared/MobileSyncToggle`로 빼 작업·주식·환율이 같은 컴포넌트를 쓴다(즉시 영속 규약 포함). ⑦ **안드로이드**: Glance 위젯 2종 — 헤더는 `주식 ⚙`/`환율 ⚙`(탭=표시 설정) + 우측 갱신 시각(탭=즉시 동기화), 행은 이름·가격·등락(**한국 관례 상승 빨강·하락 파랑**, AgendaTheme.up/down 신설), 시간외면 이름 옆 표식. v22의 글자 크기·배경 설정을 그대로 받는다(kind: stocks·fx). AgendaSyncWorker가 같은 15분 주기로 함께 받고, cancelIfNoWidgets는 **다섯 위젯이 모두 사라질 때만** 멈춘다. Glance 함정은 전부 기존 규약대로(refreshTick v15·고유 data URI v10·clickable→padding v5·행 구조 고정 v6). 검증: **실서버 20/20 PASS**(익명 401 2종 / 위젯 하나면 자동 연결 / 종목 순서 보존 / 둘인데 미지정이면 미연결 / 지정하면 그 위젯 / 미국 종목 USD / ETag 304 2종 / USD 1,366.70원·100엔 863.44원 **자릿수 대역 검사**로 뒤집기 오류 차단 / 위젯 삭제 시 빈 응답, 임시 계정 잔여 0행), **실브라우저**(playwright MCP가 끊겨 gstack browse 사용 — 주식·환율 ⋮ 편집에 토글 렌더 → 켜고 **'저장' 없이 ✕로 닫아도** DB에 `mobileSync:true`+`mobileSyncAt` 즉시 영속·symbols/quotes 보존 → 그 계정 디바이스 토큰으로 브리지 조회 시 **웹에서 켠 바로 그 인스턴스** 반환, 콘솔 에러 0), tsc 0·lint 0·test **181/181**(신규 5)·build 그린(라우트 2종 등록), APK **v23**(versionCode 23·지문 `5ecfc115…9d99` = assetlinks 동일·병합 매니페스트에 StocksWidgetReceiver·FxWidgetReceiver 확인). **검증 중 실제로 잡힌 값**: AAPL이 `session:"pre"`로 왔다(KST 오후 = 미국 프리마켓) — 표식 경로가 실데이터로 증명됨. adb 미연결로 기기 설치는 미실시. **잔여(사용자)**: v23 설치 → 홈 화면에 '주식'·'환율' 위젯 추가(웹에서 켜지 않아도 기존 위젯 1개씩이라 바로 연결). |
| 2026-09-21 | **PC 위젯 팔레트가 열리지 않던 버그 수정**(아이콘이 사라지고 아무것도 안 보임) + 추가된 위젯으로 스크롤 | components/canvas/{WidgetPalette,CanvasShell}.tsx | 사용자 신고: "PC에서 새 위젯을 추가하려고 아이콘을 클릭했는데 사라지고 작동하지 않는다". **실브라우저 재현으로 원인 확정**: 팔레트는 끌어서 옮긴 위치를 `pb:palette:pos`(localStorage)에 저장하는데, 화면 안으로 되돌리는 보정(clamp)이 **창 크기 변경 때만**, 그것도 **팔레트가 열려 있을 때만** 돌았다(리스너가 `if (collapsed) return` 뒤). 그래서 넓은 창(큰 모니터·최대화·낮은 확대율)에서 오른쪽으로 옮겨 두고 닫은 뒤 창이 좁아지면, 다시 열 때 우하단 열기 아이콘은 사라지고(=열림 상태) 팔레트는 **저장된 화면 밖 좌표**에 그려진다 — 재현: 저장 x=2300 · 뷰포트 1400px → 아이콘 소멸 + 팔레트 `onScreen:false`. 새로고침 시 열린 채 마운트되는 경로도 같은 이유로 무보정. ① **열릴 때마다 보정**: 기존 resize 효과에서 등록 직전에 한 번 `onResize()`를 실행하고 `useLayoutEffect`로 바꿔 페인트 전에 자리를 잡는다(마운트·재열기·resize 한 경로, 교정된 좌표는 저장값도 덮어써 자가치유). ② **추가 후 새 타일로 스크롤**: 재현 중 두 번째 원인도 드러났다 — 더블클릭 추가는 정상 동작하지만 보드 **맨 아래**(예 y=1710, 뷰포트 900)에 붙어 화면에 변화가 없어 '작동하지 않음'으로 보인다. `addByType`이 추가 직후 rAF 2회(커밋·레이아웃 뒤) `[data-pb-instance]`를 `scrollIntoView({block:center})` — 모바일 탭 추가도 같은 경로라 함께 개선. ③ **추가 방법 안내 한 줄**: 데스크톱은 실수 방지로 **한 번 클릭은 추가하지 않는다**(더블클릭·드래그 설계 유지) — 방법이 툴팁에만 있어 '눌러도 안 된다'로 보이므로 팔레트 목록 위에 '더블클릭하거나 캔버스로 끌어다 놓아 추가'를 상시 표시. **실브라우저 검증**(승인 테스트 계정, 검증 후 삭제 — 잔여 0행): 수정 전 x=2300 저장·닫힘 → 열기 클릭 → 아이콘 사라지고 팔레트 화면 밖(재현) / 수정 후 같은 조건 → **x=1144 완전히 화면 안**·저장값 교정, 열린 채 (-500, 3000) 저장 후 새로고침 → (16, 372) 화면 안, 더블클릭 '단위 변환' → 1개 추가·scrollY 0→1560·새 타일 화면 안, 모바일 390px FAB→시트→탭 '타이머' → 추가·시트 닫힘·새 타일 화면 안, 콘솔 에러 0. tsc 0·lint 0·test 176/176·build 그린. |
| 2026-09-21 | **폰 위젯 표시 설정** — 글자 크기 5단계 · 배경 5색 · **작업·소제목마다 글자색** 5색 (APK v22) | android/(widget/{WidgetStyle,WidgetStyleActivity,ItemColorRow}(신규 3)·{TasksWidget,NotesWidget}, data/WidgetStore, tasks/TaskEditActivity, notes/NoteEditActivity, AndroidManifest, res/values/widget_strings, app/build.gradle, twa-manifest) | 요구: "해당 핸드폰에서 간단하게 글자크기나 배경색을 변경" + "각각의 작업 및 소제목에 대해서 글자색", 크기 5단계·색 5개 내외. ① **저장은 폰 로컬 전용**(SharedPreferences) — 서버·DB·웹 변경 0, 마이그레이션 0. 요구가 '해당 핸드폰에서'이고, pb_widgets config에 올리면 디바운스 되덮기 경합과 스키마 변경을 새로 떠안는다(작업 색은 pb_tasks 컬럼 추가까지 필요해 비대칭도 생긴다). 키는 위젯 종류별 접미사(`style_text_tasks`·`item_colors_notes`…)로 작업·노트가 서로 독립. ② **글자 크기 5단계**(12·13.5·**15=지금까지의 기본**·17·19sp): 본문(제목·일자·빈 목록)은 그 값 그대로, 나머지 본문 요소(진행/완료 라벨·🖼·›·'삭제')는 `scaled()`로 같은 비율. 헤더(제목·필터·갱신 시각)는 그대로 — v16의 '본문만' 규약을 유지했다. ③ **배경 5색**(기본·흰색·회색·크림·하늘) — 라이트/다크 **쌍**으로 정의(한쪽만 정하면 반대 테마에서 글자가 배경에 묻는다). '기본'은 위젯 고유 배경(작업=흰색, 노트=v21의 옅은 회색)이라 지금 화면이 그대로 유지된다. ④ **항목별 글자색 5색**(기본·빨강·주황·초록·파랑, 역시 라이트/다크 쌍): 작업은 수정 화면, 소제목은 내용 화면에 색 줄을 붙였다 — **고르는 즉시 반영**(폰 안에만 남는 값이라 서버 왕복이 없다. '취소'로 닫아도 색은 남는다 = 글 내용과 별개의 설정). **추가 화면에는 없다** — id를 서버가 만들기 때문에 아직 색을 걸 대상이 없다(저장 후 그 항목을 눌러 고른다). 작업의 완료·삭제 예정은 **상태 표시가 우선**(흐림+취소선)이라 그때는 고른 색을 쓰지 않는다. ⑤ **진입점 = 위젯 제목 탭**(`작업 ⚙`·`노트 ⚙`) → WidgetStyleActivity. 헤더에 버튼을 하나 더 늘리면 좁은 위젯에서 ＋·갱신 시각이 밀려나므로 글리프만 붙이고, 그 폭만큼 작업 위젯 필터 버튼 좌우 여백을 7→5dp로 줄여 상쇄했다. 설정 화면은 **저장 버튼 없음**(고르는 즉시 저장+위젯 재렌더) + 미리보기 상자(고른 크기·배경을 그대로 그린다) + 라디오 글자 자체를 그 크기로 렌더. ⑥ **Glance 함정 재적용**: 종류마다 고유 data URI(`pbstyle://tasks`)로 PendingIntent 병합 차단(v10), `clickable()`→`padding()` 순서(v5), LazyColumn itemId에 **색 index를 비트로 포함**해 색이 바뀌면 완전 재바인딩(v6) — 작업은 `id shl 4 | 삭제마크 shl 3 | 색`, 노트는 `key shl 3 | 색`. ⑦ 색 맵은 기본색(0)을 아예 저장하지 않고, 200건을 넘을 때만 살아 있는 id로 정리한다(막 추가한 항목이 아직 캐시에 없을 수 있어 매번 정리하지는 않는다). 검증: APK **v22**(versionCode 22·지문 `5ecfc115…9d99` = assetlinks 동일 → v21 위에 업데이트 설치 가능·병합 매니페스트에 WidgetStyleActivity 확인), 웹은 손대지 않았음을 tsc 0·test 176/176으로 재확인(변경 파일 전부 android/). 첫 빌드는 `androidx.glance.color.ColorProvider`가 **함수만** 제공하고 타입은 `androidx.glance.unit.ColorProvider`라 4건 컴파일 오류 → 타입/팩토리 import 분리로 해소. AAB jarsigner 단계는 기존대로 실패(사이드로드 무관). adb 미연결로 기기 설치·실측은 미실시 — APK 직접 전달. **잔여(사용자)**: v22 설치 → 위젯 제목의 ⚙ 탭 → 글자 크기·배경 확인, 작업/소제목을 눌러 글자색 확인. |
| 2026-09-20 | 폰 위젯 두 종 — **새로 쓴 것이 맨 위로**, 노트 위젯 배경 **옅은 회색**으로 구분 (APK v21) | app/api/widget/tasks/route.ts, lib/api/{widgetNote(appendSection→**prependSection**),widgetNote.test}.ts, app/api/widget/notes/route.ts, android/(widget/{AgendaTheme(bgNote 신규),NotesWidget}·data/WidgetApi·res/layout/notes_widget_preview·build.gradle·twa-manifest), _workspace/{verify-notes-api,verify-tasks-order(신규)}.mjs | 요구 2건. ① **신규 항목이 맨 위**: 작업은 `/api/widget/tasks` GET 정렬을 `created_at asc → desc`로 뒤집었다(폰 위젯은 위쪽만 보이는 경우가 많다). 노트는 **표시 순서를 뒤집지 않고**(소제목 순서는 웹에서 ▲▼로 사용자가 정하는 값이라 뒤집으면 그 의도와 싸운다) **추가 위치**를 바꿨다 — `appendSection`을 `prependSection`으로 바꿔 폰에서 만든 소제목이 배열 맨 앞에 들어간다. 배열이 곧 웹 표시 순서라 **웹 노트에서도 맨 위**에 보이고, 웹의 '＋ 소제목'(캐럿 기준 위/아래 삽입)은 별도 경로라 영향 없다. ② **노트 위젯 배경 구분**: `AgendaTheme.bgNote` 신설 — 라이트 `#F1F3F5`(흰색보다 한 톤 어둡게), 다크 `#20252B`(#16191C보다 한 톤 밝게). 다크에서 더 어둡게 하면 구분이 안 되므로 방향을 반대로 잡았다. 위젯 선택기 미리보기 레이아웃 배경도 같은 회색으로 맞췄다. ⚠ **웹 작업 위젯은 여전히 오래된 순**(useTasks의 `created_at asc`) — 요구가 '핸드폰에 적용되는 위젯'이라 폰 경로만 바꿨다. 웹도 맞추려면 한 줄. 검증: 실서버 **38/38 PASS**(노트, '지정 노트 맨 위에 붙는다'로 갱신) + **4/4 PASS**(신규 `verify-tasks-order` — 오래된/중간/새것 3건을 created_at 지정해 심고 폰이 받는 순서가 `가장 새로운 → 중간 → 가장 오래된`, 폰에서 추가한 것도 맨 위), tsc 0·lint 0·test 176/176·build 그린, APK **v21**(versionCode 21·지문 동일). adb 미연결로 기기 설치는 미실시 — APK 직접 전달. **잔여(사용자)**: v21 설치 → 두 위젯 나란히 놓고 배경 차이·새 항목이 맨 위인지 확인. |
| 2026-09-20 | 노트 위젯 — **갱신 시각을 우측 상단으로 + 탭하면 즉시 동기화**, ＋ 라벨 노출, 웹에 **휴대폰 연결 상태** 표시 (APK v20) | android/(widget/{NotesWidget,SyncNowActivity(신규)}·Manifest·build.gradle·twa-manifest), components/widgets/note/ConfigEditor | 사용자 신고: "웹에서 '폰에 연결'을 켰는데 연결이 확인되지 않는다". **데이터로 먼저 확인**: 해당 노트는 `mobileSync=true`(설정 시각 기록됨)이고 기기(SM-F956N)의 `last_seen_at`도 토글 **이후** 시각이라 서버 연결은 정상이었다 — 문제는 **확인할 방법이 없다는 것**이었다. ① **웹**: 지정을 켜면 속성에 연결 상태를 보여 준다 — `pb_widget_devices`를 RLS로 직접 조회해 "휴대폰 N대 연결됨 · 마지막 동기화 9월 20일 오후 04:38", 기기가 없으면 "아직 연결된 휴대폰이 없습니다 — 앱 설치 후 설정 > 위젯에서 코드 발급" (실제 원인이 대개 이것). 갱신 주기(15분)와 즉시 갱신 방법도 같이 안내. ② **폰**: 갱신 시각을 헤더 **맨 오른쪽**으로 옮기고(요구) 아직 못 받았으면 '갱신 전', **누르면 즉시 동기화**(`SyncNowActivity` — 투명·터치 통과 포그라운드 액티비티로 syncNow 실행 후 즉시 닫힘; 비포그라운드에선 재렌더·작업 실행이 미뤄진다는 v14/v15 실측을 그대로 따름). 15분을 기다리지 않아도 된다. ③ **헤더는 연결 전에도 그린다** — 전에는 미연결이면 안내만 떠서 갱신 시각도 ＋도 보이지 않았다(신고의 다른 절반). ＋는 글리프 하나라 눈에 안 띄어 **'＋ 소제목'** 라벨을 붙였고, 미연결일 때는 눌러도 409라 감춘다. ④ 요구의 '추가·수정·삭제'는 v19에 이미 있다(＋ 소제목 / 제목 탭 → 저장 / 2단계 확인 삭제) — 발견성만 손봤다. 검증: 실브라우저(지정 켜기 → "아직 연결된 휴대폰이 없습니다" → 기기 페어링 후 재조회 → "휴대폰 1대 연결됨 · 마지막 동기화 …", 콘솔 0), tsc 0·lint 0·test 176/176·build 그린, APK **v20**(versionCode 20·지문 동일·병합 매니페스트에 SyncNowActivity 확인). **잔여(사용자)**: v20 설치 → 홈 화면 '노트' 위젯의 우측 상단 갱신 시각 탭 → 즉시 갱신 확인. |
| 2026-09-20 | **폰 노트 위젯이 볼 노트를 사용자가 선택** — 노트 속성·편집 화면의 '모바일 홈 화면에 표시' 토글, 지정된 **한 노트**에만 연동 (APK v19) | lib/api/widgetNoteTarget.ts(신규), lib/api/widgetCore(pickTasksInstance→**pickMobileInstance**), app/api/widget/{notes/route,notes/[id]/[sectionId]/route,tasks/route}.ts, components/widgets/note/{types,ConfigEditor,ExpandedView,CompactView}, lib/{persistence/usePersistence,widgets/persistence}(setShareTargetNote→**setExclusiveNoteFlag**), components/canvas/CanvasShell, output/api-shapes(WidgetNotes.instanceId), android/(WidgetApi·WidgetStore·AgendaSyncWorker·NotesWidget + build.gradle·twa-manifest), _workspace/verify-notes-api.mjs | 요구: 폰 노트 위젯이 **하나의 웹 노트 위젯**과 연결되고, 어느 위젯과 연결할지 **사용자가 편집에서 고르게**. 직전 행에선 대상 노트를 `/share`의 '공유 받기' 노트(없으면 가장 오래된 노트)로 **추측**했는데, 그러면 어느 노트에 글이 들어갈지 사용자가 모른다 → **명시적 지정**으로 바꿨다. ① **작업 위젯과 같은 규칙 재사용**: config `mobileSync`+`mobileSyncAt`, 해석은 `pickMobileInstance`(기존 pickTasksInstance를 두 위젯 공용으로 개명). 지정이 없으면 `instanceId:null`+빈 목록 → 폰이 "웹에서 켜세요" 안내(NotLinked). ② **선택은 배타적**: 노트에 이미 있던 교차 인스턴스 지정(`setShareTargetNote` — 공유 받기)을 `setExclusiveNoteFlag(instanceId, flag, on)`로 일반화해 shareTarget·mobileSync가 같은 로직을 쓴다. 켜면 다른 모든 노트에서 자동으로 꺼져 **항상 정확히 한 노트**만 연결된다(작업 위젯의 '마지막에 켠 쪽이 이긴다'보다 명확 — 두 위젯이 동시에 '연결됨'으로 보이지 않는다). mobileSyncAt은 다기기 동시 조작 대비 안전망으로 계속 기록. ③ **토글 위치 2곳**: 속성(⋮ 스타일 편집)과 **⋮ 편집(전체 화면 편집기 헤더의 '폰에 연결/연결됨' 버튼)** — 노트는 ⋮ '편집'이 편집기를 열기 때문에 지정 스위치가 '스타일 편집' 뒤에만 있으면 찾기 어렵다(요구가 "편집에 추가"). 둘 다 같은 배타 지정을 호출해 항상 일치하고 즉시 영속된다. 타일에는 📱 배지. ④ **서버도 지정으로 한정**: 조회·추가·수정·삭제가 전부 그 한 노트에만 걸리고, 비지정 노트를 건드리면 **409**(폰의 옛 캐시가 엉뚱한 노트를 고치는 사고 방지). 검증: 실서버 **38/38 PASS**(지정 전 빈 목록·추가 409 / 지정 후 그 노트만 조회 / 비지정 노트 수정·삭제 409 / 서식 보존 / 이미지 409 / 지정 이동 시 대상 전환 + 옛 캐시 409 / 지정 해제 시 다시 미연결), **실브라우저**(속성 토글 즉시 영속 확인 → 편집 화면 버튼과 상태 일치 → 편집 화면에서 켜자 다른 노트 지정 자동 해제(정확히 1개) → 폰이 그 노트만 조회·추가, 콘솔 0), tsc 0·lint 0·test 176/176·build 그린, APK **v19**(versionCode 19·지문 동일). adb 미연결로 기기 설치는 미실시 — APK 파일 직접 전달. **잔여(사용자)**: v19 설치 → 웹 노트 편집에서 '폰에 연결' → 홈 화면 '노트' 위젯에서 그 노트의 소제목 확인. |
| 2026-09-20 | **폰 위젯을 메모 → 노트로 교체** — 노트의 **소제목 하나 = 목록 한 건**, 폰에서 삭제까지 (APK v18) | app/api/widget/notes/{route,[id]/[sectionId]/route}.ts(신규), lib/api/{widgetNote,widgetNote.test}.ts(신규), output/api-shapes(WidgetNoteItem), components/widgets/note/{useNoteRemoteSync(신규),CompactView}, android/(data/model/NoteItem·widget/{NotesWidget,NotesWidgetReceiver}·notes/NoteEditActivity 신규 4 + WidgetApi·WidgetStore·AgendaSyncWorker·res 3·Manifest·build.gradle·twa-manifest), 삭제: app/api/widget/memos/**·lib/api/widgetMemo*·memo/useMemoRemoteSync·android memo 4종, _workspace/{verify-notes-api,seed-note-widget,phone-note-edit}.mjs(신규) | 직전 행(메모 위젯)의 모델을 사용자 요구로 갈아엎었다: **메모 위젯 인스턴스 하나 = 한 건** → **노트 위젯의 소제목(section) 하나 = 한 건**. 이름도 '노트 위젯'. 폰 **삭제 허용**(메모 모델에선 삭제가 캔버스 위젯을 없애서 뺐지만, 소제목 삭제는 노트 위젯을 남기므로 안전하다). 비밀번호(pwHash) 처리는 노트에 그 개념이 없어 전부 제거. ① **브리지**: GET(노트들의 소제목 전부, 노트 최근 수정 순·ETag/304) / POST(대상 노트 맨 아래에 소제목 추가) / `[id]/[sectionId]` PATCH+POST별칭·DELETE. 경로를 두 조각으로 나눠 합성 키 인코딩 문제를 피했다. **대상 노트는 `/share`와 같은 규칙**(공유 받기 노트 → 없으면 가장 오래된 노트) — "모바일에서 들어오는 것을 받는 노트"라는 뜻이 이미 그 플래그에 있어 새 설정을 만들지 않았다. ② **새 위험 = 서식 파괴**(메모는 평문이었지만 소제목 본문은 리치 HTML이다). 순수 모듈 `lib/api/widgetNote.ts`가 세 가지를 고정(테스트 19): ⓐ **이미지·표가 있는 소제목은 본문 수정 거부**(409, 폰은 읽기 전용으로 표시) — 평문으로 덮으면 data URL 이미지·표가 복구 불가, ⓑ **평문이 그대로면 HTML을 다시 쓰지 않는다** — 열어만 보거나 제목만 고쳐도 굵게·색이 풀리지 않게, ⓒ **sections 배열만 교체** — 머리말(html)·첨부·공유 플래그·접기 상태 보존. HTML↔평문 변환은 블록 경계를 줄바꿈으로 살리고(기존 sanitize.htmlToText는 미리보기용이라 공백을 접는다) 엔티티는 `&amp;`를 마지막에 풀어 `&amp;lt;`가 뭉개지지 않게 했다. ③ **되덮기 완화**는 메모와 동일 구조로 이식(`useNoteRemoteSync` — 창 포커스 복귀 시 자기 행 재조회, 입력 중 skip; pb_widgets는 realtime 발행 대상이 아니고 넣기엔 WAL 비용이 과하다). ④ **안드로이드**: NotesWidget(소제목 제목만 나열, 작업 위젯 v15 교훈 그대로) + NoteEditActivity(보기+수정+**2단계 확인 삭제**, rich면 본문 읽기 전용+안내). 검증: 실서버 **30/30 PASS**(평문 변환·머리말 제외·rich 판정·**서식 보존**·이미지 409 거부 후 원본 유지·제목만 변경은 허용·공유 받기 노트에만 추가·소제목만 삭제되고 노트는 유지·재삭제 404·ETag 304), **실브라우저**(폰 역할로 수정→추가→삭제 각각 창 포커스 복귀에 타일 반영, 머리말 무변경, 콘솔 0), tsc 0·lint 0·test **176/176**(메모 8 제거·노트 19 추가)·build 그린(노트 라우트 2종), APK **v18**(지문 = assetlinks·versionCode 18·병합 매니페스트에 NotesWidgetReceiver·NoteEditActivity, Memos* 사라짐 확인). adb 미연결로 기기 설치는 미실시 — APK 파일을 사용자에게 직접 전달. **잔여(사용자)**: v18 설치 → 홈 화면 '노트' 위젯 추가 → 소제목 목록·내용 보기·수정·추가·삭제 확인. |
| 2026-09-20 | **홈 화면 메모 위젯**(제목 목록 → 탭하면 내용) — 웹 메모 위젯과 양방향 연결 (APK v17) | app/api/widget/memos/{route,[id]/route}.ts(신규), lib/api/{widgetMemo,widgetMemo.test}.ts(신규), output/api-shapes(WidgetMemo), components/widgets/memo/{useMemoRemoteSync(신규),CompactView}, android/(data/model/MemoItem·widget/{MemosWidget,MemosWidgetReceiver}·memo/MemoEditActivity 신규 4 + WidgetApi·WidgetStore·AgendaSyncWorker·res 3·Manifest·build.gradle·twa-manifest), _workspace/{verify-memos-api,phone-memo-edit}.mjs(신규) | 요구: 작업 위젯처럼 메모도 폰 위젯으로 — 네이버 메모를 전제로 하되 **제목이 목록으로** 보이고 누르면 내용, 웹 메모 위젯과 동일·연결. **설계 선택(사용자 확인)**: 메모 한 건 = 웹 메모 위젯 인스턴스 하나(전용 테이블·마이그레이션 0, 웹 위젯 코드 무변경) + 폰에서 수정·추가 가능. ① **브리지 2라우트**: GET(그 사용자의 메모 위젯 전부, 최근 수정 순·ETag/304 — 작업과 달리 '모바일 표시' 지정이 없다, 목록이 목적이라 한 건만 고르면 성립하지 않음) / POST(=기본 보드 **맨 아래**에 메모 위젯 생성, layout gv:2) / [id] PATCH(+POST 별칭, 자바가 PATCH를 못 보냄). ② **불변식 2개를 순수 모듈 `lib/api/widgetMemo.ts`로 고정**(테스트 8): ⓐ **잠긴 메모(pwHash)는 본문 미전송**(화면 잠금을 폰이 우회하면 잠금이 무의미) + 수정은 **423 거부**, ⓑ **config 통째 쓰기 금지** — 폰은 제목·본문만 아니므로 서버가 현재 config에 **병합**해 색·글자크기·글자색·자동잠금을 보존한다. ③ **되덮기 경합 완화**(위험을 알린 뒤 사용자가 이 조합을 선택): pb_widgets는 realtime 발행 대상이 아니라(pb_tasks·pb_clipboard만) 웹이 폰의 수정을 모른 채 디바운스 저장으로 되덮을 수 있다 → pb_widgets를 realtime에 넣는 대신(캔버스 드래그마다 갱신 + config에 MB급 dataURL이 있어 replica identity full의 WAL 비용이 과다) **창 포커스 복귀 시 자기 행 재조회**(`useMemoRemoteSync` — 클립보드·작업의 포커스 재조회 선례). 입력 중(activeElement가 입력 요소)에는 건너뛴다. 남는 틈(웹·폰 동시 편집 시 마지막 저장 승)은 주석에 명시. ④ **안드로이드**: 제목만 나열하는 `MemosWidget`(작업 위젯 v15 교훈 그대로 — refreshTick 재구성으로 stale 렌더 차단·항목별 고유 data URI로 PendingIntent 병합 차단·clickable 먼저 padding 나중·행 자식 구조 고정) + 제목 탭 → `MemoEditActivity`(내용 보기+수정 한 화면, 잠긴 메모는 안내만) + ＋ 새 메모. AgendaSyncWorker가 같은 15분 주기로 함께 동기화하고 cancelIfNoWidgets는 **세 위젯이 모두 사라질 때만** 멈춘다. 글자는 직전 +2sp 규약대로(제목 15sp). **삭제는 두지 않았다** — 메모 한 건이 캔버스의 위젯이라 폰의 오조작 한 번이 웹 대시보드에서 위젯을 없앤다. 검증: 실서버 **22/22 PASS**(익명 401·빈 목록·POST가 실제 pb_widgets 위젯 생성·gv:2 레이아웃·ETag 304·병합이 color/size/textColor/lockAfterMin 보존·잠금 본문 미전송+423+원본 보존·제목 없으면 본문 첫 줄·맨 아래 배치·404·400, 임시 계정 잔여 0행), **실브라우저**(웹 입력 → 폰 역할이 브리지로 수정 → 창 포커스 복귀에 헤더 제목·본문 즉시 반영, **입력 중에는 되덮지 않음** 확인, 콘솔 0), tsc 0·lint 0·test **165/165**(신규 8)·build 그린(메모 라우트 2종 등록), APK **v17**(지문 = assetlinks·versionCode 17·병합 매니페스트에 MemosWidgetReceiver·MemoEditActivity 확인). adb 미연결로 기기 설치·실측은 미실시. **잔여(사용자)**: v17 설치 → 홈 화면 '메모' 위젯 추가 → 제목 목록·내용 보기·수정·＋추가 확인. |
| 2026-09-14 | **홈 화면 위젯 본문 글자 +2sp**(작업·오늘 일정) (APK v16) | android/widget/{TasksWidget,AgendaWidget,CommonUi}.kt, android/{app/build.gradle,twa-manifest.json,manifest-checksum.txt} | 요구: 핸드폰 위젯의 작업 등 내용 폰트를 2pt 정도 크게. **본문만 +2sp**, 헤더(제목·필터 3버튼·갱신 시각·목록·＋)는 그대로(요구가 내용 기준). ① 작업: 제목·일자 13→15, 진행/완료·삭제 라벨 10→12, 빈 목록·미연결 안내 13→15·11→13. ② 오늘 일정: 제목 13→15, 시간·대상 줄 11→13, 빈 목록 13→15, 좌측 색 막대 34→38dp(두 줄 높이 증가분에 맞춤). ③ 페어링 안내(CommonUi, 두 위젯 공용) 13→15·11→13. 제목은 maxLines 1이라 긴 제목은 전보다 일찍 말줄임된다. 검증: APK **v16** 빌드, 서명 지문 = assetlinks(v15 위에 업데이트 설치 가능), versionCode 16 확인. AAB jarsigner 단계는 기존대로 실패(사이드로드 무관). adb 미연결로 기기 설치·실측은 미실시. **잔여(사용자)**: v16 설치 후 글자 크기·말줄임 확인. |
| 2026-09-01 | **위젯 무반응의 진짜 원인 2개를 실기기 adb로 확정·수정 — Glance 세션 stale 렌더(틱 재구성) + 투명 창 입력 삼킴(터치 통과·무애니메이션)** (APK v15, 기기 설치·검증 완료) | android/{widget/{TasksWidget,SetTasksFilterActivity},tasks/{TasksListActivity,TaskEditActivity},sync/AgendaSyncWorker,res/values/widget_styles.xml(신규),Manifest} | 사용자 신고(v14): "버튼은 눌러지나 작동 안 함, 입력이 막힌 느낌". **처음으로 실기기 디버깅**(사용자 Z Fold6, USB adb — 무선 페어링 중 USB 승인됨): 단발 탭은 완벽 동작(탭→0.3~0.6초 updateAppWidget 적용, 콜드 스타트 포함)함을 로그로 확인 → 스크립트로 증상 재현에 성공해 원인 2개 확정. ① **Glance 세션 stale 렌더(주범)**: 세션은 렌더 후 수십 초 살아 있고, 살아있는 세션에 대한 updateAll은 **provideGlance 시점에 캡처된 옛 값으로 재구성** — 직전 렌더 후 ~1분 내 탭이 전부 "무반응"으로 보였다(내 성공 테스트는 우연히 매번 1~2분 간격 = 세션 사망 후). v5~v11 인플레이스 버튼 실패의 상당 부분도 이것(연속 테스트 탭은 항상 세션 생존 창 안). 수정: `TasksWidget.refreshTick`(MutableStateFlow) + 컴포지션 안 `remember(tick){ TasksUi(ctx) }`로 **모든 reads를 틱마다 재로드**, 전 갱신 경로(SetTasksFilter·목록 onPause·수정·삭제·15분 동기화)를 `TasksWidget.refresh()`(틱++ → updateAll)로 통일 — 산 세션은 틱이, 죽은 세션은 updateAll이 커버. ② **입력 삼킴**: 연속 3탭 중 2번째가 투명 액티비티(+열림·닫힘 전환 동안 런처 입력 차단)에 먹히는 것을 재현 — "입력이 막힌" 느낌의 실체. 수정: 창에 `FLAG_NOT_TOUCHABLE|NOT_FOCUSABLE`(터치 아래로 통과) + 신규 `PbInvisible` 테마(`windowAnimationStyle=@null`·`windowDisablePreview`)로 전환 시간 자체를 제거. 부수: updateAll은 갱신 '접수' 후 반환하고 실제 적용은 finish 후에 온다는 것도 실측(v9의 runBlocking 가설 교정 — 액티비티의 역할은 완료 대기가 아니라 포그라운드 크레딧). **검증(실기기, adb)**: 단일 탭 195ms 적용, 세션 생존 중(4.5초 간격) 탭 30~170ms 적용·화면 정확, **빠른 3연타 3/3 등록**(v14는 2/3)·최종 상태=마지막 탭, 완료=완료만·진행=진행만(유예 제거 확인). APK **v15**(지문 동일·versionCode 15)를 **adb로 사용자 기기에 설치 완료**. **잔여(사용자)**: 평소처럼 써보고 체감 확인만. |
| 2026-09-01 | **필터 3버튼 = 위젯 내 직접 전환 복원(투명 포그라운드 액티비티) + 완료 유예 제거** (APK v14) | android/{widget/{SetTasksFilterActivity(신규),TasksWidget},data/WidgetStore,tasks/TaskEditActivity,Manifest} | 사용자 요구 2건. ① **진행·완료·전체를 위젯 버튼으로 재확인 → 근본 원인 재판정**: v5~v11 인플레이스 버튼 실패는 탭 유실이 아니었다 — v9에서 **prefs 저장은 실측 확인**됐으므로 탭은 배달됐고, 유실된 것은 **재렌더**다. Glance updateAll은 WorkManager 세션을 경유하는데 비포그라운드 프로세스(NoDisplay 트램펄린·브로드캐스트 콜백)에선 이 기기가 실행을 미루고, 다음 자연 렌더(15분 동기화) 땐 **10분 자동 복귀가 이미 필터를 되돌린 뒤**라 사용자에겐 "진행중 고정"으로 보였다(v11의 BAL 가설은 v9의 prefs 저장 관측과 모순 — 기각. Glance 리시버·트램펄린의 병합 매니페스트 존재도 확인). 동작 증명된 v12·v13 경로(보이는 화면+updateAll)와의 유일한 차이 = **포그라운드 여부** → 신규 `SetTasksFilterActivity`(**Theme.Translucent — 보이는 UI 없음**)가 포그라운드를 잡은 채 필터 저장 → 백그라운드 스레드 runBlocking updateAll(메인 차단 ANR 회피) → 종료(+3초 백스톱 finish — 투명 창이 홈 터치를 막는 잔류 방지). 값별 고유 data URI `pbfilter://{value}`로 PendingIntent 병합 여지 0(v10 교훈). '목록' 버튼(관리 화면)은 유지 — 이 방식이 실기기에서 또 실패해도 관리 경로는 남는다. ② **완료 유예 제거(요구)**: 완료된 작업은 '진행' 필터에서 **즉시** 사라지고 완료·전체에서만 표시 — 위젯 필터에서 grace 제외 삭제, markGrace/graceIds/K_TASKS_GRACE 전부 삭제, TaskEditActivity 완료 전환 시 유예 마크 제거. 웹 TasksBody는 이미 `!t.done` 즉시 반영(realtime)이라 무변경. ③ 부수 수리: v10에서 호출측이 삭제돼 **고아였던 scheduleFilterRevert를 setTasksFilter에 재배선**(완료·전체 → 10분 뒤 재렌더 예약, 진행 → 예약 취소) — 모든 호출처(위젯 버튼·관리 화면)가 자동 커버. 검증: APK **v14**(지문 동일·versionCode 14), 병합 매니페스트+classes2.dex에 SetTasksFilterActivity 존재 확인. AAB용 jarsigner 단계만 실패(사이드로드 배포라 무관 — APK 서명은 apksigner로 검증됨). **잔여(사용자)**: 실기기 v14 — 필터 탭 시 화면 전환 없이 위젯 목록 즉시 전환, 수정 화면에서 완료 저장 시 진행 목록에서 즉시 사라짐, 10분 뒤 진행 자동 복귀 확인. |
| 2026-08-29 | **위젯 헤더에 필터 버튼 복원 — 단 '그 필터로 관리 화면 열기'로 구현** (APK v13) | android/{widget/TasksWidget.kt,tasks/TasksListActivity.kt} | v12에서 위젯 헤더를 현재 상태 텍스트만 남겼더니 사용자가 "완료·전체가 없다, 옆에 버튼을 달라"고 요구. 위젯 내부에서 값만 바꾸는 방식은 이 기기에서 여전히 불가하므로 **버튼은 되돌리되 동작을 화면 열기로** 구현: [진행][완료][전체] 각각이 `pbtask://manage/{value}`(필터별 고유 data URI)로 TasksListActivity를 열고, 화면이 `filter` extra를 읽어 그 필터로 시작 + WidgetStore에 저장 → 뒤로 나오면 위젯 표시도 그 필터로 바뀐다(선택된 버튼 강조·굵게). 즉 **탭 1회로 원하는 필터의 목록을 보고, 위젯도 따라온다**. 중복이던 '목록 열기'는 '목록'(현재 필터 유지)으로 축약. APK **v13**(지문 동일·versionCode 13). |
| 2026-08-29 | **작업 조작을 네이티브 '작업 관리' 화면으로 통일**(위젯 = 보기 + 탭하면 열기) (APK v12) | android/tasks/TasksListActivity.kt(신규), android/widget/TasksWidget.kt, AndroidManifest | v5~v11에서 위젯 내 인플레이스 버튼을 **6가지 방식**(콜백·트램펄린·전용 클래스·고유 data URI·구조 고정·minify off)으로 고쳤지만 이 기기에선 전부 무반응. **확실한 사실 하나**만 남았다: 화면을 여는 탭(＋·행 탭 → TaskEditActivity)은 **항상 동작한다**. → 추측 반복을 멈추고 설계를 바꿈: 위젯은 **표시 + '목록 열기'/행 탭**만 하고, 필터(진행·완료·전체)·완료 토글·삭제(2탭 확인)·추가·수정은 새 `TasksListActivity`(네이티브 목록 화면)에서 수행한다. 낙관 반영 후 API 호출, 화면 진입 시 서버 재조회(삭제 예정 마크도 이때 실행), onPause에서 위젯 재렌더. 위젯의 ✕는 `›`(수정 화면 열기)로 대체, 헤더는 현재 보기 상태만 표시. MS To Do류의 '위젯=요약, 조작=앱 화면' 구성과 동일. 검증: APK **v12**(지문 동일·versionCode 12·TasksListActivity 병합). mergeDex 파일 잠금(백신) 재발 → 재시도로 성공. |
| 2026-08-29 | **위젯 버튼을 Glance 표준 콜백으로 환원**(트램펄린 액티비티 폐기) — 필터 3버튼·✕·지금 갱신 (APK v11) | android/{TasksWidget,Manifest,widget/{SetFilter,SyncNow,WidgetAction}Activity 삭제} | v7~v10 내내 필터·✕가 죽은 이유를 **증거로 재판단**: 작동하는 것(＋·행 탭)은 전부 **보이는 화면**(TaskEditActivity), 죽는 것은 전부 **Theme.NoDisplay 트램펄린** — targetSdk 36의 **백그라운드 액티비티 실행 제한**에 막혀 조용히 무시된 것으로 결론(무반응·로그 없음과 일치). 게다가 v5에서 ✕가 실제로 한 번 동작했던 유일한 방식이 **Glance 표준 콜백**이었다. → 트램펄린 3종 삭제하고 `actionRunCallback`으로 환원: **필터는 값마다 전용 콜백 클래스**(FilterPending/Done/All — 파라미터 없이 구분해 병합 여지 0), ✕는 ToggleDeleteMarkAction+taskId(Glance가 목록 fill-in으로 안전 전달), 지금 갱신은 SyncNowAction. 콜백은 suspend라 `updateAll`을 끝까지 await(트램펄린의 갱신 유실 없음). v6·v7의 보강(행 구조 고정+상태 비트 itemId, minifyEnabled false)은 유지 — 콜백 리플렉션이 R8에 깨지지 않는 게 이 방식엔 필수. 터치 영역도 8~12dp로 확대. 검증: APK **v11**(지문 동일·versionCode 11), dex에서 콜백 5종(FilterPending/Done/All·ToggleDeleteMark·SyncNow) 존재 확인. 빌드 중 mergeDex 파일 잠금 2회(백신) → 데몬 정지+dex 중간산출물 삭제 후 성공. |
| 2026-08-29 | **필터 = 진행·완료·전체 3버튼 + 항목별 고유 인텐트(PendingIntent 병합 근절)** — 웹도 동일 (APK v10) | android/{SetFilterActivity(신규·CycleFilterActivity 삭제),TasksWidget,Manifest}, components/widgets/tasks/TasksBody | 사용자 요구: 순환 콤보 대신 **3버튼 나열**(기본 진행) + ✕ 재탭 해제, PC도 동일. ① **근본 원인 완결**: v7~v9에서 헤더는 전용 클래스로 갈랐지만 **목록의 ✕들은 여전히 같은 클래스+extras만 달라** filterEquals(=extras 무시)로 서로 병합 — 특정 행만 반응하거나 재탭이 죽던 잔여 증상의 실체. Glance의 **Intent 오버로드**(`androidx.glance.appwidget.action.actionStartActivity(Intent)`)로 전환해 **항목·값마다 고유 data URI**(`pbtask://delete/{id}`·`pbtask://edit/{id}`·`pbfilter://{value}`·`pbwidget://sync`) 부여 → 모든 클릭이 구조적으로 분리된다. ② **필터 3버튼**(진행·완료·전체, 선택된 것 강조·굵게): 순환 상태 계산 자체를 제거, SetFilterActivity 하나가 data URI host로 값을 읽는다(10분 자동 복귀는 유지). ③ **웹 동일화**: select → 3버튼 세그먼트, 행에 ✕ 마크 토글('삭제' 빨간 라벨+흐림, 재탭 취소) + 상단 'N개 삭제 실행'(웹은 realtime이라 '다음 갱신'이 없어 확정 버튼으로 대체). 검증: tsc 0·신규 lint 0, APK **v10**(지문 동일·versionCode 10·SetFilterActivity 병합). 첫 빌드에서 헤더 ＋·지금 갱신이 클래스 오버로드를 쓰고 있어 컴파일 오류 → 전부 Intent 방식으로 통일 후 성공. |
| 2026-08-29 | **위젯 액션 갱신 유실·앱 전면화 수정 + 웹 위젯 기능 동등화** (APK v9) | android/{CycleFilterActivity,WidgetActionActivity,TaskEditActivity,Manifest}, components/widgets/tasks/{TasksBody(재작성),useTasks} | 사용자 신고 3건(콤보 여전히 고정·✕ 표시 안 됨·**위젯 누르면 앱이 열림**) + 요구(웹도 동일 기능). 세 번째 신고가 결정적 단서였다. ① **원인 A(갱신 유실)**: 트램펄린이 `CoroutineScope(...).launch { updateAll() }` 직후 `finish()` — 위젯 다시 그리기가 **완료 전에 취소**돼 prefs에는 저장됐지만 화면은 그대로(= 콤보 고정·✕ 무표시의 실체). `runBlocking { updateAll() }`로 완료 보장 후 종료(TaskEditActivity의 수정·삭제 경로도 동일 수정). ② **원인 B(앱 전면화)**: 트램펄린이 TWA와 같은 태스크에 들어가 앱 태스크를 앞으로 끌어올림 → 액션·수정 액티비티에 **`taskAffinity=":widgetaction"`** 부여해 분리(앱 실행 여부와 무관하게 위젯만 동작). ③ **웹 동등화**: TasksBody 재작성 — 진행중(기본)/완료/전체 **select 콤보**, 체크박스 → **진행/완료 표시 라벨**, **행 클릭 = 인라인 수정 폼**(제목·일자·상태 + 저장/취소/2단계 삭제), 완료 흐림+취소선, 일자 본문 크기+요일. useTasks에 `update(id,{title,done,dueOn})` 추가. 완료 유예는 폴링 보정 장치라 realtime인 웹엔 미적용(즉시 반영). 검증: tsc 0·test 76/76·신규 lint 0, APK **v9**(지문 동일·versionCode 9·taskAffinity 병합 확인). **잔여(사용자)**: 실기기 v9 — 콤보 순환·✕ 표시 토글·앱 미실행 확인. |
| 2026-08-29 | **작업 위젯 필터 콤보 수정** — 헤더 버튼별 전용 트램펄린 클래스 + 10분 자동 진행중 복귀 (APK v8) | android/{CycleFilterActivity·SyncNowActivity(신규),WidgetActionActivity(✕ 전용 축소),TasksWidget,WidgetStore,AgendaSyncWorker,Manifest} | 사용자 신고: v7에서도 필터 콤보가 한 값에 고정. **근본 원인 특정**: v7 트램펄린이 콤보·지금 갱신을 **같은 액티비티 + extras만 다르게** 걸었는데, 안드로이드 PendingIntent는 filterEquals(extras 무시) 기준으로 캐시를 공유해 **두 버튼이 하나로 병합** — 콤보 탭이 '지금 갱신'의 extras로 배달됨(마지막 생성이 승자). ① **헤더 버튼 = 버튼마다 전용 클래스**(CycleFilterActivity·SyncNowActivity, extras 0개)로 분리 — 구조적으로 충돌 불가. WidgetActionActivity는 ✕(목록 행, taskId는 fill-in 인텐트가 항목별 전달이라 안전) 전용으로 축소. ② **10분 자동 복귀**(요구): setTasksFilter가 켠 시각 저장, tasksFilter()가 렌더 시 만료 판정(외출옷 REVERT_MS 패턴) + 필터 변경 시 10분 지연 OneTimeWork(제약 없음)로 재렌더 예약 — 위젯이 자기 힘으로 10분 뒤 진행중에 돌아온다. APK v8(지문 동일·versionCode 8·신규 액티비티 2종 병합 확인). **잔여(사용자)**: 실기기 v8 — 콤보 순환·10분 복귀·지금 갱신 분리 동작 확인. |
| 2026-08-29 | **작업 위젯 탭 액션 전면 트램펄린화** + 수정 화면 삭제·취소 + 요일 날짜 + 지금 갱신 (APK v7) | android/{WidgetActionActivity(신규),TasksWidget,TaskEditActivity,app/build.gradle}, components/widgets/tasks/{dateLabel,TasksBody}, lib/widgets/tasks/dateLabel.test.ts | 사용자 신고: 필터 콤보박스·✕(v6 수정 후에도) 여전히 오작동. **결정적 패턴**: 안 되는 것은 전부 actionRunCallback(브로드캐스트), 되는 것(＋·행 탭)은 전부 actionStartActivity — 이 기기에서 콜백 브로드캐스트 경로가 불안정. ① **모든 위젯 탭을 트램펄린 액티비티로 통일**: `WidgetActionActivity`(Theme.NoDisplay, onCreate에서 처리 후 즉시 finish) — cycleFilter·toggleDeleteMark(+taskId)·syncNow를 extras로 분기. ActionCallback 클래스 2종 삭제. ② **R8도 용의선상**: 콜백은 리플렉션 해석이라 keep 규칙 없는 minify가 조용히 죽일 수 있음 → `minifyEnabled false`(사이드로드라 크기 무의미, APK 2.3→6.3MB). ③ **수정 화면**: [저장|취소] 나란히 + '이 작업 삭제'(빨강, 수정 모드만 — 즉시 API 삭제+캐시 반영). ④ **날짜**: 본문과 같은 13sp + 요일 병기 `9.15 (화)`/`15일 (수)`/`2027.1.5 (화)` — 웹 dateLabel.ts(테스트 6케이스 갱신)·Kotlin 미러 동시 수정. ⑤ **지금 갱신** 버튼(헤더, 갱신 시각 옆) — syncNow 즉시 실행(삭제 예정·완료 유예도 이때 반영) + "갱신 중…" 토스트. 부수 확인: bubblewrap build가 버전 필드만 자동 동기화하고 위젯 통합은 보존함(구조 3종 매니페스트 잔존 확인). tsc 0·test 76/76·APK **v7**(지문 동일·versionCode 7·트램펄린 병합 확인). **잔여(사용자)**: 실기기 v7 — 필터 순환·✕ 토글·지금 갱신·수정 화면 삭제/취소·날짜 요일 확인. |
| 2026-08-29 | **작업 위젯 ✕ 재탭 무반응 수정** — 행 구조 고정 + 상태 비트 itemId (APK v6) | android/widget/TasksWidget.kt | 사용자 신고: ✕ 1탭으로 '삭제' 표시는 되는데 **재탭이 안 먹혀** 해제 불가. 유력 원인: 마크 시 '삭제' 라벨이 **새 자식 뷰로 삽입**되며 행 구조가 바뀌어, 같은 itemId의 재활용 뷰에서 클릭 바인딩이 낡는 RemoteViews 목록 특성. 이중 방어: ① **행 자식 구조를 상태와 무관하게 고정** — 일자·'삭제' 라벨 Text를 상시 렌더(없으면 빈 문자열, Spacer도 무조건), 상태는 속성(색·취소선)으로만 표현. ② **itemId에 마크 비트 포함**(`hash shl 1 + marked`) — 상태가 바뀌면 다른 항목으로 취급되어 완전 재바인딩(클릭 인텐트 재생성). APK v6(지문 동일·versionCode 6). **잔여(사용자)**: 실기기 v6에서 ✕ 토글 왕복 확인. |
| 2026-08-29 | **작업 위젯 ✕ = '삭제 예정' 토글**(즉시 삭제 → 마크 후 다음 갱신 때 삭제) + 위젯 터치 영역 수정 (APK v5) | android/{TasksWidget,WidgetStore,AgendaSyncWorker,AgendaTheme,TaskEditActivity} | 사용자 신고: 위젯 ✕를 눌러도 반응이 안 보임 + 요구: ✕ 1탭=행에 **'삭제' 표시**(삭제 예정), 재탭=표시 해제(유지). ① **원인**: Glance modifier 순서 — `padding().clickable()`은 터치 영역이 패딩 **안쪽**(글자 크기 ≈13sp)으로 좁아져 탭이 빗나감. `clickable().padding()`으로 뒤집어 ✕·필터·＋·제목 전부 터치 영역 확대. ② **✕ 재설계**: 즉시 삭제(낙관 캐시+API) → **삭제 예정 마크 토글**(prefs Set, 서버 호출 0 — 탭 즉시 '삭제' 빨간 라벨+제목 흐림으로 피드백, 재탭 취소). 실제 삭제는 다음 동기화에서 AgendaSyncWorker가 마크된 id를 DELETE(성공한 것만 마크 해제 — 오프라인이면 다음 주기 재시도) 후 fetch로 목록에서 사라짐. 완료 유예(grace)와 같은 부류의 '다음 갱신 때 반영' 패턴. ③ 수정 화면 저장 시 삭제 마크 해제(편집=유지 의사 — 마크된 항목을 고쳤는데 다음 갱신에 사라지는 배신 방지). AgendaTheme에 danger(라이트 #DC2626/다크 #F87171) 추가. **APK v5**(지문 동일·versionCode 5). **잔여(사용자)**: 실기기 v5 설치 → ✕ 토글·다음 갱신 삭제 확인. |
| 2026-08-29 | **작업 위젯 개선** — 모바일 수정 화면·필터·상태 라벨·완료 유예 + 일자(due_on) (APK v4) | supabase/migrations/20260829120001_pb_tasks_due_on.sql(신규), output/{schema.sql,types/database.ts,api-shapes.ts}, app/api/widget/tasks/{route,[id]/route}.ts, components/widgets/tasks/{dateLabel(신규),TasksBody,useTasks}, lib/widgets/tasks/dateLabel.test.ts(신규), android/(TaskEditActivity 신규·TaskAddActivity 삭제·TasksWidget 재작성·Store/Api/TaskItem 확장·PairingActivity·Manifest) | 사용자 요구 6건. ① **입력 화면 겹침 수정**: targetSdk 36 edge-to-edge에서 액션바 제목("작업 추가")이 입력 위로 겹침 → `actionBar?.hide()`+`fitsSystemWindows`+`adjustResize`(Pairing에도 동일 적용). ② **일자(due_on date)**: add-only 마이그레이션, 추가·수정 양쪽(웹 QuickAdd date input·모바일 DatePickerDialog) 입력, **표시 규칙**(다른 연도 `2027.1.5`·같은 연도 `9.15`·같은 연월 `15일`)은 웹 `dateLabel.ts`(테스트 4종) + Kotlin 미러 한 쌍. ③ **모바일 필터**: 위젯엔 드롭다운이 없어 콤보박스를 **순환 버튼**(`진행중 ▾` 탭 → 완료 → 전체, 기본 진행중, prefs 저장)으로. ④ **체크박스 → 진행/완료 표시 라벨**(조작 불가, 완료=흐림+취소선), ✕ 삭제 유지. ⑤ **행 탭 = TaskEditActivity**(추가·수정 통합, extras는 Glance actionParametersOf가 인텐트 extra로 전달) — 제목·일자·진행/완료 라디오(수정 모드만), 수정 저장은 `PATCH{title,done,dueOn}` 확장(POST 별칭 유지, dueOn null=일자 제거) + 낙관 캐시. ⑥ **완료 유예**(요구 문자 그대로): 수정 화면에서 완료 전환 시 `markGrace`(prefs Set) — 진행중 필터가 유예 항목을 흐린 채 유지하고, **다음 동기화**(putTasks가 유예 비움) 때 진행중에서 빠짐. 수정 직후 syncNow를 부르지 않는 이유도 이것(부르면 유예가 즉시 소거). 검증: tsc 0·test **76/76**(신규 4)·신규 lint 0·build 그린, assembleDebug·서명 **APK v4**(지문 동일·versionCode 4·TaskEditActivity 병합 확인). **라이브 검증**(사용자가 SQL Editor 적용 후): `verify-tasks-api.mjs` **13/13 PASS**(dueOn 저장·POST 별칭 수정·일자 제거·형식 오류 400 포함). 첫 실행에서 검증 스크립트의 "지정 해제→409/null" 가정이 **사용자 실위젯**(mobileSync 켠 상태)과 충돌해 2건 FAIL + 실목록에 "x" 1행 오염 → 오염 행 즉시 삭제하고 스크립트를 실데이터 안전형으로 수정(쓰기 프로브 제거, "임시 위젯이 지정 대상 아님"만 검증 — 해제 시 실위젯으로 정상 복귀 확인). 실브라우저는 이번엔 미실시(playwright MCP 미연결 — 날짜 라벨은 테스트 4종+API로 고정, UI 경로는 직전 E2E와 동일). **잔여(사용자)**: 실기기 v4 설치 → 겹침 해소·수정 화면·필터·완료 유예·일자 확인. |
| 2026-08-28 | **작업(tasks) 위젯** — 웹 캔버스 ↔ 안드로이드 홈 화면 위젯 실시간 양방향 동기화 (APK v3) | supabase/migrations/20260828120002_pb_tasks.sql(신규), output/{schema.sql,types/database.ts,api-shapes.ts,widget-manifest.md}, lib/api/widgetCore(+test), app/api/widget/tasks/{route,[id]/route}.ts(신규), components/widgets/tasks/*(신규 7)+registry, android/(TasksWidget·TasksWidgetReceiver·TaskAddActivity·CommonUi·TaskItem·Store/Api/Worker 확장·res 3), _workspace/verify-tasks-api.mjs(신규) | 요구: '작업' 위젯을 웹·모바일 양쪽에 만들어 한쪽에서 입력하면 서로 나타나고, 웹 위젯엔 모바일 표시 체크, 모바일 위젯은 스크롤·추가·삭제. ① **저장소 = `pb_tasks` 테이블**(config 아님): 웹(RLS 직접 CRUD)과 안드로이드(service-role 브리지)가 **동시에 쓰므로** config 통저장은 디바운스 되덮기 경합 — 행 단위 + **realtime**(replica identity full, pb_clipboard 패턴)으로 충돌 없이 즉시 상호 반영. ② **모바일 표시 지정**: config `mobileSync`+`mobileSyncAt`(켠 시각)만으로 서버가 해석(`pickTasksInstance`, 테스트 고정) — 노트 shareTarget식 교차 인스턴스 배선 없이 "여럿이면 마지막에 켠 쪽"(UI에 명시). 토글은 메모 비번 선례대로 **즉시 영속**. ③ **브리지 4엔드포인트**: GET/POST `/api/widget/tasks`(지정 인스턴스 해석·ETag/304·지정 없으면 GET instanceId null / POST **409 no_target**+안내), PATCH/DELETE `/tasks/[id]`. **`POST=PATCH` 별칭** — 안드로이드 HttpURLConnection이 PATCH를 못 보내는 자바 한계 우회(OkHttp 미도입 유지). ④ **웹 위젯**: TasksBody 공유(타일=전체보기) — 체크(취소선)·2단계 삭제·QuickAdd·모바일 표시 배지, useTasks 훅(realtime+포커스 재조회, 훅별 유니크 채널). ⑤ **안드로이드 위젯**: LazyColumn 스크롤 + Glance CheckBox(완료 토글)·✕ 삭제 — **낙관 캐시 반영**(mutateTasks가 ETag도 지워 API 실패 시 다음 동기화가 서버 진실로 복원) 후 API 호출, ＋는 TaskAddActivity(위젯은 텍스트 입력 불가 — 계획서 §0.3 원칙, 저장 즉시 웹에 realtime 반영). 동기화는 AgendaSyncWorker가 아젠다와 함께 15분/즉시 처리, 위젯 리시버 취소는 **두 위젯 모두 제거 시에만**(cancelIfNoWidgets — 한쪽 제거가 다른 쪽 동기화를 끊는 비대칭 방지). 미지정 상태 위젯은 "웹에서 '모바일 홈 화면에 표시'를 켜세요" 안내. ⑥ 검증: tsc 0·test **72/72**(신규 1)·신규 lint 0·build 그린(tasks 라우트 2종 등록), assembleDebug 그린, **APK v3**(2.27MB) apksigner 지문 동일(업데이트 설치)·versionCode 3·TasksWidgetReceiver/TaskAddActivity 병합 확인. **라이브 검증**(사용자가 SQL Editor 적용 후): `verify-tasks-api.mjs` **12/12 PASS**(DDL→지정 인스턴스 해석→304→모바일 POST 201→웹 추가분 GET 포함→PATCH done→DELETE→지정 해제 시 409/instanceId null, 임시 데이터 정리) + **POST 별칭 라우팅 401 확인**(405 아님). **실브라우저 E2E**(승인 테스트 계정, 검증 후 계정·데이터 삭제 — 4테이블 잔여 0행): 팔레트 더블클릭으로 '작업' 추가(데스크톱은 더블클릭 규칙)→타일 QuickAdd "웹에서 입력한 작업"→속성 토글 켜고 **'저장' 없이 ✕로 닫아도** DB `mobileSync:true` 즉시 영속→모바일 역할(브리지 pair+POST)로 추가하자 **리로드 없이 realtime으로 타일에 즉시 표시**+'모바일 홈 화면에 표시 중' 배지→웹 체크 토글·2단계 삭제가 DB에 정확 반영(done:true·행 삭제), 콘솔 에러 0. **잔여(사용자)**: 실기기 v3 설치 → 홈 화면 '작업' 위젯 추가 → 양방향 확인. |
| 2026-08-28 | **홈 화면 위젯 본체(P3)** — Jetpack Glance 읽기 전용 위젯 + 페어링 + 15분 동기화 (APK v2) | android/app/src/main/java/com/tkleen/schedule/{widget/{AgendaWidget,AgendaWidgetReceiver,AgendaTheme},data/{WidgetApi,WidgetStore,model/AgendaItem},pairing/PairingActivity,sync/AgendaSyncWorker}.kt(신규 8), android/app/src/main/res/{xml/agenda_widget_info,layout/agenda_widget_preview,values/widget_strings}.xml(신규 3), android/{build.gradle,app/build.gradle,gradle.properties,twa-manifest.json,AndroidManifest.xml}, docs/release.md | PLAN-android-widget.md P3(계획서 파일 구조 그대로, 단 의존성·저장소는 축소 채택을 문서화). ① **구성**: Kotlin 2.0.21+Compose 컴파일러(루트 classpath, plugins id로 해석)+Glance 1.1.1+work-runtime-ktx 2.9.1. HTTP·JSON은 **플랫폼 내장**(HttpURLConnection·org.json — okhttp/kotlinx-serialization 미도입), 캐시는 DataStore 대신 **SharedPreferences**(값 4개 동기 읽기), 토큰은 계획서 대안대로 **Android Keystore AES/GCM 직접 암호화**(security-crypto 지원 종료). minSdk 21→**26**(Glance 23+·Keystore·java.time). ② **동작**: 위젯 추가→APPWIDGET_CONFIGURE로 PairingActivity(6자리 코드→`/api/widget/pair`→토큰 저장, 이미 연결이면 즉시 완료)→AgendaSyncWorker(**15분 주기**+즉시 1회, CONNECTED 제약, ETag 304 시 시각만 갱신, 실패 시 **캐시 유지**·재시도≤3, 401은 unauthorized 마크→위젯이 '다시 연결' 안내)→AgendaWidget(LazyColumn: 대상색 인디케이터 바·`오후 3:30`/`내일`/`M/d` 시간 라벨(연기는 연기 시각 자리)·done 취소선·빈 목록 "오늘 남은 일정이 없습니다"·헤더 날짜+마지막 갱신 시각·헤더 탭=앱 열기). 브랜드: #4A90C2 강조, 라이트 #FFF/다크 #16191C(day/night ColorProvider). ③ **삽질 5건 기록**(docs/release.md에 재발 방지): ⓐ bubblewrap 자동설치 JDK가 **32비트**→gradle 데몬 힙 확보 실패("Could not reserve 1572864KB")→Temurin **x64** 17.0.20.1 설치·config 교체, ⓑ 한국어 Windows CP949→`-Dfile.encoding=UTF-8`(gradle+kotlin.daemon jvmargs), ⓒ **Kotlin 블록 주석 중첩** — KDoc 속 `/api/widget/*` 문자열의 `/*`가 주석을 다시 열어 EOF까지 "Unclosed comment"(진짜 원인, ⓑ는 예방 차원 유지), ⓓ `updateAll`은 확장함수 import·reified `actionStartActivity<T>()`는 `androidx.glance.action` 패키지, ⓔ twa-manifest 수정 후 build가 **대화형 "적용할까요?" 프롬프트**에 무한 대기 — Y는 위젯 통합을 덮어쓰므로 **manifest-checksum.txt SHA1 수동 동기화**로 프롬프트 제거(이후 `bubblewrap update` 금지 규칙 문서화). ④ **검증**: assembleDebug 그린 → 서명 릴리스 APK v2(2.25MB) — apksigner 지문 **assetlinks와 동일**(v1 위에 업데이트 설치 가능)·versionCode 2·병합 매니페스트에 AgendaWidgetReceiver/PairingActivity 포함 확인. **잔여(사용자, P3 DoD)**: 실기기에서 APK v2 설치→위젯 추가→페어링→일정 표시·15분 갱신 확인(P2 DoD 주소창 미표시 확인도 대기 중). P4(완료/연기 액션·새로고침·딥링크)는 미착수. |
| 2026-08-28 | **안드로이드 앱 셸(P2)** — TWA(Bubblewrap) 생성·서명·APK 빌드 + PNG 아이콘 + assetlinks | lib/brandIcon.tsx(신규), app/{icon-192.png,icon-512.png,icon-maskable-512.png}(신규 라우트 3)·apple-icon(공유화)·manifest.ts, public/.well-known/assetlinks.json(신규), android/(신규 TWA 프로젝트), docs/release.md(신규), .gitignore | PLAN-android-widget.md P2. ① **PNG 아이콘**(P0 발견 선결): apple-icon의 마크 드로잉을 `lib/brandIcon`으로 추출해 192/512/maskable-512 정적 라우트 공유 — 마크가 중앙 ≈51%라 maskable 안전존 충족, PNG 바이트·크기 실측 검증 후 배포(라이브 manifest 200 확인). ② **TWA**: init의 inquirer 프롬프트가 파이프 stdin에서 첫 줄만 소비하는 문제 → **`android/twa-manifest.json` 직접 작성 + `bubblewrap update --skipVersionUpgrade`**(비대화형)로 우회. 패키지 `com.tkleen.schedule`, launcherName "Dashboard"(12자 제한), fallback customtabs 유지(계획서 §4), 웹 manifest의 share_target도 TWA에 승계. ③ **서명**: keytool 비대화형 키스토어(RSA 2048/10000일) — `android.keystore`·`keystore.properties`는 **gitignore**(예시 파일만 커밋), SHA-256 지문 `5E:CF:C1:…:9D:99`. ④ **assetlinks.json** 배포·라이브 200 확인(지문·패키지 일치). ⑤ **빌드 환경**: JDK 17·Android SDK를 bubblewrap 자동설치+수동 보완(`~/.bubblewrap/`, cmdline-tools 6609375 직접 다운로드 — SDK 설치 프롬프트도 같은 stdin 문제, build-tools 36.1.0은 `yes|sdkmanager`로 라이선스 동의). **함정 2건 기록**: ⓐ 이 셸은 `NoDefaultCurrentDirectoryInExePath=1`이라 bubblewrap의 `gradlew.bat` 호출이 실패 → `env -u …` 해제 필요, ⓑ 백신 임시파일 잠금으로 gradle 의존성 다운로드가 간헐 실패(시도마다 캐시 누적) → 재시도 루프로 5회차 성공. ⑥ **산출**: `android/app-release-signed.apk`(1.05MB) — apksigner 검증으로 **서명 지문 = assetlinks 지문** 일치·패키지/버전(v1)/라벨("모두의 Dashboard") 확인. 재빌드·버전·백업 규칙은 docs/release.md. **잔여(사용자)**: 실기기 설치 → 주소창 미표시 확인(P2 DoD). P3(Glance 위젯 UI)~는 미착수. |
| 2026-08-28 | **안드로이드 홈 화면 위젯 백엔드(P0+P1)** — 디바이스 토큰 페어링 + 위젯 아젠다 API + 설정>위젯 탭 | supabase/migrations/20260828120001_pb_widget_devices.sql(신규), output/{schema.sql,types/database.ts,api-shapes.ts}, lib/api/{widgetCore,widgetCore.test,widgetDevice}(신규), app/api/widget/{pair,pairing-codes,agenda,appointments/[id]}(신규 라우트 4), components/canvas/SettingsDialog, docs/{schema-before.sql,widget-P0-precheck.md}(신규), _workspace/verify-widget-api.mjs(신규) | `PLAN-android-widget.md`(사용자 제공 계획서) P0·P1 적용 — 네이티브(Jetpack Glance) 위젯이 쓸 서버 표면. 계획서 매핑 결정 3건은 docs/widget-P0-precheck.md §5에 기록: appointments/targets→**pb_circle_***, owner_id→user_id, `/settings/widget` 페이지→**설정 다이얼로그 '위젯' 탭**(저장소의 설정 패턴), `DELETE /api/widget/devices/[id]` 라우트→RLS(select/delete own) 직접 삭제로 대체. ① **스키마(add-only)**: `pb_circle_appointments`에 status(pending/done/snoozed)+completed_at+snooze_until; `pb_widget_devices`(**토큰 원문 미저장 — sha256 해시만**, 본인 select/delete RLS, insert는 service-role 전용)·`pb_widget_pairing_codes`(정책 없음=deny-by-default, pb_members 패턴). ② **라우트 4종**: `POST /api/widget/pairing-codes`(웹 세션, 6자리·5분·1인 1코드), `POST /api/widget/pair`(코드→토큰 교환 — `update … where consumed_at is null` 원자적 소모로 동시 재사용 차단, IP 분당 10회), `GET /api/widget/agenda?days=`(Bearer 디바이스 토큰, **KST 자정 기준 창**·서버 TZ 무관, 대상 이름·색 join, **ETag/If-None-Match 304**로 15분 폴링 비용 절감, 토큰당 분당 20회), `PATCH /api/widget/appointments/[id]`(done/snoozed/pending, 연기 기본=**다음 날 오전 9시 KST**, user_id 스코프라 타인 id는 404). 공용 게이트 `requireDevice`(cards/ingest의 per-user 토큰 패턴, last_seen_at 갱신). 시간·해시·rate limit 순수 로직은 `widgetCore`로 분리해 테스트 5종(KST 창·자정 경계·연기 기본값·슬라이딩 윈도우·코드/토큰 형식) 고정. ③ **설정>위젯 탭**: 코드 발급+남은 시간 카운트다운, 디바이스 목록(마지막 사용 표시)·2단계 폐기(RLS delete=토큰 즉시 무효), 설치 안내. ④ Next 16 `RouteContext` 타입은 typegen 산출물이라 신규 라우트 후 `node node_modules/next/dist/bin/next typegen` 필요(`npx next`는 rtk 훅이 출력을 가로채 미실행처럼 보임 — 직접 바이너리 경로로). 검증: tsc 0·신규 lint 0(기존 WidgetFrame 1건 잔존)·test **71/71**(신규 5)·build 그린(위젯 라우트 4종 등록 확인), dev 실서버 스모크 agenda 무토큰 **401**·pairing-codes 무세션 **401**·pair 형식오류 **400**. **라이브 적용**: supabase CLI 403(다른 계정, 07-11·08-12와 동일) → 사용자가 SQL Editor에서 직접 실행. 적용 후 `_workspace/verify-widget-api.mjs` **13/13 PASS** = 계획서 P1 DoD 증명(DDL 존재→코드 시드→pair 200+토큰→**코드 재사용 400**→agenda 200(임시 일정 포함·ETag)→**If-None-Match 304**→PATCH done(DB completed_at)→snoozed 기본값 **2026-08-29 09:00 KST**→무효 토큰 401→디바이스 행 삭제 후 401, 임시 데이터 전부 삭제). **실브라우저 검증**(승인 테스트 계정, 검증 후 계정·데이터 삭제 — 잔여 0행 확인): 설정에 [앱 표시·위젯·계정] 탭(비관리자라 멤버 탭 미노출), 위젯 탭에서 코드 발급→`321817`+카운트다운 4:55, 그 코드로 curl pair→디바이스 목록에 등록 시각 표시, 토큰으로 agenda 호출 후 '마지막 사용' 갱신(14:57→14:58), 폐기 2단계→목록 비움+같은 토큰 즉시 401, 콘솔 에러 0. P2(안드로이드 셸)~P6은 계획서 §7대로 승인 후 착수. |
| 2026-08-19 | 노트·메모 **링크 실제 연결**(평문 주소 자동 링크 + 새 탭) + 공유받은 웹페이지 식별 | components/widgets/shared/urls(신규), components/widgets/note/{sanitize,prose,RichTextArea,CompactView}, components/widgets/memo/{MemoLinks(신규),CompactView,ExpandedView}, app/share/route, lib/widgets/shared/urls.test(신규) | 요구: 노트·메모에 웹페이지가 보이면 링크가 실제로 연결되고, 특히 **외부에서 공유되어 들어온 경우** 링크로 표시·식별되게. ① **주소 인식 한 곳(`shared/urls.ts`)** — `findUrls`(위치까지)·`urlHost`(라벨). 노트(자동 링크)와 메모(링크 칩)가 같은 규칙을 쓴다. 매칭 문자셋을 **ASCII로 제한**해 `https://naver.com를`의 조사·`…a.com,`의 문장부호가 주소에 딸려가지 않고, 점 없는 호스트(`https://`, `localhost`)는 링크로 보지 않는다. ② **노트 자동 링크**: 살균기(`sanitizeHtml`)에 `linkify` 옵션을 더해 평문 주소를 `<a>`로 승격 — `a`·`code`·`pre` 안은 건드리지 않고, TreeWalker 순회를 **끝낸 뒤** 교체(도중 DOM 변경 시 walker가 어긋남). **저장 경로에선 끄고**(NoteEditor.persistKey) 화면 렌더·편집기 최초 주입에서만 켠다 — 타이핑 중의 `https://exam` 같은 미완성 주소가 링크로 굳는 걸 막는다(실측: 타이핑 직후 DB엔 평문, 새로고침 후 링크). ③ **항상 새 탭**: `cleanElement`가 href 있는 모든 `<a>`에 `target=_blank`+`rel=noopener noreferrer` 강제 — 대시보드(캔버스 상태)를 두고 떠나지 않는다. 기존에 target 없이 저장된 링크도 렌더 시 교정된다. ④ **편집 중에도 클릭 연결**: contentEditable은 브라우저가 링크를 열지 않으므로(캐럿만 이동) RichTextArea가 클릭을 직접 처리 — **클릭=새 탭**, **Alt+클릭=캐럿**(링크 글자 수정용). prose에 `cursor-pointer`·`break-words`(긴 주소 줄바꿈) 추가. ⑤ **메모(평문 textarea)는 본문 안 링크가 원천적으로 불가** → 본문 아래 `MemoLinks` 칩 줄(호스트 라벨+전체 주소 title, 중복 제거, 타일·전체보기 공용, `data-pb-no-drag`로 그리드 드래그와 무충돌). ⑥ **공유 블록**: `/share`가 `🔗 news.naver.com — {url}` 형태 링크로 append(사이트 식별), 본문 text에 섞여 온 주소는 ②의 자동 링크가 처리. **실브라우저 검증**(승인 테스트 계정, 검증 후 삭제): 타일·전체보기·소제목 섹션의 평문 주소 4종이 모두 `<a target=_blank rel=noopener noreferrer>`로 렌더, 타일 링크 클릭→새 탭(그리드 드래그가 클릭을 삼키지 않음), 편집기 링크 클릭→새 탭·Alt+클릭→열리지 않음, 메모 칩 클릭→새 탭·본문에 주소 입력 시 칩 즉시 추가, `/share` 실호출→노트에 `🔗 news.naver.com — https://…` + text 속 블로그 주소 자동 링크, 콘솔에러 0. tsc·lint 0·test 66/66(신규 5)·build 그린. |
| 2026-08-18 | 구독 관리 **구독중/해지 표시 + 해지일**(기본값=다음 결제일) | components/widgets/subscriptions/{types,compute,SubscriptionManager,CompactView,ExpandedView}, lib/widgets/subscriptions/compute.test(신규) | 요구: 구독중/해지를 표시(기본 구독중), 해지면 날짜 입력. 추가 요구: 해지 기본 날짜 = **다음 결제일**(예 기준 2026-01-06·오늘 2026-03-18 → 2026-04-06). ① 이미 있던 `active` 불리언을 **구독중/해지로 승격**(새 필드 없음) + `canceledAt?`(ISO, 선택) 추가. 표시 문구는 `subStatusText` 한 곳에서 파생 — `해지 (2026.09.06)`, 날짜 없으면 `해지`. ② **해지 항목을 목록에 남긴다**(요구: 표시) — 기존 `sortedByNextDue`는 active만 걸러 화면에서 아예 사라졌다 → `sortedForList`로 교체(구독중 먼저·결제 임박 순, 해지는 뒤·최근 해지 순). 타일은 취소선+흐린 톤, 전체보기는 **상태 배지**(색만이 아니라 글자로도 구분). **합계는 그대로 구독중만** 더한다(computeTotals 무변경). ③ 해지 기본일 = `defaultCancelDate` = 기존 `computeNextDue`의 다음 결제일 재사용(새 날짜 계산 로직 0). 상태를 해지로 바꿀 때 **비어 있을 때만** 채우고(사용자가 고친 값 보존), 구독중으로 되돌리면 해지일을 지운다. ④ 편집 화면: '사용중' 체크박스 → **구독중/해지 select**, 해지일 input은 해지일 때만 노출 + 안내 문구. **테스트 6종**(신규 파일)이 사용자 예시(1/6→4/6)·주기별(주간 3/24·연간 2027-01-06·미래 기준일)·잘못된 날짜·상태 문구·합계 제외·정렬을 고정. **실브라우저 검증**(승인 테스트 계정, 검증 후 삭제): 편집에 구독중/해지 선택(기본 구독중), 넷플릭스(기준 2026-01-06)를 해지→해지일 **2026-09-06 자동 입력**(검증일 2026-08-18 기준 규칙 동일), 저장 후 타일 `해지 (2026.09.06)`·합계 ₩24,400→₩10,900, 전체보기 배지 `구독중`/`해지 (2026.09.06)`, DB `active:false, canceledAt:"2026-09-06"`, 구독중으로 되돌리면 해지일 칸·값 사라지고 합계 복귀(₩24,400). 콘솔에러 0(로그아웃 403은 삭제된 이전 테스트 계정 건으로 무관). tsc·lint 0·test 61/61(신규 6)·build 그린. |
| 2026-08-18 | 노트 **머리말 삭제**(+ 되돌리기 '＋ 머리말') | components/widgets/note/{sanitize,NoteEditor}, lib/widgets/note/sanitize.test(신규) | 요구: 소제목처럼 머리말도 지울 수 있게. 기존엔 내용을 비워도 빈 한 줄이 계속 남아 실제로 지울 방법이 없었다. ① **표시 여부를 저장값에서 파생** — `showIntro = 소제목없음 || 다시열기 || !isBlankHtml(html)`. 새 config 필드 없이 `html: ""` 하나로 "삭제됨" 상태가 표현되고 새로고침에도 유지된다. ② `isBlankHtml`(sanitize.ts): 빈 `<p></p>`·`<br>`·공백은 비었다고 보되 **글자가 없어도 이미지·표·구분선이 있으면 내용 있음**으로 판정 — 텍스트만 보면 사용자의 이미지가 든 머리말을 숨겨버린다(테스트 3종으로 고정). ③ **소제목이 있을 때만** 삭제 제공: 소제목이 없으면 이 영역이 곧 노트 본문이라 지울 대상이 아니다(전체 비우기는 '스타일 편집 > 노트 비우기'). 헤더 한 줄(`머리말 (선택)` + 휴지통)은 그 조건에서만 렌더. 삭제는 소제목과 같은 **2단계 확인**. ④ 삭제 시 `commit`으로 저장하면서 **등록된 편집 영역의 DOM도 즉시 비운다** — 언마운트 직전 blur→persist가 옛 내용을 되쓰는 경합 차단(직전 커밋의 commit 패턴과 같은 부류). ⑤ 지운 뒤 다시 쓰려면 상단 툴바의 **'＋ 머리말'**(showIntro가 false일 때만 노출). **실브라우저 검증**(승인 테스트 계정, 검증 후 삭제): 삭제 버튼→2단계 확인→머리말 영역·라벨 사라짐·소제목 유지, DB `html:""` 확인, **새로고침 후에도 숨김 유지**, '＋ 머리말'로 복구 후 입력→DB 저장, 소제목 없는 노트는 본문 그대로+삭제 버튼 미노출(회귀 없음), 콘솔에러 0. tsc·lint 0·test 55/55(신규 3)·build 그린. |
| 2026-08-18 | 이미지슬라이드 **캡처 붙여넣기**(위젯에서 바로) + 노트 소제목 **삽입 위치 수정** | components/widgets/image-slider/{imageFiles,useAddImages(신규),CompactView,ExpandedView}, components/widgets/note/{sections,NoteEditor}, lib/widgets/note/sections.test | 요구 2건. ① **캡처 붙여넣기**: 편집 다이얼로그엔 이미 붙여넣기가 있었지만 위젯에서 바로는 불가했다 → `imageFiles`에 `imagesFromClipboardEvent`(paste 이벤트)·`readClipboardImages`(`navigator.clipboard.read`, 버튼용)·실패 안내 문구 추가하고, 타일·전체보기가 공유하는 `useAddImages` 훅으로 **기존 파이프라인(filesToSlides→축소→config 영속)** 에 합류(새 저장 경로 0). 타일: 빈 상태는 포커스 가능(`tabIndex=-1`)해 Ctrl+V를 직접 받고, 이미지가 있으면 우상단 오버레이에 **붙여넣기 버튼**(호버/터치 상시). 전체보기: 한 번에 하나만 열리므로 **window paste 리스너**로 Ctrl+V를 받아도 대상이 모호하지 않다(이미지가 있을 때만 소비) + 버튼. 캡처 이미지는 파일명이 없어 캡션은 빈 값. 상한 초과·클립보드 권한 실패는 같은 message 슬롯으로 안내(조용한 무시 방지). 훅이 `configRef`를 저장 즉시 갱신해 연속 붙여넣기에도 직전 장이 유실되지 않는다. ② **노트 소제목이 늘 맨 아래 생기던 문제**(사용자 신고): `insertIndexFor`가 머리말(=활성 영역 기본값) 기준 'below'를 `sections.length`로 보내고 있었다. 머리말은 모든 소제목보다 **위**라 그 기준의 위/아래는 모두 목록 맨 앞이 맞다 → 0으로 수정(마지막 섹션 기준 'below'는 그대로 append). 또 **소제목 입력란은 contenteditable이 아니라 selectionchange로 안 잡혀** 커서를 거기 둬도 기준이 안 됐다 → 제목 input `onFocus`에서 activeKey 갱신. **실브라우저 검증**(승인 테스트 계정, 검증 후 삭제): 캡처 PNG를 paste 이벤트로 빈 타일에 → 즉시 표시 + DB 저장(dataURL 3,210자), `clipboard.write` 후 붙여넣기 버튼 2회 → DB 3장 누적. 노트는 ⓐ 아무 곳도 클릭 안 한 상태 '아래에' → **맨 위**(기존 오동작 해소), ⓑ '1주차' 소제목 칸 커서 → 그 바로 아래, ⓒ 마지막 섹션 본문 커서 → 맨 끝 append. 콘솔에러 0. tsc·lint 0·test 52/52·build 그린. |
| 2026-08-18 | 노트 **소제목 추가를 상단 툴바로 이동 + 위/아래 위치 선택** (+ 저장 되덮임 버그 수정) | components/widgets/note/{sections,NoteEditor,Toolbar}, lib/widgets/note/sections.test | 요구: 소제목 추가를 상단 메뉴로 옮기고, 현재 작성 중인 내용의 **위/아래를 골라** 추가. ① 순수 연산 2개 신설 — `insertSectionAt`(index 클램프)·`insertIndexFor`(활성 key 기준 위/아래; 머리말·모르는 key는 맨 위/맨 아래). 테스트 6종 추가(51/51). ② Toolbar에 옵션 `extra` 슬롯을 두어 **같은 상단 바 안**에 `소제목 [↑위에][↓아래에]` 배치(별도 행 없이 flex-wrap 공유), 하단의 '소제목 추가' 버튼 제거. 기준은 **캐럿이 있는 영역** — 툴바 버튼과 같이 `onMouseDown preventDefault`로 캐럿을 유지해야 위치 판정이 맞는다. 단일 섹션 모드는 삽입 위치가 모호해 미노출('전체 보기'에서). 새 섹션의 소제목 칸에 `autoFocus`(브라우저가 스크롤도 해줌). ③ **버그: 새로 추가한 섹션이 즉시 사라짐**(실브라우저에서 포착 — 본문에 캐럿을 둔 뒤 추가할 때만 재현). 원인은 autoFocus가 커밋 단계에서 본문 **blur → 즉시 persist**를 유발하는데, 그 시점의 `configRef`가 아직 **이전 config**(passive effect 미실행)라 방금 추가한 섹션을 되덮었다. 저장 경로를 `commit(next)`로 통일해 **우리가 만든 next를 ref에 즉시 반영**(effect를 기다리지 않음) — persistKey·saveSections·saveAttachments 모두 경유하므로 같은 부류의 경합(디바운스 저장이 옛 config를 되쓰는)도 함께 막힌다. **실브라우저 검증**(승인 테스트 계정, 검증 후 삭제): 상단 버튼 2종 노출·하단 버튼 0개, '1주차' 캐럿→아래에=바로 다음 위치 삽입+새 칸 포커스, '2주차' 캐럿→위에=그 앞 삽입, 머리말 캐럿→위에=맨 위, DB에 순서·기존 본문 그대로 반영(`1주차 내용` 보존), 삽입 후 본문 타이핑 저장도 섹션과 공존, 콘솔에러 0. tsc·lint 0·test 51/51·build 그린. |
| 2026-08-18 | 메모 제목 **한 곳에만 표시**(타일=헤더 / 전체보기=본문) | lib/widgets/contract(+output 동기화), components/canvas/FocusOverlay, components/widgets/memo/{index,CompactView,types,types.test} | 요구: 타일에서는 본문 제목을 숨기고, 전체보기에서는 상단 헤더 제목 대신 본문 제목이 보이게 — **둘 중 하나만**. 직전 커밋은 양쪽에 다 보여 중복이었다. ① 타일: CompactView의 제목 입력란 제거(프레임 헤더가 이미 제목을 보여주고 더블클릭 변경도 지원 — 기능 손실 없음). ② 전체보기: 컨트랙트에 옵션 `titleInBody?` 추가(메모만 true) → FocusOverlay가 `<h2>` 대신 **같은 `flex-1` 자리표시자**를 렌더(버튼 정렬 유지). **`aria-label`은 그대로 제목을 담아** 스크린리더에는 이름이 계속 전달된다(접근성 회귀 없음). ③ 타일 제목이 사라지며 죽은 코드가 된 `MEMO_TITLE_CLASS`(컨테이너 쿼리 변형) 삭제, 테스트도 전체보기 기준으로 정리. 저장 구조(`config.title`)·양방향 동기화는 그대로라 마이그레이션 불필요. **실브라우저 검증**(승인 테스트 계정, 검증 후 삭제): 타일=헤더 "회의 준비"·본문 제목 입력란 0개, 전체보기=헤더 h2 없음·본문 제목만·aria-label "회의 준비 자세히 보기"·헤더 버튼 3개 정렬 유지, 전체보기에서 제목 수정→타일 헤더 반영, 헤더 더블클릭 수정→전체보기 본문 제목 반영(양방향 유지), **노트·뉴스 전체보기 헤더 제목은 그대로**(회귀 없음), 콘솔에러 0. tsc·test 48/48·build 그린. lint는 기존 `WidgetFrame.tsx:149` 1건만 잔존(이번 변경과 무관). |
| 2026-08-18 | 메모 위젯 **제목**(본문 상단 입력란 ↔ 헤더 제목 양방향) + **기존 위젯 제목 이관** | components/widgets/memo/{types,types.test(신규),index,useMemoText,CompactView,ExpandedView}, components/canvas/GridCanvas | 요구: 내용 상단에 제목을 넣고, 그 제목을 바꾸면 메모 제목(헤더)도 바뀌게 + 본문보다 큰 글씨. 반대로 헤더 제목을 바꾸면 본문 제목도 바뀌어야 함. ① **한 필드 공유**로 양방향 해결 — `MemoConfig.title?`을 뉴스가 쓰던 컨트랙트 옵션 `instanceTitle`/`renameInstance`에 연결(신규 배선 0). 헤더·전체보기 h2·PiP 창 제목이 모두 같은 값을 읽고, 헤더 더블클릭 변경도 같은 필드에 저장되므로 두 방향이 자동으로 맞는다(비면 null→displayName "메모" 폴백). ② **useMemoText 일반화**: 필드 인자(`"text"|"title"`, 기본 text)와 제네릭 엘리먼트 타입 추가 — 언컨트롤드+디바운스 저장+비포커스 외부동기화가 제목에도 그대로 적용된다. 헤더에서 이름을 바꾸면 본문 입력란이 **포커스 중이 아닐 때만** 갱신돼(사용자 입력 우선) 유실이 없다. 신규 훅을 만들지 않은 이유: 두 필드가 같은 config에 저장돼 저장 경합·스테일 클로저 처리가 동일하다. ③ **글자 크기**: `MEMO_TITLE_CLASS`(타일)·`MEMO_TITLE_CLASS_EXPANDED`(전체) — 같은 size 버킷 본문보다 **한 단계 위** + `font-semibold`. 테스트가 본문/제목 클래스의 크기 순위를 비교해 이 불변식을 강제(하드코딩 문자열 비교 아님). ④ 잠금 메모는 기존대로 프롬프트가 본문을 가림(제목은 헤더에 이미 보이므로 동작 변화 없음). ⑤ **기존 위젯 회귀 차단(GridCanvas)**: 메모는 그동안 헤더 제목을 **기기별 `pb:title:{id}`(localStorage)** 에 저장했는데, `instanceTitle`이 생기면 GridCanvas가 그 값을 더 이상 읽지 않아 **예전에 바꿔둔 이름이 사라져 보인다**(직전 커밋의 뉴스 위젯도 동일). → 셀에 **1회 자가치유 이관** 추가: config 제목이 비어 있고 로컬 제목이 있으면 `renameInstance`로 config에 옮기고 로컬 키를 지운다. 특정 위젯이 아니라 "instanceTitle을 갖게 된 모든 위젯"에 적용되는 공통 경로라 메모·뉴스가 한 번에 해결된다. **실브라우저 검증**(승인 테스트 계정, 검증 후 삭제): 타일 제목 입력 "장보기 목록"→헤더 즉시 반영, 헤더 더블클릭 "주간 회의록"→타일 입력란 반영(역방향), 전체보기 제목 "8월 3주차 회의"→h2·타일 동기화, 제목 18px/600 vs 본문 16px/400, DB config에 `title` 저장 확인, 새로고침 복원. **구버전 상태 재현 검증**(title 없는 메모 2개 + 한쪽에 `pb:title` 주입): 로컬 제목이 헤더·본문 제목으로 이관되고 로컬 키 삭제·DB에 `title` 저장, 재방문에도 안정(멱등), 제목 없던 메모는 빈 입력란으로 정상 동작 후 새 제목 입력 시 헤더 반영. 콘솔에러 0. lint의 react-hooks `refs` 오탐은 기존 선례대로 구조 분해로 해소. tsc·lint 0·test 48/48(신규 3)·build 그린. |
| 2026-08-12 | 신규 로그인 **기본 위젯 5종 교체** + 뉴스 위젯 **제목에 검색어 노출** | lib/supabase/queries/boards.ts, components/widgets/news/{types,types.test(신규),index,CompactView} | 요구 2건. ① 첫 로그인 시드를 `memo·todo·stock` → **`weather·memo·world-clock·clipboard·news`**(배열 순서 = 배치 순서, buildSeedWidgets가 그 순으로 빈 칸을 채움). 기존 사용자는 보드가 이미 있어 영향 없음(부트스트랩은 dashboards 0건일 때만). ② 뉴스 헤더 제목이 항상 "뉴스"라 어떤 검색어인지 안 보였다 → 노트가 쓰던 **컨트랙트 옵션 `instanceTitle`/`renameInstance` 재사용**(신규 배선 0): `newsInstanceTitle`이 `뉴스 (보험 검색)`을 파생하고, 더블클릭 제목 변경은 `NewsConfig.title?`에 저장돼 **검색어를 바꿔도 유지**(둘 다 비면 null→displayName 폴백). 헤더·전체보기·PiP가 같은 함수를 쓴다. 타일 안 중복 검색어 줄(`"보험"`)은 제거해 목록 한 줄 확보(빈 결과 안내문은 유지). ③ 승인 회원 `tkleen@naver.com`의 기본 보드는 **손대지 않은 시드 3종**(빈 메모·샘플 할일·기본 지수)뿐이라, 배포 반영 후 보드 행을 삭제해 다음 로그인에 새 시드로 재부트스트랩(위젯은 FK on delete cascade). tsc·lint 0·test 45/45(신규 3)·build 그린. |
| 2026-08-12 | 로그아웃 **발견성 개선**(설정>계정에 버튼+안내 문구) | lib/auth/useSignOut(신규), components/canvas/{AccountMenu,SettingsDialog,NoAccess,CanvasShell} | 요구: "로그아웃 버튼이 어디 있나요? 안내 문구도 추가". 기존 유일 진입점이 **좌하단 라벨 없는 아바타**뿐이라 못 찾음(dev에선 Next 개발도구 오버레이가 같은 좌하단을 덮어 클릭까지 가로챈다 — 프로덕션은 무관). ① **설정 > 계정 탭에 로그아웃 섹션** 추가(⚙는 상단 툴바라 발견 쉬움) + 안내 문구 "왼쪽 아래 동그란 프로필 아이콘('P')을 눌러도 로그아웃… 내용은 그대로 저장되며 다시 로그인하면 이어서 사용". 아바타 글자는 `initialOf`를 AccountMenu에서 export해 **같은 규칙 인용**(문구와 실제 글자 어긋남 방지). ② 아바타 aria-label "계정 메뉴"→"계정 메뉴 (로그아웃)", title에 "· 클릭하면 로그아웃". ③ 진입점이 3곳(아바타·설정·승인대기 화면)이 되면서 signOut 로직을 **공용 훅 `useSignOut`** 으로 통일 — flush(onBeforeSignOut) await → signOut → replace/refresh 순서가 한 곳에만 있다(세션 소멸 후엔 RLS로 대기 upsert가 실패하므로 순서가 중요). 설정 경로도 `flushNow`를 받아 저장 유실 없음. 라벨 "소유자 계정"→"로그인된 계정"(승인 회원도 사용). **실브라우저 검증**(승인 테스트 계정, 검증 후 삭제): 설정>계정에 안내 문구·로그아웃 노출→클릭→/login, 재로그인 후 아바타 드롭다운("로그인된 계정"+로그아웃)→클릭→/login, 콘솔에러 0, 테스트 계정·데이터 삭제 확인(소유자 외 0행). tsc·lint 0·test 42/42·build 그린. |
| 2026-08-12 | **관리자 승인제 다중 사용자**(단일 ALLOWED_EMAIL → 승인된 회원 공유) | supabase/migrations/20260812120001_pb_members.sql(신규), output/{schema.sql,types/database.ts}, lib/auth/{access,members,actions,members.test}(신규), app/{page,auth/callback/route,share/route,(auth)/login/{page,actions}}, proxy.ts, components/canvas/{SettingsDialog,NoAccess(신규),CanvasShell}, lib/supabase/admin(주석), .env.example | 요구: 관리자가 이메일을 승인해 소수가 기능(주식 토큰 등)을 공유. **로그아웃은 이미 존재**(AccountMenu 좌하단)라 신규 작업 없음. ① **허용목록 = `pb_members`**(email PK·pending/approved/blocked, RLS enable + **정책 없음**=deny-by-default → service-role 경로로만 접근). 이메일이 PK인 이유: 승인은 auth.users가 생기기 **전에** 일어난다(미승인자는 계정 자체가 없어 봇 가입·SMTP가 불필요). `ALLOWED_EMAIL`은 env에 유지 = **관리자 + 부트스트랩**(표가 비거나 DB 실패해도 관리자는 안 잠김, DB 조회도 안 함). ② **판정 규칙 1곳**: 순수 `lib/auth/access.ts`(decideAccess)를 5개 진입점이 공유 — DB·별칭 import가 없어 node --test 가능(members.ts는 DB 접근만). ③ **proxy는 세션 유무만** 검사로 축소 — Next 문서상 proxy에 DB 금지이고, 미승인 세션을 /login으로 튕기면 "authed+/login→/" 규칙과 물려 **리다이렉트 루프**가 된다. 대신 page.tsx가 미승인 세션에 `NoAccess`(로그아웃이 유일한 출구)를 렌더. ④ **가입 흐름(SMTP 0)**: /login '비밀번호 설정' → 미승인이면 계정 생성 없이 pending 접수 + 안내(pending은 빨간 오류 아닌 notice 스타일) → 관리자가 설정 > **멤버 탭**(관리자에게만 노출, 권한은 서버 액션이 재검증)에서 승인 → 사용자가 다시 눌러 계정 생성. 승인 알림 메일은 없음(관리자가 직접 통지). ⑤ **권한 회수 = auth 계정 삭제**: 요금 유발 라우트의 `requireUser()`는 세션 유무만 보므로 목록만 고치면 리프레시 토큰으로 계속 쓸 수 있다 → 차단·삭제 시 `admin.deleteUser`로 세션을 죽인다(그 사용자 pb_* 데이터도 FK로 함께 삭제). ⑥ **공유는 무작업**: KIS 토큰이 이미 Supabase Storage 전역 캐시라 승인 회원이 그대로 공유(단 KIS 시세 재배포 금지 조항과 상충 소지 — 사용자 판단으로 진행). tsc·lint 0·test 42/42(신규 4)·build 그린. **라이브 적용**: CLI가 다른 계정 로그인 상태(403, 2026-07-11과 동일)라 사용자가 SQL Editor에서 직접 실행 → REST로 확인. **실브라우저 E2E 검증**(playwright, 실제 Supabase·테스트 계정은 검증 후 삭제): ① 미인증 게이트 `/`→307 /login, /share→307 `/login?next=…`, /api/weather→401. ② RLS — anon select 0행·anon이 approved 행 insert 시도 **42501 차단**, 대문자 이메일 insert **23514 차단**(lower 제약). ③ 요청→승인→가입 전 과정: 미승인 제출 → notice "접근 요청이 접수되었습니다" + pb_members pending 1행 + **auth 계정 미생성**(소유자 1명뿐) → 승인 후 재제출 → 계정 생성(email_confirmed) → 캔버스 진입(기본 보드 부트스트랩). ④ **공유 확인** — 승인 회원 화면에서 주식 위젯이 서버 키로 실시세 수신(코스피지수 6,634.52 +4.55%). ⑤ 비관리자는 설정에 **멤버 탭 미노출**(앱 표시·계정만). ⑥ 승인 취소 후(세션 유지) `/` → **리다이렉트 루프 없이** '승인 대기 중' 화면 → 로그아웃 → /login. ⑦ 정리: 계정 삭제 시 그 사용자 pb_dashboards·pb_widgets **FK 캐스케이드 삭제 확인**(소유자 외 0행). 콘솔에러 0. |
| 2026-08-07 | 주식 **국내 ETF 검색 지원**(코스피100·KODEX…) + 검색 랭킹 수정 | scripts/gen-krx-catalog.mjs, lib/api/stock/{krx-catalog.generated(재생성 2639→3794),symbols,symbols.test}, components/widgets/stock/format | 요구: "코스피100" 같은 ETF가 검색되지 않음. 원인: 번들 카탈로그가 KIND **상장법인목록**(법인)이라 ETF가 통째로 없음. ① 생성기에 **네이버 금융 ETF 전종목**(`finance.naver.com/api/sise/etfItemList.nhn`, 키리스 JSON, **EUC-KR 디코딩 필수** — UTF-8로 읽으면 한글이 깨져 0건 매칭) 소스 추가 → 1,160종 합류(총 3,794). ETF는 전부 유가증권시장이라 KOSPI와 같은 bare 코드. ② **KRX 신형 영숫자 단축코드**(`0167A0`, 최근 상장분 294건) 수용: `krCode`를 `^[0-9][0-9A-Z]{5}$`로 확장(미국 티커는 영문 시작이라 무충돌), format.quoteInfoUrl 동일 확장. 단 `isDomesticSymbol`(=KIS 라우팅)은 **숫자 6자리로 한정** — KIS 국내시세에서의 신형 코드 동작이 미확인이라 실측 확인된 Yahoo 경로로 보낸다(`0167A0.KS` 15,180원 수신). ③ **검색 랭킹 수정**: 질의에서 숫자만 뽑아 코드 접두 매칭하던 로직이 "코스피100"을 코드 100…(동국S&C 등)에 매칭시켜 정작 코스피100 ETF를 밀어냈다 → 코드 검색은 **질의 전체가 코드 모양일 때만**, 그리고 결과를 정확일치→이름접두→포함 순으로 정렬(ETF 합류로 "삼성전자"가 "1Q 삼성전자선물…레버리지"에 밀리던 것도 해소). **실측**: 코스피100→파워/마이티/KODEX/KIWOOM 코스피100 4건, 삼성전자→005930 1순위, kodex 200→069500 1순위, 시세 237350=77,820원·226490=63,850원·0167A0=15,180원. tsc·test 38/38(신규 3)·build 그린. |
| 2026-08-07 | 주식 미국 종목 **한글 이름 검색**("애플"·"배당 ETF") | app/api/stocks/search/route.ts, components/widgets/stock/SymbolManager | 요구: 한글 이름으로도 미국 종목 검색. Yahoo 검색은 **한글 질의에 400**(`lang=ko-KR&region=KR`로도 동일 — 실측)이라 한 단계 앞에 번역을 둔다: 질의에 한글이 있으면 저장소의 기존 `translateClient.translate(q,"ko","en")`(키리스 Google gtx → MyMemory, 새 의존성 0)로 변환 후 기존 검색 경로 그대로. 번역 실패·결과에 라틴 문자가 없으면 업스트림 호출 없이 빈 결과(400 방지). 클라이언트 게이트도 `[A-Za-z]`→`[A-Za-z가-힣]`로 확대(숫자만=국내 코드는 여전히 로컬 카탈로그). **대안 검토**: 네이버 종목 자동완성(`ac.stock.naver.com/ac`)은 한글명→티커가 정확하지만("애플"→AAPL) '배당'류 주제어에 빈 결과라 제외 — 번역 경로가 회사명·주제어를 모두 커버. **실측**(번역+필터 실제 함수 체인): 애플→AAPL, 엔비디아→NVDA, 테슬라→TSLA, 마이크로소프트→MSFT, 버크셔 해서웨이→BRK-B, 배당 ETF→SPYD·DVY·HDV·SDY, 반도체 ETF→SOXX·SMH·XSD, 나스닥 100 ETF→QQQM. tsc·test 35/35·build 그린, 신규 lint 0. ⚠ 번역 의존이라 애매한 이름은 어긋날 수 있음(코카콜라→"coke"→COKE가 KO보다 상위) — 영문명·티커 입력 경로 유지. |
| 2026-08-07 | 주식 **미국 종목·ETF 이름 검색** + **종목명 표시**(티커 → 실제 이름) | app/api/stocks/search/route.ts(신규), output/api-shapes(StockSearch*), lib/api/stock/{symbols(mapUsSearchResults),fallbackClient,symbols.test}, components/widgets/stock/SymbolManager | 요구: ETF 등이 이름으로 검색이 안 되고 표시도 코드로만 됨. ① **표시명**: 미국은 로컬 카탈로그가 없어 `resolveMeta`가 티커를 이름으로 돌려줬다 → Yahoo **chart 응답에 이미 들어 있는** `longName`(폴백 `shortName`)을 미국 티커에 한해 사용(추가 요청 0, 국내·지수는 큐레이션 한글명 보존). shortName은 31자에서 잘려 오므로 longName 우선(CSS 말줄임이 자연스럽게 처리). ② **이름 검색**: `/api/stocks/search?q=`(requireUser 게이트, Yahoo `v1/finance/search` 키리스 프록시 — 브라우저 직접 호출은 CORS). 비-라틴 질의는 Yahoo가 400을 주고 국내는 로컬 카탈로그가 담당하므로 호출 생략. 필터·정규화는 순수 함수 `mapUsSearchResults`로 분리(라우트는 export 제약이 있어 테스트 불가) — quoteType EQUITY/ETF만, 접미사(.) 있는 심볼 제외(AAPL.BA·APC.DE 해외 중복상장, 옵션 심볼). ③ SymbolManager: 300ms 디바운스 + AbortController, 응답을 질의와 함께 보관해 **렌더 시 현재 질의와 일치할 때만 노출**(지연 응답 경합 차단 + effect 본문 동기 setState 회피 = react-hooks v6 규칙). 결과는 '미국 주식 · ETF' 그룹으로 국내 결과 아래 표시(ETF 배지). **실측**: 라이브 Yahoo 응답에 실제 필터 적용 — apple→AAPL·APLE, dividend etf→HDV·SPYD·SDY·DVY·IDV, nasdaq 100→QQQM 등, 옵션·MUTUALFUND·해외중복상장 전부 제외. 라우트 미인증 401 실측. tsc·lint(신규 0)·test 35/35(신규 2)·build 그린. 로그인 상태 브라우저 UI 검증은 미실시. |
| 2026-08-07 | 이미지 위젯 **타일 이미지 클릭 = '전체'** | components/widgets/image-slider/CompactView.tsx | 요구: 이미지를 클릭하면 [전체] 버튼과 같은 효과. 기존 `useOpenWidgetFocus`(노트 제목 클릭이 쓰는 GridCanvas 제공 경로) 재사용 — 새 배선 없음. 같은 div가 드래그 패닝도 담당해 드래그 종료 후에도 click이 발화하므로, 포인터 제스처의 **누적 이동거리 5px 미만일 때만** 클릭으로 취급(손떨림 허용, 패닝 후에는 안 열림). 터치는 pointerdown 조기 반환 경로라 이동 추적 없이 탭이면 열림(네이티브 스크롤 후 click은 브라우저가 억제). 우상단 [추가·다운로드] 버튼은 스크롤 박스의 형제라 버블링 무관. title 속성으로 어포던스 안내. tsc·lint·build 그린. 브라우저 검증 미실시(playwright MCP 미연결). |
| 2026-07-31 | 주식 위젯 **미국 개별 종목·ETF 추가** 지원(지수만 → 티커 직접 입력) | lib/api/stock/{symbols,kisClient,symbols.test(신규)}, components/widgets/stock/{SymbolManager,format,QuoteRow}, tsconfig | 요구: 미국 주식·ETF도 추가. 기존엔 UI가 6자리 국내 코드만 허용("미국은 지수만")하고, KIS 활성 시 라우팅이 **US 지수만** 폴백에 위임해 미국 종목은 조용히 errors로 빠졌다. ① `isUsTicker`(야후 티커 문법: 영문 시작+영숫자/./-, 국내 코드는 숫자 시작이라 무충돌)·`isDomesticSymbol` 추가, `resolveMeta`가 미국 티커를 USD·개별종목으로. ② **kisClient 라우팅 근본 수정**: `!isUsIndex` → `!isDomesticSymbol` — 국내(지수·6자리)만 KIS, **해외 전부**(지수+AAPL·SPY) 키리스 야후 폴백 위임. ③ SymbolManager: 티커 입력 허용(대문자 정규화)·'직접 추가' 행을 검색 결과 위에 상시 노출·라벨/안내문 갱신. ④ quoteInfoUrl 미국 티커→야후 파이낸스 종목 페이지(네이버 검색 폴백 대신). ⑤ 심볼 카탈로그 import에 `.ts` 확장자 + `allowImportingTsExtensions`(node --test가 확장자 없는 상대경로를 못 여는 문제, 번들러는 동일 해석). **실측**: 야후 chart로 AAPL/SPY/QQQ/BRK-B 가격·전일종가·`currency:USD` 수신 확인. tsc·test 33/33(신규 4)·build 그린. 브라우저 UI 검증은 미실시(폼 검증 로직만 변경). ⚠ 미국 시세는 폴백(야후) 경로라 15분 지연·'근사치' 성격. |
| 2026-07-28 | 메모도 **편집=전체 화면 환경 + '스타일 편집' 분리**(노트와 동일 UX) | components/widgets/memo/{index,ExpandedView,CompactView,ConfigEditor,useMemoText(신규)} | 요구: 노트에 이어 메모에도 적용. `editInFocus: true` — ⋮ '편집'→전체보기, '스타일 편집'→ConfigDialog("메모 스타일 편집"). ① **전체보기 편집 가능 전환**: 읽기 전용 `<p>`→타일과 같은 인라인 편집 textarea(잠금 게이트·'지금 잠금'·view-only 글자 크기·강조 레일 유지, 안내문은 '스타일 편집'으로 갱신). ② **ConfigEditor에서 본문 제거**: 내용 textarea·잠금 우회 경고 블록 삭제(내용이 아예 미노출이라 우회 우려 소멸), 비밀번호·강조 색상·글자 색·글자 크기만 + 내용 위치 안내 1줄. ③ **양방향 텍스트 동기화(useMemoText 신규)**: 타일·전체보기가 둘 다 uncontrolled 편집기가 되면서 한쪽 수정 후 다른 쪽 blur가 옛 텍스트를 덮어쓰는 유실 경로 발생 → 공용 훅으로 디바운스 저장(500ms·blur 즉시) + **외부 변경 시 비포커스 textarea DOM 값 갱신**(포커스 중이면 사용자 입력 우선), CompactView 기존 중복 persist 로직도 훅으로 흡수. react-hooks v6 `refs` 오탐(훅 반환 객체 멤버 접근)은 구조 분해로 해소. **실브라우저 검증**(임시 /edit-test + 실제 WidgetPersistenceProvider, 검증 후 삭제): 메모 메뉴 [자세히·편집·스타일 편집], 편집→"메모 자세히 보기" 편집 textarea·헤더 스타일 편집 버튼, 전체보기 입력→저장→타일 동기화, 타일 수정→전체보기 최신값(역방향), 스타일 편집 다이얼로그(본문 없음·비밀번호/강조/글자 색/크기), 노트 회귀 정상, 콘솔에러 0. tsc·lint·test 29/29·build 그린. |
| 2026-07-28 | 노트 **편집=전체 화면 환경** + **'스타일 편집' 분리** | lib/widgets/contract.ts(+output 동기화), components/widgets/note/{index,ConfigEditor}, components/canvas/{WidgetMenu,CanvasShell,FocusOverlay,ConfigDialog} | 요구: 노트 내용 편집은 전체 화면과 동일 환경에서, 색·비밀번호류 설정은 별도 '스타일 편집' 버튼으로. 컨트랙트에 옵션 플래그 `editInFocus?`(true=내용 편집이 전체보기에서 이뤄지는 위젯) 추가, 노트만 설정 — CanvasShell renderActions가 이 플래그로 분기해 ⋮ **'편집'→전체보기(openFocus, NoteEditor 풀 편집 환경)** 로 라우팅하고 ⋮ 에 **'스타일 편집'(팔레트 아이콘)→ConfigDialog** 항목을 추가(비-editInFocus 위젯은 기존 그대로, 스타일 편집 항목 미노출). 전체보기(FocusOverlay) 헤더의 연필 버튼도 editInFocus면 팔레트 아이콘·"스타일 편집" 라벨로, ConfigDialog 헤더/aria도 "노트 스타일 편집"으로 명명(한 플래그가 세 곳 라벨 일괄 구동). 노트 ConfigEditor 안내문을 "메뉴의 '편집'(전체 화면)"으로 갱신. **실브라우저 검증**(임시 /edit-test 스텁 하네스, 검증 후 삭제): 노트 메뉴 [자세히·편집·스타일 편집], 편집→전체보기(contenteditable 편집기)·헤더 버튼 "스타일 편집", 스타일 편집→"노트 스타일 편집" 다이얼로그(내용 편집기 없음·공유/제목/비우기만)·전체보기 위 스택·Esc 단계별 닫힘(스타일만→전체보기), 메뉴 '스타일 편집' 직접 진입, 메모 대조군(스타일 편집 미노출·편집→"메모 편집" 그대로), 콘솔에러 0. tsc·lint·test 29/29·build 그린. |
| 2026-07-19 | 전체보기 **편집 버튼** + 위젯 **항상-위 화면 고정(Document PiP)** | components/canvas/{FocusOverlay,CanvasShell,WidgetPip(신규)} | 요구 2건: ① **전체보기에서 편집**: FocusOverlay 헤더에 편집(연필) 버튼 → 기존 openEdit 경로로 ConfigDialog(z-70)가 전체보기(z-60) 위에 스택(백스택 LIFO 그대로, 저장 시 전체보기 라이브 갱신). 신규 경합 차단: 둘 다 window Esc 리스너라 Esc 한 번에 같이 닫히던 문제 → `escDisabled`(편집 오픈 동안 FocusOverlay Esc 무시). ② **항상-위 고정**: 전체보기 헤더 고정 버튼 → **Document Picture-in-Picture**(Chromium) 창(420×560 "적절히 작게")에 ExpandedView를 createPortal — OS 최상위 창이라 다른 앱/프로그램 위에 항상 표시, 이동·크기조절은 OS 창 네이티브, 내용은 내부 overflow-auto 스크롤(요구 전부 충족). 포털이 CanvasShell 트리(WidgetPersistenceProvider) 안 → PiP 안 인라인 편집/저장도 같은 영속 경로, React 19 cross-document 포털로 컨텍스트·이벤트 정상. 스타일은 cssRules `<style>` 인라인 복사(접근불가 시트는 link 폴백)+루트 속성(data-theme/data-updown/폰트 클래스) 미러+테마 토글 동기화, 위젯 제목=창 타이틀. 대상 인스턴스는 **전 보드에서 해석**(탭 바꿔도 유지), 삭제 시 창 자동 닫힘, 창 닫힘(pagehide)·로그아웃 언마운트 시 정리, 문서 PiP 1창 제한이라 재고정 시 기존 창 닫음, 미지원(Safari/Firefox)은 토스트 안내. **실브라우저 검증**(임시 /pip-test 스텁 하네스, 검증 후 삭제): 편집 스택·저장 라이브 반영·Esc 단계별 닫힘(편집만→전체보기), PiP 오픈 시 전체보기 자동 닫힘·다크 토큰/폰트 적용·창 타이틀 "메모"·내부 버튼 클릭(React 이벤트, aria-pressed 토글)·메인 편집→PiP 즉시 동기화·close→pagehide 상태 정리, 콘솔에러 0. tsc·lint·test 29/29·build 그린. |
| 2026-07-17 | 위젯 리사이즈 **4모서리 핸들**(우하단 1곳 → 좌상·우상·좌하 추가) | components/canvas/GridCanvas.tsx, app/globals.css | 요구: 크기 조절을 우측하단뿐 아니라 4군데 모서리 전부에서. `resizeConfig.handles` `["se"]`→`["se","sw","ne","nw"]` — RGL v2 `resizeItemInDirection`이 nw/ne/sw에서 x/y를 함께 조정하고, 겹침-밀기·클램프 로직은 전부 렉트 기반(desiredRef가 x/y/w/h 기록, pushResolveDown은 anchor 렉트 푸시)이라 **무수정 호환**. 상단 두 핸들(nw/ne)만 20→**16px**(globals.css) — RGL이 핸들을 타일 콘텐츠 뒤에 append해 클릭을 가로채므로 헤더 ⋮ 메뉴(우상, 12px 인셋)·종류 아이콘(좌상) 클릭 영역을 비켜감(하단 sw/se는 20px 유지, 글리프는 회전 상속이라 CSS 추가 불요). **실브라우저 검증**(임시 /resize-test 스텁 페이지, 검증 후 삭제): NW 드래그 A{x8,y8,6×6}→{x6,y6,8×8}(위치+크기 동시 조정), NE는 y↓·w/h↑·x 불변, SW는 x↓·w↑·y 불변, NW로 이웃 위로 확장 시 release에서 anchor(B) 유지·A→y14·C→y20 연쇄 밀림(기존 겹침 해소 경로 그대로), ⋮ 스탠드인(size-7 동일 지오메트리) 중심 클릭=버튼 수신·타일 극단 모서리=핸들 수신, 콘솔에러 0. tsc·lint·test 29/29·build 그린. |
| 2026-06-20 | 초기 하네스 구성(에이전트 5 + 스킬 5 + 오케스트레이터) | 전체 | 설계서 §9 하네스 청사진 실체화, 실행모드=에이전트 팀, QA·rls-verify 추가 |
| 2026-07-12 | 이미지슬라이드 **저장 한도 2배(40장·~4MB) + 타일 이미지 패닝·마지막 화면 유지** | image-slider/{imageFiles,CompactView,ExpandedView,useSlideView(신규)}, output/widget-manifest.md | 사용자 요청 2건: ① **한도 확대("20→40")**: 코드에 20MB 제한은 없고 실제 상한은 `MAX_IMAGES=20장`·`MAX_TOTAL_CHARS≈2MB`(dataURL 인라인) — 요청 취지대로 **2배**(40장·~4MB)로 확대, 안내문구는 상수에서 파생(하드코딩 제거). config(jsonb) 인라인 구조는 유지(40MB급 대용량은 pb-images Storage 이관이 필요 — 보류·TODO 유지). ② **타일 패닝+마지막 화면**: 마우스/펜 드래그로 이미지 이동(터치=네이티브 스크롤, `[data-pb-no-drag]`라 그리드 드래그와 무충돌, setPointerCapture), 마지막 본 슬라이드·스크롤 위치를 인스턴스별 localStorage(`pb:imageSliderView:{id}`, useSlideView.ts — 뷰 상태는 기기 종속이라 config 대신 로컬)에 rAF 스로틀 저장, 재방문 시 복원. '전체'도 마지막 본 슬라이드에서 시작(읽기만). **경합 2건 설계로 차단**: 복원 스크롤은 대상 슬라이드 img onLoad에서만 적용(+index 일치 검증), 첫 슬라이드는 passive effect보다 onLoad가 먼저 올 수 있어 effect에서 즉시 1회 시도(높이 없으면 pending 보존); 마운트 직후 첫 persist는 skip(복원 전 {0,0} 덮어쓰기 방지). **실브라우저 검증**(임시 /slider-test, note-test 선례): 드래그 +80px→scrollTop 80·저장, 새로고침→같은 슬라이드·위치 복원, index 시드→해당 슬라이드 복원, 콘솔에러 0 — 검증 후 페이지 삭제. tsc·lint·build 그린 |
| 2026-07-11 | 클립보드 **당일 강조 + 즐겨찾기(맨 위 고정·파란색)** | clipboard/{types,useClipboardHistory,CompactView,ExpandedView}, supabase/migrations/20260711120001_pb_clipboard_fav.sql(신규), output/{types/database.ts,schema.sql} | ① **당일 복사 강조**(요구): 오늘 복사한 항목을 2단계 큰 글씨+bold(타일 text-xs→base, 전체 text-sm→lg), `isSameLocalDay`+`useNow(60s)`로 자정 지나면 자연 해제. ② **즐겨찾기**(요구): 각 항목에 별(★) 토글(타일·전체) — 즐겨찾기는 **맨 위 고정**(fav desc→created_at desc 정렬), 당일과 같은 형태이되 **파란색**(text-blue-500), cap 자동 정리에서 제외, 같은 텍스트 재복사 시 fav 승계. `pb_clipboard.fav boolean` 컬럼(멱등, RLS 기존 정책 그대로) — **라이브 적용**(migration list 11/11 local=remote, REST로 fav 조회 200 실측). CLI가 다른 Supabase 계정 로그인 상태여서 403 → 계정 재로그인 후 push(참고: 이 PC는 Supabase 계정 2개 사용 중). gen types 재생성으로 수동 타입 선반영 검증(diff=fav 3줄+BOM). tsc·lint·build 그린. |
| 2026-06-20 | Phase 1(DB) 라이브 적용 + 게이트 통과 | supabase/migrations, output/types | pb_* 5테이블·RLS·Storage → all_in_one_board(nsedndrdykeujribqyqx), RLS 10/10 PASS |
| 2026-06-20 | Phase 2(캔버스 셸) 구현 — 빌드 그린 | lib/, components/canvas/, app/ | 컨트랙트 동결·백스택(§6.3)·GridCanvas(RGL v2)·팔레트·보드탭·툴바·포커스, Playwright QA 통과 |
| 2026-06-20 | Phase 4 위젯 9종(무외부의존) 구현 | components/widgets/ | 메모·계산기·세계시계·D-Day·할일·즐겨찾기·연락처·필수정보·이미지슬라이드 + 실 레지스트리, 라이브 렌더 확인 |
| 2026-06-20 | 인증(매직링크) + DB 영속화 | proxy.ts, app/(auth), lib/persistence | **Next 16 Proxy**(=middleware) 가드·ALLOWED_EMAIL 단일사용자 게이트·pb_* 낙관적 디바운스 저장. proxy 리다이렉트→/login·로그인 UI 런타임 확인. 잔여: 사용자 Supabase redirect 설정 + 매직링크 로그인으로 영속화 런타임 검증 |
| 2026-06-20 | Phase 3 데이터 계층 + 위젯 15종 완성 | app/api/*, lib/api/*, output/api-shapes.ts, components/widgets/* | 주식(KIS/SSE+공개지수 fallback)·날씨·환율·뉴스 프록시·카드 인제스트/CSV/파서·캘린더 + api-shapes 단일소스. 결합 빌드 그린. keyless fallback로 즉시 동작, KIS/KMA/Naver/Google 키 시 라이브 전환. 잔여(라이브): 키 입력 + Supabase Google provider 설정 |
| 2026-06-21 | 모바일/태블릿 반응형 + UX 보강 | GridCanvas, WidgetMenu, CanvasShell, calculator/*, app/icon·apple-icon·manifest | ① 반응형 그리드: lg(>1024,12col,영속)·md(>640,6col flow)·sm(≤640,1col) 브레이크포인트 재도입, 비-lg는 lg에서 파생·flow-pack(가로 가득), 비-lg 드래그/리사이즈/드롭 비활성 → 모바일 배치 변경이 PC 미반영(추가/삭제/설정은 영속 경로로 반영). ② 모바일 ⋮ 메뉴: pointer-coarse opacity-100로 터치 기기 상시 노출. ③ 헤더 한 줄(제목\|보드탭 스크롤\|툴바). ④ 계산기 CompactView에 √·x²·xʸ·eˣ·ln·log·π·e 추가(evaluate.ts 기존 지원). ⑤ 앱 아이콘(패널 마크) svg/apple-icon(PNG)/manifest. tsc·build 그린 |
| 2026-06-21 | KIS 토큰 영속 캐시 강건화 + 전체 종목 검색 + 연락처 CSV | lib/api/stock/{tokenStore,kisClient,kisWebSocket,symbols,krx-catalog.generated}, scripts/gen-krx-catalog.mjs, contacts/{csv,ContactManager} | ① KIS 토큰 캐시: tmpdir→프로젝트 .cache 고정 경로(mkdir 보장), 발급 시 console.warn 진단 로그(외부 동일 appkey 사용 시 충돌 안내), 캐시 쓰기 실패 가시화, 접속키 self-heal 무효화 제거(churn 방지). ② 전체 종목 검색: KRX KIND 코스피+코스닥 2,639종목 카탈로그 자동생성(gen-krx-catalog.mjs)→searchKrStocks가 전체 검색, 큐레이션은 빈검색 추천으로 유지. ③ 연락처 CSV 가져오기(구글/아웃룩/네이버) 추가 + 안내문구 보강(아이폰 vCard·구글 CSV). tsc·build 그린 |
| 2026-06-21 | 주식 새로고침/시각 · 드롭다운 포털 · 날씨 지역/예보 | stock/{useStockQuotes,RefreshBar,Compact/Expanded,format}, ui/primitives, ConfigDialog, weather/{types,CompactView,ConfigEditor} | ① 주식 위젯 새로고침 버튼+결과 시각(useStockQuotes에 refresh()/lastUpdated, RefreshBar; 캐시 토큰 재사용→발급 없음). ② **KIS 토큰 캐시 실측 검증**: 새 서버 프로세스가 .cache 파일 토큰 재사용→발급 0회 확인(증상은 옛 코드 서버 미재시작 탓; 재시작 필요). ③ ⋮ 메뉴(속성) 위젯 overflow-hidden에 잘리던 문제 → DropdownMenuContent를 body로 portal+고정좌표+상하 flip(화면 안에 전체 표시), ConfigDialog 60→75dvh. ④ 날씨: 지역 선택 33개로 확대(3열), 타일 표시 내용 현재/시간별/주간 선택(WeatherConfig.view; 데이터는 기존 hourly/daily 활용). tsc·build 그린 |
| 2026-06-21 | 날씨 위치 주소/장소 검색(동·골프장) | lib/api/geocodeClient.ts, app/api/geocode/route.ts, weather/ConfigEditor | 지오코딩 검색 추가: 동 단위 주소·골프장·랜드마크 이름으로 위치 지정. /api/geocode(서버 전용, KAKAO_REST_API_KEY 있으면 카카오 우선 → 골프장/동 정확도↑, 없으면 무료 Nominatim/OSM 폴백). 날씨 ConfigEditor에 검색창+결과목록. Nominatim 실측: 역삼동·정자동·스카이72 OK(일부 골프장은 카카오 키 필요). tsc·build 그린 |
| 2026-06-21 | 위젯 최소높이↓ · 클립보드 위젯 · 날씨/환율 새로고침 · 팔레트 드롭 | widgets/*/index.ts(minSize.h=1), widgets/clipboard/*, registry, widgets/shared/RefreshBar, weather/{useWeather,CompactView}, fx/CompactView, GridCanvas | ① 전 위젯 minSize.h=1(메모 수준 축소 허용). ② 신규 clipboard 위젯: 페이지 copy 자동기록+수동추가(readText/붙여넣기), 클릭 재복사, localStorage 인스턴스별(OS 전역감시 불가 명시). ③ 공용 RefreshBar로 날씨·환율 타일에 새로고침+갱신시각; 날씨 자동주기 10→30분. ④ 팔레트 드롭: 그리드에 min-h-[70vh] 부여해 빈 공간 드롭 가능(옆에만 붙던 문제 해소). **calendar 구글연결 'provider is not enabled'는 코드 아닌 Supabase 대시보드에서 Google provider 활성화 필요(설정 안내).** tsc·build 그린 |
| 2026-06-21 | 리사이즈 '다른 앱 밀기' · 환율 전일대비 | GridCanvas(compactor/resize/PushPrompt), fx/{useFxRates,FxRateRow,format}, fxClient, output/api-shapes(FxRates.changePct) | ① 리사이즈 충돌 처리: 컴팩터가 리사이즈 중 타일을 이웃에 닿도록 클램프(겹침 방지), 늘려서 막히면 '다른 앱 밀기' 버튼(dwell 450ms, 타일 SE에 portal) → 클릭 시 desired 크기로 키우고 겹치는 타일을 아래로 밀기(pushResolveDown, x 보존). 드래그/드롭/줌 동작은 불변. ② 환율 전일 대비 증감: Frankfurter 타임시리즈로 최신+전일 1콜 조회→changePct 계산(api-shapes에 옵션 필드 추가), FxRateRow에 +/−%·전일대비 표시, 방향색은 changePct 기준. tsc·build 그린 |
| 2026-06-21 | 주식갱신·타일입체·헤더↓·팔레트드롭·클립보드자동·날씨컬러·위젯색·메모인라인·제목변경·환율원화 | 다수(stock/*, WidgetFrame, GridCanvas, clipboard/*, weather/format, widgetColor/widgetTitle, memo, fx/*, widgets/persistence) | KIS 구독에 15s REST 폴링(시세 멈춤 해소)·fallback range=5d(등락 정확). 타일 shadow+ring 입체감, 헤더 py-1. 팔레트 드롭이 커서 셀에 배치. 클립보드 포커스 복귀 시 OS 클립보드 자동 기록. 날씨 아이콘 컬러. **전 위젯 ⋮ 색 지정(파스텔10, color-mix 틴트)**. 메모 타일 인라인 편집(WidgetPersistenceProvider). 헤더 제목 더블클릭 변경(localStorage). 환율 원화기준(1USD/100JPY→원). 테마는 기존 기기별 유지 확인 |
| 2026-06-21 | KIS 토큰 Supabase 공유캐시 · 메모장(비번) · 격자 2배 해상도 | tokenStore(+supabase admin), essential-info/*(메모장 비번), persistence/types(gv), GridCanvas/grid/CanvasShell COLS·ROW, 전 위젯 index 사이즈 ×2 | ① 토큰 캐시 메모리→**Supabase Storage(pb-cache 공유·영속)**→파일 3계층(전역 24h 1회 발급, 실측 검증). ② 필수정보→**'메모장(비번설정)'**: 자유 메모+SHA-256 비번 잠금+미사용 자동 재잠금(1~60분), 구 items 자동 마이그레이션. ③ **격자 해상도 2배**(열 12→24·행 96→48·여백 12→6로 화면크기 동일·스텝 미세화). 저장 레이아웃은 **위젯별 gv 마커**로 읽을 때 v1→×2 스케일·쓸 때 gv:2 스탬프(멱등·무스키마·다기기 안전). 실DB 12위젯 마이그레이션·멱등·픽셀불변(Δ≤6px) 테스트 통과 |
| 2026-07-10 | 제목 변경 발견성(⋮ 메뉴·연필) + 타입 툴팁 · 이미지슬라이드 타일 직접 추가·다운로드 | WidgetFrame, WidgetMenu, GridCanvas, CanvasShell, lib/utils/widgetTitle(rename 버스), image-slider/{CompactView,ExpandedView,ImageManager,imageFiles} | 사용자 요청 3건: ① 위젯 제목 변경을 ⋮ 메뉴 "제목 변경" + 제목 옆 호버 연필로 노출(기존 더블클릭 유지, requestRename→editSignal 버스), 헤더 아이콘에 "{원래이름} 위젯" 툴팁 — 제목을 바꿔도 종류 식별. ② 이미지슬라이드 빈 타일 클릭=파일 선택 즉시 추가(편집과 동일 filesToSlides 파이프라인, useSaveWidgetConfig 영속) + 이미지 있을 때 우상단 호버 [추가] 버튼. ③ 현재 슬라이드 [다운로드] 버튼(타일 오버레이+전체보기, dataURL→blob 저장·원격 CORS 실패 시 새 탭 폴백). 공용 로직은 imageFiles.ts로 분리(ImageManager 중복 제거). tsc·build 그린(런타임은 로그인 게이트로 미검증) |
| 2026-06-21 | 날씨 주간 가로 · 드래그 멈춤 밀기 · 환율 금액 · 모바일 편집(기기별) · 전체버튼 · 윤곽선 | weather/{CompactView,ExpandedView}, fx/{useFxRates,FxRateRow,format}, GridCanvas, WidgetFrame, CanvasShell, lib/utils/mobileLayout(신규) | ① **날씨 주간 가로 나열**(월·화·수…): DailyBody·Expanded 주간을 세로 리스트→가로 일별 컬럼 스트립(시간별과 동일 스타일). ② **드래그-멈춤 밀기**(lg): 앱을 끌어 점유 공간 위에서 ~500ms 멈추면 포인터 셀 기준 그 앱+아래로 연속된 앱들을 pushBelowRect로 아래로 밀기(드래그된 타일 고정, x 보존, 빈 공간이면 무동작). ③ **환율 전일대비 금액(원)**: changePct+현재 krw로 Δ금액 역산(changeAbs), FxRateRow를 %→금액 표기로 교체(formatFxAmount). ④ **모바일/태블릿 편집 활성**: 전 브레이크포인트 드래그·리사이즈 허용, md/sm 편집은 **기기별 localStorage**(pb:mlayout:{board}:{bp})로 영속→DB lg 배치 불변(toDeviceLayout: 저장셀+신규 flow-pack). 팔레트 드롭은 lg 전용 유지. ⑤ **'전체' 버튼**: WidgetFrame 헤더에 전체 버튼→기존 FocusOverlay(자세히)·X 닫기·뒤로가기=오버레이만 닫힘(useBackStack) 재사용. ⑥ **윤곽선 강화**: 타일 border 2px + color-mix 고대비 + ring 진하게. tsc·build·dev부팅 그린 |
| 2026-06-21 | 외출옷 2단 레이아웃·일러스트 가변축소·시간대 유지 + 드래그밀기 둔감화 | outfit/{CompactView,ExpandedView,OutfitIllustration,usePersistedPeriod(신규)}, GridCanvas | ① **2단 레이아웃**: 좌(날씨 위 + 의상 설명 목록)·우(일러스트)로 공간 분리, 좁으면 세로 스택. ② **일러스트 가변축소**: OutfitIllustration `fit` 모드(부모 박스 너비·높이로 비율유지 축소)→타일 줄이면 그림도 함께 작아짐. ③ **시간대 유지**: 선택 시간대를 인스턴스별 localStorage(usePersistedPeriod)로 타일↔'전체' 공유, '전체' 열어도 현재로 안 바뀜(설정 기본값 변경 시에만 ref가드 동기화). ④ **드래그 밀기 둔감화**: 포인터 픽셀이동(>8px)마다 dwell 재무장+1200ms로 상향→움직이는 중엔 안 밀리고 멈춰서 오래 있어야 작동. tsc·build 그린 |
| 2026-06-21 | 외출옷 위젯 보강(시간대 상단·기본 세로크게·위치 편집 전면) + **위젯 탭 간 드래그 이동** | outfit/{CompactView,ExpandedView,ConfigEditor,index}, usePersistence(moveInstanceToBoard), GridCanvas(탭 히트테스트), BoardTabs(data-pb-board-tab), CanvasShell | ① 외출옷: 시간대 버튼 **상단**으로, 기본 크기 w8×**h16**(모든 내용 보이게 세로 길게), 일러스트 maxWidth↑. ② ConfigEditor **위치 편집 전면**(현재위치 표시+주소·장소 검색/현재위치/지역/직접입력을 최상단, 기존 날씨 위젯 방법 재사용). ③ **위젯을 상단 탭으로 드래그→해당 보드로 이동**: 드래그 중 포인터가 탭 rect 위면 하이라이트, 드롭 시 moveInstanceToBoard(소스 제거+타깃 하단 배치, 같은 id로 dashboard_id만 재귀속)→타깃 탭 자동 선택. 트랜스퍼 직후 RGL 잔류 onLayoutChange 1회 가드. tsc·build 그린 |
| 2026-06-21 | 신규 위젯 **외출옷 추천**(Weather_Outfit_Suggestion 이식) | components/widgets/outfit/*(신규), registry, public/outfit/characters/*(webp 39장) | 원본 앱의 **외출옷 추천+캐릭터 일러스트**만 개별 위젯으로 이식. ① 추천 로직(recommender·rules·categories·types) 원본 그대로 이식, illustration(characterIllust·WeatherCharBg·OutfitIllustration)도 동일 합성(체감구간·날씨 슬롯 캐릭터 webp + 날씨 데코 SVG `mix-blend-mode:darken`). ② **장소명 검색**(/api/geocode 재사용)·**시간대 선택**(OUTFIT_PERIODS 7구간, 타일/전체에서 즉시 전환)·기본 활동 **산책=외출**(urban_walk). ③ 설정(ConfigEditor): 성별·활동(8종)·**체감온도 보정(추위/더위 민감도 ±3°C)**·기본 시간대. ④ PaneBoard /api/weather(Open-Meteo) → OutfitInput 매핑(condition→pty/sky코드, pop→강수 근사, UV는 시간·계절 근사, 미세먼지 미수신). 32개 슬롯×성별 에셋 존재 검증·tsc·build 그린. (미세먼지/오존 액세서리·경고는 데이터 없어 미반영) |
| 2026-06-22 | 타일 목록 전체보기(스크롤) + 주식 시간외·해외지수 반영 | contacts/todo/news/card-usage CompactView, world-clock/dday CompactView, lib/api/stock/{fallbackClient,kisClient} | ① **타일에서 모든 항목 스크롤 표시**: 연락처·할일은 maxRows `slice`+"+N개 더" 잘림 제거→전체 렌더(넘치면 `overflow-y-auto`로 스크롤), 뉴스 `slice(0,8)`·카드사용 `slice(0,4)` 캡 제거. ② **세계시계·D-Day `justify-center` 스크롤 잘림 수정**: 스크롤 div + 내부 `<ul className="my-auto">` 구조로 — 항목 적으면 세로 가운데, 넘치면 위에서부터 스크롤(상단 잘림 해소). ③ **주식 시간외(pre/post) 반영**: Yahoo fallback이 `includePrePost=true` 2m 분봉의 마지막 체결가를 정규장 시각 이후면 채택(지수는 시간외 없어 스킵)→등락은 전일종가 대비라 시간외 상승/하락 자연 반영(AAPL 296.5 시간외가 확인). ④ **다우·S&P·나스닥 미반영 수정**: KIS 활성 시 해외지수가 `null`→미표시였음. kisClient가 US 인덱스를 `fetchFallbackQuotes`로 위임→최신/최종 종가 표시(errors:[] 실측). tsc 그린, fallback 경로 dev 런타임 검증. (해외지수 行은 provider=kis라 '근사치' 배지 미표시 — 값은 정확) ⑤ **Kakao 키 변수 호환**: geocodeClient가 `KAKAO_LOCAL_API_KEY`(우선)·`KAKAO_REST_API_KEY`(레거시) 둘 다 인식(둘 다 동일 Kakao REST 키). 사용자 키로 Local API 실측(강남역 3건). |
| 2026-06-22 | 국내주식 시간외 단일가 반영(KIS) + 클립보드 기록 영속 보강 | lib/api/stock/kisClient.ts, components/widgets/clipboard/useClipboardHistory.ts | ① **시간외 단일가**: KIS 국내주식 시간외현재가(`inquire-overtime-price`, tr_id `FHPST02300000`, 필드 `ovtm_untp_prpr/prdy_vrss/prdy_vrss_sign/prdy_ctrt/vol` — 공식 koreainvestment 샘플로 확정·실응답 검증)를 **저녁·야간(KST 16시 이후 또는 09시 이전)** 에만 조회해 정규장 가격 위에 덧입힘. 등락은 전일종가 대비라 위젯 기존 기준과 일치. **`ovtm_untp_vol>0`(실제 시간외 체결)일 때만 반영** — 거래 없으면 KIS가 ovtm_untp_prpr를 기준가로 채우고 등락 0을 줘서 정규장 실제 등락이 0으로 덮이는 버그가 있어, 그 경우 정규장 시세 유지. 시간외 조회 실패는 try/catch로 격리(정규장 시세 보존). 실측: 시간외 미거래 시 005930 −0.14%·000660 +5.61% 정규장 등락 보존, 다우 fallback 동시 정상. ② **클립보드 영속 보강**: instanceId(=DB id, 안정적)·localStorage라 창 종료엔 이미 보존되나, `setItem` 용량초과를 조용히 삼켜 최근 기록이 디스크 미반영→새로고침 시 유실되던 무음 경로 수정 — 실패 시 **오래된 항목부터 줄여 재시도**(최신 기록 우선 보존), 항목당 텍스트 10만자 캡으로 단일 거대 붙여넣기의 quota 오염 방지. (가드레일: 비밀번호 등 민감값 우려로 클라우드 저장은 하지 않고 기기 로컬 유지 — 사용자 선택) tsc·build 그린 |
| 2026-06-22 | 시간외 단일가 재검증 도구 + 로컬 스케줄 | scripts/verify-overtime.mjs(신규) | KIS 시간외 반영을 **실거래 시간대(평일 16~18시 KST)** 에 재검증하는 자체완결 스크립트(.env.local KIS키 직접 사용, 파일캐시 토큰 재사용, 읽기전용 시세만). 5개 유동 종목의 정규장 vs 시간외(prpr/vol/등락)를 표로 출력하고 `vol>0`+창 조건으로 위젯 반영 여부를 판정(PASS/유지). 순차호출+1회 재시도로 KIS 초당 한도 회피. **클라우드 라우트는 로컬 KIS키 접근 불가**라 검증 불가 → 로컬 Windows 예약작업 `PaneBoard-Overtime-Verify`(화 16:40·17:20 KST)로 자동 실행, 결과는 `.cache/overtime-verify.log`(gitignored)에 append. 강제 실행으로 동작 확인(현재는 18시 이후라 vol 0 → 정규장 등락 보존 정상 출력). |
| 2026-06-23 | **설정 버튼**(헤더, 테마 우측) — 팔레트 앱 표시 + 비밀번호 변경 | components/canvas/{Toolbar,SettingsDialog(신규),CanvasShell}, lib/utils/paletteVisibility(신규) | ① Toolbar 테마 토글 우측에 **설정(기어) 버튼** 추가. ② **SettingsDialog**(탭 2: 앱 표시·계정). **앱 표시**: 전 위젯 체크박스로 팔레트 노출 토글(모두표시/숨기기)→`pb:hidden-widgets`(기기별 localStorage, useSyncExternalStore 패턴). CanvasShell이 hidden을 빼 `paletteRegistry` 생성해 WidgetPalette에만 전달(캔버스/포커스/편집은 전체 registry라 **이미 배치한 인스턴스는 그대로 동작**, 새 위젯은 기본 표시=hidden에 없으면). **계정**: 로그인 이메일 표시 + **비밀번호 변경**(supabase.auth.updateUser({password}), 8자+확인 검증, 표시토글). 인증은 이미 이메일+비밀번호(매직링크 대체)라 적용 가능. 모달은 ConfigDialog 패턴(스크림+모바일 시트, Esc/✕/스크림 닫기). 실브라우저 검증: 노트 숨기기→localStorage["note"]·팔레트에서 제거, 계정 탭 비번폼 렌더. tsc·build 그린. |
| 2026-06-23 | 일출/일몰·달 지역선택 **국내/해외 탭** | shared/LocationPicker, sun-moon/ConfigEditor | LocationPicker에 `cityGroups`(탭) 지원 추가 — 그룹 배열을 받으면 탭 버튼 렌더, 첫 탭이 기본. 상수 정리: `KOREA_CITIES`(국내 19) 신설, `LOCATION_CITIES`=국내+도쿄·뉴욕(기존 기본 유지), `WORLD_CITIES`에 도쿄·뉴욕 포함(해외 탭). sun-moon이 `cityGroups=[국내(KOREA_CITIES,기본),해외(WORLD_CITIES)]` 전달. air-quality는 단일 목록 그대로. 실브라우저 검증: 국내 기본(서울O·런던X), 해외 클릭(런던·시드니O·서울X). tsc·build 그린. |
| 2026-06-23 | 차량 **이벤트 기록 입력**으로 전환 + 일출/일몰·달 **세계 도시** | vehicle/{CompactView,VehicleManager,ExpandedView}, shared/LocationPicker, sun-moon/ConfigEditor | ① **차량 입력 형태 변경**: 타일 빠른추가를 주유전용(주행km/L/₩)→**이벤트 기록**(내용 예:오일교환·주행거리 + 일자 + 필요시 주행거리km)로 교체, maintLogs에 기록(주행거리는 선택, 상단 '주행거리' 통계에 반영). ConfigEditor 정비행에 **주행거리(선택)** 입력 추가·섹션명 '정비·이벤트 기록'. ExpandedView 정비목록에 주행거리 표시. ② **LocationPicker `cities` prop** 추가(기본=국내) + `WORLD_CITIES` 28개(런던·파리·뉴욕·시드니·호놀룰루 등) export. 일출/일몰·달 ConfigEditor가 국내+세계 도시 합본 전달(air-quality는 기존 국내 유지). 실브라우저 검증: 차량 '오일교환'+주행 54321→maintLogs 저장, sun-moon에 런던·시드니·호놀룰루 표시. tsc·build 그린. |
| 2026-06-23 | 구독 빠른추가에 **결제일자·화폐단위·주기** 필드 | components/widgets/subscriptions/CompactView(SubQuickAdd) | 타일 하단 ‘구독 추가’ 폼에 결제일자(date)·화폐단위(KRW/USD/EUR/JPY)·주기(주간/월간/연간, **기본 월간**) 추가. 기존엔 이름+금액만 받고 통화=baseCurrency·주기=월간·날짜=오늘로 고정됐던 것을 폼에서 직접 선택하도록 확장. 연속 추가를 위해 주기·통화·날짜는 추가 후 유지(이름·금액만 비움). tsc·build 그린. |
| 2026-06-23 | 노트 **이미지 크기 조절** | components/widgets/note/{ImageResizer(신규),NoteEditor} | 본문 이미지 클릭 시 선택 오버레이(ring) + **코너 드래그 핸들**(pointer 드래그→inline width 설정, height auto로 비율 유지) + **크기 프리셋**(작게40%/중간70%/크게100%/원본). width는 살균기가 보존하는 inline style이라 config로 왕복. 에디터를 relative 래퍼로 감싸 오버레이를 절대배치, 스크롤/리사이즈/드래그마다 재측정, 이미지가 뷰 밖이면 숨김. **실브라우저 검증**(임시 /note-test): 클릭→핸들 표시, 작게=282px, 드래그 +120px→402px, 저장본 width 보존, 콘솔에러 0. tsc·build 그린. |
| 2026-06-23 | **신규 위젯 노트**(리치 텍스트 + 이미지 + 표 + 파일첨부) | components/widgets/note/*(신규 11파일), registry | 강의 기록용 다기능 노트. **서식**: 굵게·기울임·밑줄·취소선·글자색·형광펜·글자크기(px 프리셋)·제목(H2/H3)·글머리/번호목록·정렬·인용·구분선·링크. **이미지**: 삽입/붙여넣기/드롭 → 캔버스 자동축소(≤1600px) data URL 인라인. **표**: NxM 삽입 + 행/열 추가·삭제(캐럿 셀 기준). **파일첨부**: 인라인 base64(≤5MB/파일, 사용자 선택), 다운로드/삭제. **엔진**: 의존성 0 — `contentEditable`+execCommand(styleWithCSS) + Selection/Range 표 편집. **보안**: 붙여넣기/드롭 HTML을 allowlist sanitizer로 살균(script/iframe/on*·javascript: 제거, style 속성 필터) 후 저장·렌더(2중). 언컨트롤드 에디터(메모 패턴)로 캐럿 보존·디바운스 저장. CompactView=읽기전용 미리보기(SSR 하이드레이션 가드), ExpandedView=전체 에디터(제목+툴바+본문+첨부). 첨부 저장방식은 사용자에 확인(인라인 채택). **실브라우저 검증**(임시 /note-test): 굵게→span style, 글자색 rgb(239,68,68), 형광펜 bg, 표 2×2 삽입·저장보존, 살균(script/onerror/javascript: 전부 제거·안전텍스트 보존) 모두 PASS. tsc·next build 그린, palette-test 표시·콘솔에러 0. |
| 2026-06-23 | **신규 위젯 7종**(대기질·구독관리·단위변환·타이머·일출일몰달·번역기·차량관리) | components/widgets/{air-quality,subscriptions,unit-converter,timer,sun-moon,translate,vehicle}/*(신규), shared/LocationPicker.tsx(신규), app/api/{air-quality,translate}/route.ts(신규), lib/api/{airQualityClient,translateClient}.ts(신규), output/api-shapes(AirQuality·Translate), registry, widget-manifest | ① **대기질**(poll, 30분): Open-Meteo Air-Quality 키리스 → PM2.5/PM10/O₃/NO₂/SO₂/CO+EU/US AQI, 환경부 4등급(좋음·보통·나쁨·매우나쁨, 이모지+라벨+색). 실측 PM2.5 16.9·PM10 18.7·euAqi 66. ② **구독관리**(static/config): 정기결제+다음결제일(앵커 롤포워드)+월/연 합계(추정환율), QuickAdd. ③ **단위변환**(static): 길이·무게·온도·넓이·부피·속도·시간·데이터+한국단위(평·근·돈·되), 타일 즉시변환·확장 전체환산표. ④ **타이머**(static/local): 타이머/스톱워치/뽀모도로, 절대시각 기반(드리프트無)·런타임 instance별 localStorage·종료 비프(WebAudio)+알림(선택)·진행링. ⑤ **일출일몰/달**(static): 순수 로컬 천문계산(NOAA 일출일몰+삭망월 달위상), API無 오프라인 동작. ⑥ **번역기**(on-demand): /api/translate 키리스(Google gtx→MyMemory), DEEPL_API_KEY 시 DeepL, auto감지·스왑·복사. 실측 "Hello world"→"안녕하세요 세상". ⑦ **차량관리**(static/config): 주유(연비 km/L 자동)·정비·갱신만기(D-day), QuickAdd. 공용 **LocationPicker**(검색/현재위치/지역/직접입력)로 위치 위젯 통일. tsc·next build 그린, 두 신규 라우트 실서버 200 검증, palette-test 7종 표시·콘솔에러 0. (config 기반 위젯은 새 DB테이블 없이 pb_widgets jsonb 저장) |
| 2026-06-24 | 날씨 시간별 시간대 버그 수정 + 현재위치 동(洞) 반영 | lib/api/weatherClient.ts, lib/api/geocodeClient.ts, app/api/geocode/route.ts, weather/ConfigEditor | ① **시간별 날씨 '너무 낮음' 버그**: Open-Meteo `timezone=auto`는 오프셋 없는 로컬 시각 문자열을 반환 → `Date.parse`가 **서버 TZ**(Vercel=UTC)로 해석해 시간별 ts가 +9h 밀려 새벽/야간(낮은 기온) 구간이 현재처럼 표시됐음. 응답의 `utc_offset_seconds`로 참 UTC epoch 계산하는 `localTimeToEpoch` 도입(서버 위치 무관). 실측: UTC 서버 가정 시 13시 KST→옛 코드 22시(밤)로 오인, 신 코드 13시 정확. ② **현재 위치 동 반영**: `useCurrentLocation`이 라벨을 '현재 위치'로만 두던 것을 → /api/geocode **reverse**(`?lat=&lon=`) 추가(Kakao coord2regioncode 우선·행정동, 무키 시 Nominatim reverse)로 실제 동명 라벨링. 실측 Nominatim: 역삼동→역삼1동·정자동·태평로1가. tsc·next build 그린. |
| 2026-06-24 | **신규 위젯 비밀번호 금고**(사이트·아이디·비밀번호·비고, 마스터 비번 잠금) | components/widgets/credentials/*(신규: types·useVault·CredentialsBody·Compact/Expanded/Config·index), registry | 웹사이트 로그인 관리 위젯. **가드레일(비밀번호 저장 금지) 준수**: 항목 전체를 **WebCrypto AES-GCM**으로 암호화(키=마스터 비번 PBKDF2-150k)해 **localStorage(기기 로컬)에만** 저장 — 평문 비번 어디에도 미저장, config(Supabase) 미경유. 상태기계 setup/locked/unlocked, 미사용 자동 재잠금(1~60분), 비번 reveal·필드별 복사, 비고 노트, 마스터 비번 변경, 분실 시 금고 초기화. SSR 하이드레이션 가드(localStorage 읽기 mount 지연+ready 게이트). **검증**: crypto 라운드트립(평문 미포함·정답 복호화·오답 null) + 실브라우저(설정→추가→잠금→리로드→복호화 복원, 오답 거부, 콘솔에러 0). tsc·next build 그린. |
| 2026-06-25 | 해외/지수 등락 **전일종가 기준 수정** + 주식 행 **외부정보 링크** + 외출옷 시간대 **현재→미래(내일) 롤링** | lib/api/stock/{fallbackClient,format,QuoteRow}, lib/api/weatherClient, outfit/{constants,weatherMap,useSelectedPeriod,PeriodPicker,Compact/Expanded} | ① **다우·S&P·나스닥 등락 오표시 수정(부호까지 반전)**: Yahoo 지수 응답이 `meta.previousClose`를 빠뜨리자 코드가 `chartPreviousClose`(=range 5d **시작 직전 봉**, 약 5거래일 전 종가)로 폴백→전일대비가 폭·부호 모두 틀어짐(다우 −0.29%↓로 표시됐으나 실제 +0.35%↑). **일봉 시계열의 직전 세션 종가**(`previousDailyClose`)를 등락 기준으로 — 해당 국가 시장 '전일종가'(Naver/Google과 동일). 우선순위 previousClose→일봉 직전 세션→chartPreviousClose. KIS 활성 시 US지수는 `fetchFallbackQuotes` 위임이라 동일 적용. 실측: 다우 +182.06(+0.35%)↑로 정상. ② **시세/회사명 클릭→외부 주식정보**: QuoteRow를 `<a target=_blank>`로, `quoteInfoUrl`이 국내종목→네이버 종목메인, 코스피/코스닥→네이버 지수, 다우/S&P/나스닥→네이버 월드(DJI@DJI·SPI@SPX·NAS@IXIC, 실측 200·콘텐츠 확인). ③ **외출옷 시간대 롤링**: 고정 7구간(오늘 새벽~밤)→`buildForwardPeriods`로 **현재 구간부터 미래로**(오늘 남은 구간+내일, 예보 한도 47h 내). 키 인코딩 오늘=baseId·내일=`id@1`, weatherMap이 앵커 날짜+시각으로 내일 예보 정확 매칭, 지나간 고정선택은 '지금'으로 자가복원. 예보 보관 `HOURLY_KEEP` 24→48h(오후에도 내일 저녁 확보, 날씨 위젯은 slice(0,12)라 무영향). 시뮬: 11:30→10~12시부터, 22:30→21~23시+내일 전구간. tsc·next build 그린. |
| 2026-06-27 | 노트·메모 **글자색 테마 적응 + 색 변경(자동·프리셋·임의색)** | note/{types,Toolbar,ConfigEditor,CompactView}, memo/{types,ConfigEditor,CompactView,ExpandedView} | 본문 기본색은 이미 `text-foreground`(라이트=검정·다크=흰색 자동). ① **노트 '글자 색' 팝오버 확장**: `자동(테마)`=`color: var(--foreground)`(테마 적응) + 프리셋 8색 + **임의색 `<input type=color>`**. 네이티브 컬러 인풋은 포커스를 가져가 에디터 선택영역을 잃으므로 팝오버 오픈 시 Range 스냅샷→적용 직전 복원(`ColorMenu`). ② **메모 글자색 신규**: `MemoConfig.textColor?`(미설정=자동/테마) + ConfigEditor에 자동·프리셋·임의색, Compact/Expanded에 inline 적용(미설정 시 text-foreground). **실브라우저 검증**(임시 /note-test): 노트 자동→`color:var(--foreground)`, 프리셋→`rgb(239,68,68)`, 임의색 #123456→`rgb(18,52,86)`(선택영역 보존), 메모 프리셋→textColor `#10b981`·자동→null. tsc·next build 그린. |
| 2026-06-27 | **모바일 공유 받기**(Web Share Target → 지정 노트에 저장) + 노트 속성 '공유 받기'(단일 대상) | app/manifest.ts(share_target), app/share/{route.ts,done/page.tsx}(신규), proxy.ts, note/{types,ConfigEditor,CompactView}, lib/widgets/persistence.tsx, lib/persistence/usePersistence, components/canvas/{ConfigDialog,CanvasShell}, lib/widgets/contract(+output) | 표준 **Web Share Target**(PWA manifest `share_target`, GET title/text/url, SW 불필요)로 모바일 공유 시트→앱이 받음. ① **`/share` 라우트 핸들러**: 인증(소유자 게이트)→`config.shareTarget===true` 노트 탐색(없으면 가장 오래된 노트=첫 노트 폴백)→공유 내용을 **안전 HTML 블록**으로 append(평문 escape + http(s) 링크만 — `<script>`·`onerror`·`javascript:` 무력화 실측)→대상 노트 self-heal→`/share/done` 확인 페이지. ② **노트 속성 `shareTarget`**: ConfigEditor 토글, **단일 불변식** — 한 노트에서 켜면 다른 모든 노트는 해제(`usePersistence.setShareTargetNote` 교차 보드, 위젯 persistence 컨텍스트로 노출), **첫 노트는 기본 ON**(addInstance: 기존 대상 없을 때만, 복제/붙여넣기 노트는 플래그 strip). 타일에 공유 대상 아이콘 표시. `ConfigEditorProps`에 옵션 `instanceId` 추가(contract+output 동기화). ③ proxy: `/share` protected→미인증 공유는 `/login?next=/share?…`로 파라미터 보존 후 처리(실측 307 리다이렉트 확인). **한계**: iOS Safari는 Web Share Target 미지원(Android/Chrome PWA 기준), 동시 데스크톱 세션 열려 있으면 낙관적 flush 경합 가능(콜드 오픈 정상). tsc·next build 그린, 매니페스트 share_target·미인증 리다이렉트·HTML 빌더 XSS 안전 검증. |
| 2026-06-28 | 드래그 **가장자리 자동 스크롤**(상·하단) + **자동정렬 = 현재 배열 공백 제거(flow-pack)** + 날씨 **소숫점 1자리·가로/세로 선택·어제 대비** | lib/utils/dragAutoScroll.ts(신규), components/canvas/{GridCanvas,CanvasShell}, weather/{format,types,CompactView,ExpandedView,ConfigEditor}, lib/api/weatherClient, output/api-shapes(WeatherCurrent.tempYesterday) | ① **드래그 자동 스크롤**: 위젯을 위로 끌어 상단 sticky 헤더에 닿으면 페이지가 위로(캔버스가 아래로) 스크롤돼 더 위 칸에 배치 가능, 아래 경계에 닿으면 아래로 스크롤. RGL(react-draggable)은 mousemove에서만 위치를 재계산하므로 — 커서가 멈춘 채 스크롤만 하면 타일이 안 따라옴 — rAF 루프로 `window.scrollBy` + **직전 커서 좌표로 합성 `mousemove` 디스패치**해 타일이 스크롤된 그리드를 추종(offsetParent rect는 라이브라 같은 clientY라도 그리드 행이 위로 이동). 상단 트리거 영역은 `data-pb-sticky-header` 높이 측정으로 헤더에 정합. onDragStart→start, onDrag→onPointer(e), onDragStop→stop. ② **자동정렬(정리하기) 교체**: 세로 공백만 없애던 `verticalCompactor`→**현재 위치 읽기 순서(y→x)로 정렬 후 firstFreeSlot flow-pack**(폭/높이 보존)으로 가로·세로 공백 모두 제거. 만든 순서가 아니라 화면에 보이는 현재 배열 기준. ③ **날씨**: `formatTemp` `Math.round`→`toFixed(1)`(현재·시간별·주간·체감 전부 소숫점 1자리), 큰 현재 기온 폰트 한 단계 축소(타일 text-3xl→2xl·자세히 4xl→3xl). 시간별·주간 **가로/세로 방향 선택**(`WeatherConfig.hourlyLayout/dailyLayout`, 기본 가로, 타일·자세히 공통). **어제 대비**: Open-Meteo `past_days=1`로 어제 같은 시각(±90분) 기온→`WeatherCurrent.tempYesterday`→"어제보다 +N°"(오름 주황/내림 파랑), KMA 경로는 미제공(옵션). ConfigEditor 위치 변경 onChange를 `{...config}` 스프레드로 바꿔 신규 필드 보존. tsc·next build 그린, 어제대비 로직 실 Open-Meteo로 검증(같은 시각 매칭 22분 오차). |
| 2026-06-28 | 자동정렬 보강: **되돌리기 + 가로 빈공간 채우기 + 행 높이 맞춤(선택) + 정렬 애니메이션** | lib/utils/{arrange.ts,arrangeBaseline.ts}(신규), components/canvas/{CanvasShell,Toolbar}, lib/persistence/usePersistence(restoreBoardLayout), app/globals.css | ② 자동정렬을 flow-pack(masonry)→**행(row) 기반 배치기**(`arrangeLayout`)로 교체. 현재 위치 읽기 순서(y→x)로 한 행에 폭이 차도록 좌→우로 담고 넘치면 다음 행 = 공백 제거. ① **되돌리기**: 정렬 직전 lg 레이아웃 깊은 복사 스냅샷 → 토스트 `되돌리기` 액션이 `restoreBoardLayout(boardId, snapshot)`로 **그 보드** 복원(탭 바꿔도 정확). usePersistence에 `restoreBoardLayout`(특정 보드 레이아웃 교체) 추가. ② **가로 빈공간 채우기(justify, 상시)**: 행의 남은 칸을 위젯들에 라운드로빈으로 분배해 각 위젯 **maxW 한도까지** 넓혀 가로를 채움(전 위젯 maxSize.w=10~16이라 풀폭 폭주 없음 — 단독 위젯도 maxW까지만). ③ **행 높이 맞춤(baseline, 선택)**: Toolbar에 토글 아이콘(Rows3, 기기별 `pb:arrange-baseline`) — 켜면 한 행 위젯 높이를 그 행 최대치로 통일(들쭉날쭉 제거). ④ **정렬 애니메이션**: `.react-grid-item:not(.react-draggable-dragging):not(.resizing)`에 260ms cubic-bezier transition(transform/size) — 정렬·재배치만 매끄럽고 끌고 있는 타일은 RGL의 더 구체적 `transition:none`이 이겨 즉각 추종, reduced-motion 무력화. 정렬 완료 토스트(공백 제거·가로 채우기[·행 높이 맞춤]). tsc·next build 그린, arrange 알고리즘 포트 테스트(행 폭 합 24 정확·단독 위젯 maxW 클램프·세로 공백 제거·baseline 통일) 검증. |
| 2026-06-28 | 자동정렬 **기본=크기 유지(masonry)** 로 변경 + 가로 채우기를 선택 토글로 분리 | lib/utils/{arrange.ts,arrangeJustify.ts(신규)}, components/canvas/{CanvasShell,Toolbar} | 요구: 자동정렬이 기본적으로 위젯 크기를 바꾸지 말고, 상단 공백은 채울 것. ① **기본 동작 = 크기 유지 masonry**: justify·baseline 둘 다 OFF면 `arrangeLayout`이 `masonryPack`(현재 위치 y→x 순서로 각 위젯을 첫 빈 칸에 그 크기 그대로 담기)으로 동작 — **폭·높이 불변**, 상단/세로/가로 공백을 위로 끌어올려 메움(위젯은 이동만, 리사이즈 없음). ② **가로 채우기(justify)를 상시→선택 토글**로 분리(`pb:arrange-justify`, 기본 OFF) — 켤 때만 행 기반으로 폭을 maxW까지 넓힘. 행 높이 맞춤(baseline)도 기존대로 선택. Toolbar에 가로채우기 토글 아이콘(StretchHorizontal) 추가(자동정렬·행높이맞춤 옆). ③ 완료 토스트 설명을 적용 옵션에 맞게 동적 표기(기본 "공백 제거 (위젯 크기 유지)"). 되돌리기·정렬 애니메이션은 그대로. tsc·next build 그린, masonry 포트 테스트(상단 y=6 공백→min y=0·전 위젯 w/h 보존) 검증. |
| 2026-06-28 | 날씨 **실제 동(洞) 표시** + 자동정렬 옵션을 **서브메뉴**로 통합 | app/api/weather/route.ts, lib/api/geocodeClient(reverseGeocode), output/api-shapes(location.dong), weather/{types(isGenericWeatherLabel),CompactView,ExpandedView,ConfigEditor}, components/canvas/Toolbar | ① **날씨 동 세분화**: 위젯이 '서울'처럼 도시 라벨만 보이던 문제 → `/api/weather`가 좌표를 **역지오코딩(Kakao coord2regioncode 행정동)** 해 `location.dong`을 함께 반환(날씨 fetch와 **병렬**, best-effort·캐시 10분, KR 밖/실패 시 dong 없음). 타일·자세히는 라벨이 **대략적(도시·기본값·현재 위치)** 일 때 `dong`으로 더 세분화해 표시(`isGenericWeatherLabel`; "우리집" 등 커스텀 라벨은 유지). 날씨는 이미 좌표 기반이라 해당 동 날씨가 그대로 나옴. ConfigEditor 도시 선택도 좌표를 역지오코딩해 라벨을 동으로 보강. **실측(dev)**: 서울시청 좌표→`명동`, 역삼동 좌표→`역삼1동`, 뉴욕→dong 없음(라벨 유지). ② **자동정렬 서브메뉴**: 가로 채우기·행 높이 맞춤을 **툴바에 개별 버튼으로 늘리지 않고**(요구) 자동정렬 split 버튼의 캐럿(▾)이 여는 드롭다운 체크 항목으로 통합(`DropdownMenuItem` + `e.preventDefault()`로 메뉴 유지 토글), 본 버튼/'지금 정렬'은 현재 선택대로 실행. 향후 유사 옵션도 이 메뉴에 추가. tsc·next build 그린. |
| 2026-07-01 | 잠금 **절대 재잠금** + 외출옷 **기본=지금·시간 후 복귀** + 주식 **더블클릭** + 겹침 이동 **1초 dwell** + 이미지 슬라이드 **제목·세로 스크롤** | credentials/{useVault,ConfigEditor}, essential-info/{LockedMemo,types,ConfigEditor}, outfit/{useSelectedPeriod,types,ConfigEditor,Compact/Expanded}, stock/QuoteRow, canvas/GridCanvas, image-slider/{types,ImageManager,Compact/Expanded} | ① **잠금 위젯 절대 재잠금**(금고·메모장): 재잠금 타이머를 비활성 기반→**해제 시각부터 고정 `lockAfterMin` 절대 타이머**로 변경(요구). `armRelock`은 setup/unlock에서만, add/update/remove/편집·`touch`는 타이머를 리셋하지 않음(touch=no-op) → 사용 중이어도 정해진 시간 뒤 잠김. 앱 재시작 잠금은 기존대로(인메모리 키/상태 소실). ② **외출옷 기본=지금**: `useSelectedPeriod`가 config 기본 시간대 폴백 제거→**항상 auto('지금')** 기본, 비-auto 선택은 `pb:outfitPeriodAt:{id}` 타임스탬프 + **REVERT_MS(10분)** 경과 시 '지금'으로 자가 복귀(윈도우 이탈 복귀도 유지). `OutfitConfig.periodId`·ConfigEditor '기본 시간대' 섹션 제거. ③ **주식 더블클릭**: QuoteRow 단일 클릭 이동→`onClick` preventDefault·`onDoubleClick`(+Enter)로 이동(Ctrl/⌘/Shift·가운데클릭은 기본 새탭 유지). ④ **겹침 이동 1초 dwell**: GridCanvas onDragTick의 즉시 swap 대상 지정→**같은 타일 위 ~1초(SWAP_DWELL_MS=1000) 머물러야** 대상 확정+하이라이트(후보 변경 시 타이머 재시작, 탭 이동/드래그 종료 시 정리). ⑤ **이미지 슬라이드**: `ImageSliderConfig.title` 추가(ImageManager 상단 입력), 타일·전체에 제목 표시. 이미지 `object-cover/contain`→**폭 맞춤+세로 스크롤**(`overflow-y-auto`+`w-full h-auto`, `data-pb-no-drag`)로 큰 이미지 상하 확인 가능. tsc·next build 그린. |
| 2026-07-01 | **신규 위젯 지인 일정 정리**(circle-schedule) — 카카오톡 텍스트 → 약속 추출(LLM) + 구분별 정리 | supabase/migrations/20260701120001_pb_circle_schedule.sql(신규), output/types/database.ts, output/api-shapes.ts, lib/api/circleScheduleClient.ts(신규), app/api/circle-schedule/extract/route.ts(신규), components/widgets/circle-schedule/*(신규 7파일), registry | 카카오톡 대화에서 약속을 **한 문장**(+시간은 문장 뒤 괄호)으로 추출해 **대상(구분: 가족·친구 등)별**로 정리·확인하는 위젯. ① **전용 테이블**(사용자 선택): `pb_circle_targets`(구분, `email`은 향후 공유용 스키마만·UI 미노출)·`pb_circle_appointments`(약속, `when_at` 정렬용·미지정 target 허용) + **RLS**(`auth.uid()=user_id`, 자식은 대상 소유까지 with-check). `supabase db push`로 라이브 적용 확인(migration list 로컬=원격). types/database.ts·api-shapes(Extract·CircleTarget·CircleAppointment zod) 동기화. ② **LLM 추출**: `lib/api/circleScheduleClient`(SERVER-ONLY)가 `ANTHROPIC_API_KEY`로 `POST /v1/messages`(**Haiku 4.5**, temp 0, max 1024)를 raw fetch 호출(SDK 미도입 — 저장소 관례·의존성 최소화). **KST 오늘 앵커**를 서버 계산해 상대표현 해석, 코드펜스 제거+JSON.parse+`ExtractSchema.safeParse`. 라우트는 **세션 소유자 게이트**(getUser→401, LLM 비용 남용 방지)·실패 이유별 상태코드(no_key 503·parse 502). 실측: 미인증 POST→401 JSON. ③ **위젯 UI**: `SchedulePanel`(목록↔정리 2모드) — 구분 필터 칩+약속 인라인 수정/삭제, '카카오톡 정리하기'→붙여넣기(클립보드 원클릭)+[정리하기]→**후보 검토**(문장 편집·대상 드롭다운·개별 제외·인라인 새 구분)→[저장](일괄 insert+토스트). `useCircleData`(getUser+RLS 읽기/쓰기·refresh), `useExtract`(translate 훅 패턴). ConfigEditor=구분 관리(추가/이름·색 수정/삭제, 삭제 시 약속은 '미지정'으로 잔존). 선택 필터는 config에 저장(타일↔전체 공유). dataMode static·sensitive·copyBehavior config, 크기 8×12(min 4×6). tsc·next build 그린(신규 라우트·팔레트 등록 확인). 비범위: 카카오톡 실시간연동·이메일 발송·캘린더 동기화 없음(붙여넣기 입력만). |
| 2026-07-01 | 지인 일정 정리 — **직접 추가 폼** + **추출 문구 간결화**(요구) | components/widgets/circle-schedule/SchedulePanel.tsx, lib/api/circleScheduleClient.ts | ① **직접 추가(수동 등록)**: SchedulePanel에 `manual` 모드 추가 — 목록 하단 버튼을 **[직접 추가]·[카카오톡 정리] 2개**로 분리. '직접 추가'는 내용·구분(드롭다운+인라인 새 구분)·날짜(선택)·시간(선택) 폼으로 약속 1건을 바로 저장. 날짜·시간→**`when_at`(ISO +09:00) + 표시 접미사**를 `buildWhenAt`로 생성(시간만 입력 시 KST 오늘 날짜로, 예 "저녁 (7/1 오후 5시)"; 날짜만이면 "(7/2)"; 없으면 접미사 없음). 타일·전체(SchedulePanel 공유) 모두에서 추가 가능(요구: 전체는 보기+추가, 화면 추가버튼도 별개 제공). ② **추출 문구 간결화**: 시스템 프롬프트를 '한 문장'→**핵심 명사구만**(동사·군더더기 제거)으로, 날짜는 **M/D**·시각은 사용자 표현 유지·시각만 있으면 오늘 날짜 포함으로 변경. 예(오늘 7/1): "내일 엄마와 병원 갑니다"→`엄마와 병원 (7/2)`. 대상(소연 등)은 그대로 사용자가 검토에서 지정. ③ **애매한 시각 낮 시간대 기본**(요구): 오전/오후가 불분명한 단순 "N시"는 **오전 9시~오후 8시**에 들도록 해석(9·10·11→오전, 12→낮 12시, 1~8→오후; content·when_at 반영), 예외(새벽 등)는 사용자가 수정. 예 "5시에 저녁"→`저녁 (7/1 오후 5시)`. 직접 추가 폼은 24h 시간 선택기라 무관. tsc·next build 그린. |
| 2026-07-01 | 지인 일정 정리 — 정리 화면 **텍스트 축소 + 대상자 선택/추가 통합** + **키 무효 오류 명확화** | components/widgets/circle-schedule/SchedulePanel.tsx, lib/api/circleScheduleClient.ts, app/api/circle-schedule/extract/route.ts | ① **카카오톡 정리 화면 컴팩트화**(요구: 텍스트 줄이기): 제목줄·"구분(대화 상대)" 라벨·상시 새구분 입력줄 제거 → 상단 **한 줄 `[대상자 선택 ▼] [＋] [✕]`**. `＋`는 평소 숨김, 누르면 새 대상자 인라인 입력(만들면 즉시 선택). 안내문 축약(placeholder "약속 관련 카톡 내용 붙여넣기", 검토 "N건 · 확인 후 저장", 빈결과 "약속을 찾지 못했어요."). ② **대상자 입력 단계 선택/추가**(요구): 상단 select로 이 대화의 대상자 지정 → 추출 후보 **전부 그 대상자로 기본 적용**(`applyTargetToAll`, 검토서 항목별 변경). ③ **키 무효 오류 명확화**: 실측으로 `.env.local`의 `ANTHROPIC_API_KEY`가 401(무효)임을 확인 → 클라이언트가 401/403을 `auth` 사유로 분리, 라우트가 "AI 키(ANTHROPIC_API_KEY)가 유효하지 않습니다…"로 안내(전엔 '잠시 후 재시도'로 오해). **유효 키 교체 후 추출 실동작 검증**: "내일 엄마와 병원"→`엄마와 병원 (7/2)`, "5시에 저녁"→`저녁 (7/1 오후 5시)`, 잡담 필터·빈결과 OK. tsc·eslint·next build 그린. |
| 2026-07-01 | 메모 **개별 비밀번호(화면 잠금)** + 클립보드 **기기 간 동기화 + 기기별 색** | memo/{types,MemoLock(신규),CompactView,ExpandedView,ConfigEditor}, clipboard/{types,useClipboardHistory,CompactView,ExpandedView}, supabase/migrations/20260701120002_pb_clipboard.sql(신규), output/types/database.ts | ① **메모 개별 비밀번호**(요구, 사용자 선택=화면 잠금): `MemoConfig.pwHash`(SHA-256)·`lockAfterMin` 추가. `useMemoLock`+`MemoLockPrompt`로 타일(Compact/Expanded) 잠금 게이트 — 비번 있으면 프롬프트로 본문 가림, 해제 후 절대 타이머로 자동 재잠금(리로드 시 재잠금). ConfigEditor: **잠금 시 본문 필드 숨김**(편집기 우회 방지)·비번 설정/변경/해제(변경·해제는 **현재 비번 확인** 필요)·자동잠금 시간. **화면 잠금만**(본문은 config에 평문·RLS) 명시 + 진짜 비밀은 금고 위젯 안내. ② **클립보드 동기화**(요구): localStorage(기기전용)→**Supabase `pb_clipboard`**(user_id+instance_id, RLS, **realtime**+replica identity full). `useClipboardHistory`를 Supabase(fetch+realtime 구독+포커스 재조회)로 재작성 — 같은 계정 모바일⇄PC가 같은 인스턴스 기록 공유. `device`(모바일/PC, UA+포인터 감지)로 기록별 **색 구분**(모바일=바이올렛·PC=시안: Compact 점, Expanded 좌측바+배지). `supabase db push`로 라이브 적용(local=remote). ⚠ 클립보드 텍스트가 서버 저장됨(전엔 로컬 유지, 사용자 요청으로 변경). tsc·next build 그린. |
| 2026-07-01 | 메모 비밀번호 **즉시 유지·즉시 잠금**(버그 수정) | components/widgets/memo/ConfigEditor.tsx | 증상: ConfigDialog는 로컬 `draft`만 갱신하고 하단 '저장'을 눌러야 영속화 → '잠금 설정' 후 X/취소로 닫으면 **비밀번호가 사라짐**. 수정(요구): MemoPasswordSection이 `useSaveWidgetConfig`로 **설정·변경·해제를 즉시 영속**(`apply`=onChange draft 동기화 + save 즉시 반영, ConfigDialog가 provider 안이라 동작). 결과: 비번 설정 즉시 유지되고 **타일이 바로 잠김**('저장' 불필요, 취소해도 유지), 해제는 화면 프롬프트로, 비번 제거 시 일반 메모로 복귀. tsc·eslint·next build 그린. |
| 2026-07-01 | 메모 **‘지금 잠금’ 버튼** + 클립보드 **‘전체’ 에러 수정 + 항목 삭제 버튼** | memo/{MemoLock,CompactView,ExpandedView}, clipboard/{useClipboardHistory,CompactView} | ① **메모 지금 잠금**(요구): `useMemoLock`에 `lock()`·`hasLock` 노출. 잠금이 설정된(해제 상태) 메모에만 상단에 잠금 버튼(Compact 우상단 `🔒 잠금`, Expanded 좌상단 `🔒 지금 잠금`) → 누르면 즉시 잠금(타이머 정리). 잠금 미설정 메모엔 미표시. ② **클립보드 ‘전체’ 에러 수정**: '전체' 열면 타일(Compact)+전체(Expanded)가 같은 instanceId로 동시 마운트 → realtime 채널 토픽 `pb_clipboard:<id>` 중복 충돌. `React.useId()`로 훅별 유니크 토픽(`…:<uid>`) 부여 + 채널 생성/구독 try/catch 방어(실패해도 포커스 재조회로 동기화). ③ **항목 삭제 버튼**(요구): CompactView 각 항목에 복사영역과 별도로 삭제(휴지통) 버튼 추가 — 2단계(휴지통→'삭제?') 확인, 기기 공유 저장이라 삭제 시 PC·모바일 모두 반영. tsc·eslint·next build 그린. |
| 2026-07-02 | 모바일 좁은 화면 **보드탭 표시**(헤더 2줄 반응형) | components/canvas/CanvasShell.tsx | 좁은 폭에서 고정폭 제목·툴바가 가운데 탭 영역(flex-1)을 0으로 찌그러뜨려 탭 미표시 → `<sm` 2줄(1줄 브랜드+툴바 ml-auto / 2줄 탭 `order-last w-full` 전체폭 스크롤), `sm+` 기존 한 줄 유지(`sm:flex-nowrap`). tsc 그린 |
| 2026-07-03 | 소제목 모드 **목차 fit 높이**(아래 공백 제거) + 타일 하단 **추가 버튼 축소**(전 위젯) | note/{collapseLayout,types,CompactView}, shared/QuickAdd, circle-schedule/SchedulePanel, lib/persistence/usePersistence(주석), collapseLayout.test.ts | 요구: 소제목 선택 시 아래 공백 제거 + 다른 위젯들 하단 '추가' 버튼 크기/여백 축소. ① **소제목(more) fit 높이**: `tocCollapseH(n)`(= ceil((104+28n+6)/54), GridCanvas ROW 48/margin 6 기준) — 타일을 목차 행 수에 딱 맞게 축소하고 아래 위젯이 그만큼 올라옴. 사용자 높이(base) 상한(넘치면 내부 스크롤), normalHeight로 펼침 복원. `NoteCollapseConfig.sections?`(개수만 사용, 구조적 캐스트라 호출부 무변경). 테스트 29개(fit 3케이스·상한·no-op 갱신). ② **추가 버튼 축소**: 공용 QuickAdd 접힘 버튼 py-1.5→py-0.5·text-[11px]·아이콘 12(≈30→22px, 12개 위젯 일괄) + 폼 여백 p-1.5→p-1·mt-0.5, circle-schedule [직접 추가·카카오톡 정리] py-1.5→py-0.5·text-[11px], 노트 타일 '＋ 소제목' py-0.5(21px). 실브라우저 검증(임시 /note-test, lg 1400px, 검증 후 삭제): 소제목 클릭→노트 534→210px(3소제목=4행)·아래 메모 592→268 상승·오버플로 없음·목차만, 펼침→534px+원위치 복원, QuickAdd 22px, 콘솔에러 0. (뷰포트 1036px에서 그리드 폭 1004<1024라 md 파생 배치로 보였던 것은 브레이크포인트 정상 동작 — 코드 무관) tsc·lint 0·test 29/29·next build 그린. |
| 2026-07-03 | 노트 표시 모드 **[펼침·소제목·제목만]** + 빈 머리말 한 줄 | note/{collapseLayout,types,CompactView,RichTextArea,NoteEditor}, lib/persistence/usePersistence(주석), lib/widgets/note/collapseLayout.test.ts | 요구: 절반→소제목으로 바꾸고 소제목=목차만, 펼침=소제목+내용 전부 리스트(크기는 사용자만 결정), 빈 머리말이 큰 영역 차지하지 않게. ① **펼침(normal)**: 타일에 머리말+각 소제목+**그 내용까지** 세로 스택(살균 2중, mounted 게이트, 빈 본문 섹션은 제목만) — 소제목 클릭=단독 열기 유지. ② **소제목(more, 저장 키 하위호환)**: 목차만(머리말·내용 숨김), **높이 자동 절반 제거**(computeNoteCollapse 'more'가 base 유지/복원만) — 크기는 사용자가 드래그로. 구버전 절반 높이로 저장된 노트는 재클릭 시 원래 높이로 self-heal. ③ **제목만(title)**: 기존 그대로(TITLE_COLLAPSE_H, 유일하게 높이 변경). ④ **빈 머리말 한 줄**: RichTextArea `slim` prop(min-h-28→min-h-10) — 머리말(선택)만 적용, 내용 있으면 자라남, 섹션 영역 112px 유지. 접기 테스트 14개 재작성(소제목=높이 불변·self-heal·title 왕복). 실브라우저 검증(임시 /note-test 하네스, 검증 후 삭제): 펼침 내용 스택·소제목 목차만(534px 유지)·제목만 102px·제목만→소제목 높이 복원·빈 머리말 43px+placeholder, 콘솔에러 0. tsc·lint 0·test 27/27·next build 그린. |
| 2026-07-03 | 노트 **제목 통합** — 헤더 제목=노트 제목(config.title), 전체보기 이름 표시, 타일 안쪽 제목 줄 제거 | lib/widgets/contract.ts(+output 동기화), canvas/{GridCanvas,FocusOverlay}, note/{index,CompactView} | 요구: 맨 상단 '노트' 글자가 곧 제목(변경 가능)이고 전체보기에 그 이름이 나오며, 그 아래는 소제목 목록. ① **컨트랙트 확장**: `WidgetDefinition.instanceTitle?(config)`(인스턴스 제목 읽기)·`renameInstance?(config,title)`(새 config 반환, 반환은 SaveConfigFn과 같은 unknown — 캐스트 호환) 옵션 추가, 노트만 구현(`config.title`). ② **프레임 헤더**: instanceTitle 위젯은 헤더 제목=config 제목(비면 displayName), **더블클릭 변경이 renameInstance→config 저장**(기기 간 동기화, 노트는 기기별 pb:title 미사용). ③ **전체보기(FocusOverlay)**: 상단 h2·aria-label을 '노트'→노트 제목으로(비면 displayName), config가 라이브라 제목 입력 시 즉시 반영. ④ **타일**: 안쪽 제목 줄(클릭→전체) 삭제 — 상단은 [공유/첨부 아이콘+접기 토글] 한 줄, 그 아래 머리말+소제목 리스트(전체 진입=헤더 '전체' 버튼·소제목 클릭). ExpandedView 제목 입력란은 수정용으로 유지. 실브라우저 검증(임시 /note-test 하네스, 검증 후 삭제): 헤더 더블클릭 개명→config 저장·pb:title=null, 전체보기 이름/aria 동기화, 제목 입력 실시간 반영(500ms 디바운스), 빈 제목→'노트' 폴백, 단일 섹션 모드 정상, 콘솔에러 0. ⚠ dev 404 전체 발생은 직전 `next build`가 남긴 .next 충돌 — `.next` 삭제로 해소(코드 무관). tsc·lint·test 27/27·next build 그린. |
| 2026-07-03 | 노트 소제목 **추가 진입점 확장**(타일 '＋ 소제목' + 전체보기 버튼 강조) | note/{CompactView,NoteEditor} | 사용자가 소제목 추가 방법을 못 찾음(전체보기 하단에만 있었음) → ① **타일 '＋ 소제목' 버튼**: 섹션 리스트 아래(있을 때)·본문 아래(없을 때, '제목만' 접기 제외) 상시 노출 — 클릭 시 빈 섹션 생성 저장 + `setPendingNoteSection`으로 **그 섹션 단독 편집 화면을 바로 오픈**(소제목·내용 즉시 입력). `useSaveWidgetConfig` 사용, openFocus 없으면 숨김(어포던스 패턴). ② 전체보기 하단 '소제목 추가' 버튼을 primary 톤(dashed primary/50·hover bg-primary/10)으로 강조. 실브라우저 검증(임시 /note-test, 검증 후 삭제): 무섹션 타일 버튼 노출→클릭→섹션 생성+단일 편집 오픈→제목 입력 저장→타일 리스트 반영→2번째 추가→전체 보기 스택+강조 버튼, 콘솔에러 0. tsc·lint·test 27/27·next build 그린. |
| 2026-07-03 | 노트 **소제목(섹션)** — 한 노트에 같은 분류를 묶기 | note/{types,sections(신규),focusSection(신규),prose(신규),RichTextArea(신규),NoteEditor,ExpandedView,CompactView,ConfigEditor,index}, lib/widgets/note/sections.test.ts(신규) | `NoteConfig.sections?: NoteSection[]`(id·title·html, 기존 `html`은 **머리말로 무마이그레이션 유지**). ① **타일**: 소제목 없으면 기존 그대로(제목+본문), 있으면 머리말 미리보기(있을 때)+**소제목 리스트**. 제목 클릭=전체(머리말+전 섹션), **소제목 클릭=그 섹션만** 열기(모듈 스코프 `focusSection` 핸드오프 — 일회성 UI 의도라 config 미경유, peek/clear 분리로 StrictMode 안전). ② **전체보기**: 머리말+섹션 세로 스택, 섹션별 소제목 입력(디바운스)+본문 편집, ▲▼ 순서이동·2단계 삭제·[＋ 소제목 추가]. 단일 섹션 모드엔 [← 전체 보기]. ③ **편집기 구조**: NoteEditor를 다중 영역으로 재작성 — `RichTextArea`(언컨트롤드 contentEditable+살균 붙여넣기/드롭+이미지+ImageResizer) 추출, **공유 툴바 1개가 마지막 포커스 영역을 추적**(selectionchange→activeEditorRef 재타깃; richText 명령이 selection 기반이라 가능). 저장은 부모 소유(persistKey가 innerHTML 살균→올바른 슬롯, 영역별 디바운스; 순수 `sections.ts` 연산이 no-op이면 같은 참조 반환→저장 생략). NOTE_PROSE_CLASS는 `prose.ts`로 분리(순환 import 방지). **버그 픽스**: RichTextArea ref 콜백을 useCallback으로 memoize — 인라인이면 렌더마다 detach/reattach로 활성 영역이 첫 영역으로 리셋돼 툴바가 머리말에 적용됐음(실브라우저에서 포착). ④ ConfigEditor '노트 비우기'가 sections도 삭제. tsc·lint·test 27/27(신규 6)·next build 그린. **실브라우저 검증**(임시 /note-test, 검증 후 삭제): 소제목 클릭→해당 섹션만, 전체 보기 복귀, 섹션 본문 편집→`sections[i].html`만 갱신, 굵게(공유 툴바)→캐럿 영역 정확, 제목/순서/추가/2단계삭제, 첫 소제목 추가 시 타일 리스트 전환, '제목만' 접기 시 리스트 숨김, 콘솔에러 0. |
| 2026-07-02 | **전면 감사 업그레이드**(4방향 병렬 감사→일괄 수정) + 노트 **'제목만' 접기** + **API 인증 게이트** + **lint 0** | note/*, canvas/*, lib/persistence, lib/widgets/persistence, app/api/8라우트, lib/api/{requireUser(신규),stock/tokenStore}, widgets/{todo,subscriptions,shared,image-slider,clipboard,credentials,essential-info,memo,fx,outfit,timer}, eslint.config, .env.example(신규), package.json | ① **노트 '제목만' 접기**(요구): collapse 레벨 `title` 추가(`TITLE_COLLAPSE_H=2`, minSize.h 3→2) — 타일이 제목 한 줄(≈102px)로 접히고 아래 위젯이 올라옴. 토글 [펼침·절반·제목만] 3분할(구 접기/더접기 개명). **제목 클릭=바로 전체보기**(신규 `WidgetFocusProvider`/`useOpenWidgetFocus`, GridCanvas 제공), 전체보기에 **'제목만 접기' 버튼**(접기+닫기 한 번에, 신규 `FocusCloseProvider`; CanvasShell이 GridCanvas handleCollapse를 `onRegisterCollapse`로 받아 FocusOverlay에도 브레이크포인트 인식 접기 제공→모바일 기기-로컬 레이아웃 일관). 단위테스트 +5(21/21), lib/widgets/note/collapseLayout.ts **죽은 복제본 삭제**. ② **API 인증 게이트(HIGH)**: 비용유발 8라우트(translate·geocode·weather·air-quality·news·fx·stocks·stocks/stream SSE)에 `requireUser()` 401 게이트 — 익명 호출 8/8 401 실측. ③ **할일 체크박스 타일/전체에서 즉시 토글**(HIGH, 기존 disabled). ④ **저장 유실 방지**(HIGH): pagehide/visibilitychange(hidden) 즉시 flush + 로그아웃 전 `flushNow()` await(AccountMenu onBeforeSignOut). ⑤ 오버레이 3종 보강: FocusOverlay·ConfigDialog **Esc 닫기**, ConfigDialog·SettingsDialog **배경 스크롤 잠금+overscroll-contain+safe-area 하단 패딩**. ⑥ WidgetFrame 드래그 핸들 `touch-none`(모바일 드래그 스크롤 경합). ⑦ 위젯/보드 삭제 시 **기기-로컬 키 정리**(pb:title/color/fontscale·pb:mlayout). ⑧ usePoll 스테일 클로저(dataRef)·구독 임박배지 `text-positive`→`text-destructive`(등락토큰 오용)·이미지 슬라이드 상한(20장/≈2MB+안내)·터치타겟 `pointer-coarse:size-9`(RefreshBar·QuickAdd·ImageManager)·demo dead code 삭제. ⑨ 운영: `.env.example`(18키) 신설, KIS tokenStore Vercel에서 `os.tmpdir()`(EROFS 회피)·실패로그 1회, share 라우트 에러로그 message/code만, `/palette-test` 프로덕션 404 가드, `pg`→devDependencies. ⑩ **lint 25→0**(react-hooks v6 규칙: 실수정 12곳 — ref 미러 useEffect화·선언순서·uSES mounted·minuteKey 파생·자기참조 rAF ref 경유 + 근거 명시 disable 7곳, `_workspace/scripts` ignore). tsc·lint·test 21/21·next build 그린, 실브라우저(스텁 provider QA 페이지): 제목만→본문숨김→제목클릭 전체보기→제목만 접기(접힘+닫힘)→펼침 복원, 콘솔에러 0. 잔여(보고서 기재): 다중 세션 realtime 병합(L)·포커스 트랩·클립보드 서버저장 경고문 |
